# Master TODO — repo-wide

*Rebuilt 2026-09-10 — ported from `golf-master`'s TODO.md system (see
[`docs/methodology/todo-system.md`](docs/methodology/todo-system.md)).
Previous content (a 5-line `Fixes` list + a folded-in `Totals model`
changelog) is migrated below, not discarded.*

*Item format (see `docs/methodology/todo-system.md`): `- [ ] \`#slug\`
**Title** <!-- id: slug --> — what; why/done-when; where`. **Point to items
with \`#slug\`**, **to sections with \`#sec-*\`** (e.g. `#sec-totals`, not
`§3`). Expand stubs before they land. Index: `python scripts/todo_sweep.py
refs`.*

*No-lookahead gate (root `CLAUDE.md`): pre-game features use only
information available before kickoff; a result-informed feature is tagged
`result_lookahead` and quarantined in UI.*

---

## 0. NOW — this week, in order `#sec-now` <!-- section: sec-now -->

*(empty — pull the next sitting-sized item from a unit section when ready.
Soft cap ~8 open items here; `todo_sweep.py check` warns past that.)*

---

## 1. System maker — CLI, Flask UI, backtest engine (`cfb_system_maker/`) `#sec-system-maker` <!-- section: sec-system-maker -->

- [ ] `#system-maker-delete-saved-systems` **Let saved systems be deleted from the web UI** <!-- id: system-maker-delete-saved-systems --> <!-- plan: needed --> — currently `SavedSystem` JSON can be created and listed but not removed; add a delete action (UI button + route) that removes a saved system's JSON and updates the list view. **Done when** a saved system can be deleted from `/system` without editing files by hand. **Where:** `web.py` (`create_app`, `/system` route), wherever `SavedSystem` JSON is persisted. Migrated 2026-09-10 from the old `TODO.md` "Fixes" list.
  - Original note also said "better management of the systems" beyond just delete — that half was never specified. If there's more to it (rename, duplicate, bulk-clear), say so and this item can be split or expanded; don't infer scope here.

---

## 2. Data — warehouse, ingestion, scrapers `#sec-data` <!-- section: sec-data -->

*Cross-cutting: PFF, Action Network, odds, GraphQL, DuckDB — not owned by one unit.*

*Source-rationalization items below are the numbered steps of
[`docs/superpowers/plans/2026-09-08-warehouse-rationalization-master.md`](docs/superpowers/plans/2026-09-08-warehouse-rationalization-master.md)
§8 and run **in order** — each is gated on the one above. Step 0 (preflight)
is green: `python scripts/verify_warehouse_plan.py` exits 0. `#gql-relation-keys`
landed 2026-09-10, so the chain is open at the top and
`#warehouse-remeasure-containment` is now the gate on everything below it.*

### Source rationalization — the gated chain

- [x] ~~`#gql-relation-keys` **Give `coachSeason` and `teamTalent` relation keys, then re-scrape**~~ <!-- id: gql-relation-keys --> — done 2026-09-10 (`7d1751a`): added both to `GQL_RELATION_KEYS` (`coach` → id/firstName/lastName, `team` → teamId/school/conference; the GraphQL types are `CoachSeason`/`TeamTalent`, not the root field names) and re-scraped on the rotated key. `stg_gql.coach_season` now carries `coach_id`/`team_teamId`, `stg_gql.team_talent` carries `team_teamId` — both joinable. **Row counts unchanged** (12,564 / 2,413), so the old dumps were not short after all; the defect was joinability, not completeness. 252 coach_season and 8 team_talent rows have no team in `currentTeams` — real, not a mapping failure. Write-up: [`docs/gql-relation-keys-2026-09-10.md`](docs/gql-relation-keys-2026-09-10.md), which also records that `duckdb --only` rebuilds rather than adds. Unblocks the chain.
- [x] ~~`#warehouse-remeasure-containment` **Re-measure containment and record the buckets**~~ <!-- id: warehouse-remeasure-containment --> — done 2026-09-10: both repaired pairs re-measured against the new pull; **both stay in Bucket B**, so no drop list changes. Two substantive moves: `coach_season` now merges by **clean join** — `coach_id` became shared and `(coach_id, team_id, year)` is unique on both sides, matching 1,961/1,961 REST rows, zero unmatched — and `team_talent`'s column direction **reversed** (GraphQL holds the FK, REST only a school name), yet REST is still not droppable because 17 REST school-seasons have no GraphQL twin (Jacksonville, St. Francis (PA)). New reusable tool `scripts/audit_pair_columns.py` makes §3's hand-measured tables reproducible. Write-up: [`docs/warehouse-containment-remeasure-2026-09-10.md`](docs/warehouse-containment-remeasure-2026-09-10.md); §3 Bucket B restated in the plan. Verifier 0 failed, 875 passed.
- [x] ~~`#core-merge-bucket-c` **Merge the complementary pairs into `core`**~~ <!-- id: core-merge-bucket-c --> <!-- plan: docs/superpowers/plans/2026-09-08-warehouse-rationalization-master.md --> — done 2026-09-10: all 9 actionable pairs, in two passes. **Pass 1 (5 new tables):** full outer joins on the key, all guarded and key-unique — `core.dim_coach` 1,842 + `coach_name_conflicts` 2, `dim_draft_pick` 13,080, `dim_recruit` 93,363, `fact_team_talent` 2,430, `fact_coach_season` 12,564 + `coach_season_unmatched` 0. **Pass 2 (4 merges into existing tables)** — [`docs/core-merge-bucket-c-2026-09-10.md`](docs/core-merge-bucket-c-2026-09-10.md), `python scripts/audit_core_merges.py`: `conference` added `division` (256/256 match; `srName` fills 1/256, not carried); `game` repaired 3,714 home / 3,985 away conference ids that a non-injective name lookup had picked arbitrarily among 44 duplicate `dim_conference` names, and sent its 78,030 pre-2012 surplus to the new `core.fact_game_historical` per ADR-0001; `calendar` is a **measured no-op** (0 in-span gaps; taking its 2002–2011 rows would expand `fact_game` through `dim_week`'s bound). Every pre-existing `core` row count is unchanged, validated on a scratch copy first. Pinned by `tests/test_core_merges.py` (16). `lines` split out to `#core-fact-game-line-repoint` — it is not a merge — and closed the same day: §7's full-game match-count gate **was** exercised, rejected a repoint, and the tape landed as a union (`core.fact_game_line` 39,006 → 47,581). All 9 actionable pairs are done. `recruiting_team` stays deferred (no id↔name bridge; see `#core-dim-team`).
- [x] ~~`#core-fact-game-line-repoint` **Repoint `core.fact_game_line` at `stg_gql.game_lines`**~~ <!-- id: core-fact-game-line-repoint --> — done 2026-09-10, **as a union rather than a repoint**. §7's gate was run in both directions first and rejected the repoint: it drops 278 REST offers `game_lines` has no row for (all 2026, on `draft kings`/`bovada`/`draftkings`) and overwrites ~700 values where both sides are populated and differ. Built instead as a full outer on `(game_id, provider_key)` with `coalesce(rest, gql)` — REST wins a conflict because that is what `core` already held, so **no existing value changes** and the merge is purely additive. `core.fact_game_line` 39,006 → 47,581 (38,728 both / 8,575 gql / 278 rest), five new books (circa, fanduel, betmgm, bet365, pinnacle), `core.dim_lines_provider` 12 → 17, and the 700 conflicts preserved in `core.fact_game_line_conflicts` rather than discarded. **`game_lines` spells a missing number NaN, not NULL** — the first build coalesced 3,414 NaNs into REST NULLs and the slow suite caught it; nulled out before the join and pinned by `test_no_nan_reached_the_line_table`. `has_line` stays REST-defined, so 90 line rows on 16 games now sit under `has_line = false` (was 0) — documented, not hidden. [`docs/core-merge-bucket-c-2026-09-10.md`](docs/core-merge-bucket-c-2026-09-10.md).
- [x] ~~`#lines-provider-key-split` **`draft kings` and `draftkings` are one book under two keys**~~ <!-- id: lines-provider-key-split --> — done 2026-09-10. [`docs/lines-provider-key-split-2026-09-10.md`](docs/lines-provider-key-split-2026-09-10.md). **It was not only a naming problem.** CFBD emits both spellings *inside the same `lines` array* on 215 games, and `Draft Kings` is a strictly degraded duplicate — across all 215 it never carries a value `DraftKings` lacks, while the other supplies 161 `spreadOpen`, 121 `overUnderOpen` and ~185 moneylines. Four consumers were each resolving that duplicate **by array order**: `normalize._select_line` (so `games.csv` carried the degraded nulls), `_build_fact_game_line` (first-occurrence), `enrich._build_line_move_index` (null line-move feature for a game whose open sat in the same payload), and the merge view. Same non-injective-key shape as the `dim_conference` collision. Fix: `normalize.provider_key` owns the alias map and `duckdb_core._provider_key` delegates; `normalize._best_of_book` picks the most complete row **for the book already selected** (which book is chosen is untouched); the merge view re-establishes the grain with `QUALIFY row_number()` because the alias maps two provider ids onto one key (CFBD's 100 and the synthetic 888888) and the full outer join fanned out — that is how it was caught. `fact_game_line` 47,581 → 47,366, `dim_lines_provider` 17 → 16, 56 games **gain** a DraftKings line. Verified: 0 rows lost, 0 non-NULL values became NULL, 51 values changed and all on `draftkings`. `games.csv` rebuilt for all 14 seasons, unchanged at 13,841 rows. **Caesars family left split on purpose** — never share a game, disjoint season ranges, no degraded twin to justify a winner; pinned by a test so a future merge is a decision, not a drift. New: `#lines-synthetic-provider-ids`.
- [x] ~~`#lines-synthetic-provider-ids` **ActionNetwork books carry invented CFBD provider ids**~~ <!-- id: lines-synthetic-provider-ids --> — done 2026-09-10, **and the premise was backwards**. 888888 and 999999 are **CFBD's own** — its GraphQL `linesProvider` table ships 12 rows including `888888 DraftKings` alongside `100 Draft Kings`, the same vendor duplicate its REST `lines` payload emits. `_AN_BOOK_PROVIDER` maps to those correctly and invents nothing. **The real risk was the other five.** Circa 30, Pinnacle 49, FanDuel 68, BetMGM 69 and Bet365 75 have no CFBD id, and `ELSE book_id` put the raw AN id straight into CFBD's namespace — four of the five sit inside CFBD's live range (38–1004), on 16,469 `stg.game_lines` rows. Had CFBD issued 68 to another book, the explode would rebuild `stg.lines_provider` with that name, the AN insert would skip (id exists), and ~5,300 FanDuel rows would silently join to the wrong book. Now offset by `_AN_ID_OFFSET = 9_000_000` (seven digits because CFBD already uses 999999), via `_an_provider_id`. **Migration landed 2026-09-11** via a full `refresh_cfbd.py` pass — the only safe route, because `backfill_gamelines_from_actionnetwork` is not idempotent against an already-backfilled `stg.game_lines` (re-running it fanned 63,831 rows to 80,359 with only 63,971 distinct keys), while the refresh rebuilds `game_lines` from raw and backfills once. Verified on live: the five offset ids carry 16,478 rows (Circa 9000030 5,350 / FanDuel 9000068 5,300 / BetMGM 9000069 5,061 / Bet365 9000075 540 / Pinnacle 9000049 227), every one resolves in `stg.lines_provider`, **no bare 30/49/68/69/75 remains**, `stg.game_lines` 63,843 rows at 63,843 distinct keys (grain clean, no fan-out), `core.fact_game_line` 47,366 → 47,380 with 0 NULL `provider_key` across 16 books, and all 318 tables survived. Sign convention re-audited after the identity change: unchanged, every book still home-relative. Downstream is unaffected either way: `provider_key` is derived from the book *name*. Pinned by two tests in `tests/test_duckdb_load.py`. Follow-up: `#an-backfill-not-idempotent`.
- [ ] `#an-backfill-not-idempotent` **`backfill_gamelines_from_actionnetwork` double-processes an already-backfilled table** <!-- id: an-backfill-not-idempotent --> — found 2026-09-10 while migrating `#lines-synthetic-provider-ids`. Running it twice without rebuilding `stg.game_lines` from `raw.gql_game_lines` first takes 63,831 rows to **80,359 with only 63,971 distinct `(gameId, linesProviderId, period)`** — a fan-out — and collapses `period='game'` from 47,303 to 63,831, i.e. it re-reads its own output as input. Harmless on the nightly path, which always explodes from raw first, and a trap for any hand-run: this is the same class as the rebuild that silently dropped `stg.an_scoreboard`. **Still open after 2026-09-11** — the full refresh that landed `#lines-synthetic-provider-ids` sidesteps the defect by always rebuilding `stg.game_lines` from raw, it does not fix it; a hand-run against a populated table still fans out. What is gone is only the *blocking* framing: nothing is now waiting on this. Shape: make it rebuild from `raw.gql_game_lines` itself rather than unioning onto whatever `stg.game_lines` currently holds, or refuse outright when the table already contains `line_source` values it wrote. Done when running it twice in a row leaves row count and grain unchanged. P2
- [x] ~~`#lines-spread-sign-convention` **The 8,575 ActionNetwork line rows are unvalidated on spread sign**~~ <!-- id: lines-spread-sign-convention --> — done 2026-09-10, **verdict: all 17 books are home-relative, nothing needed negating**. [`docs/lines-spread-sign-2026-09-10.md`](docs/lines-spread-sign-2026-09-10.md), `python scripts/audit_line_sign_convention.py`. Two independent checks because either alone has an excuse: sign agreement with a 4-book reference average on the same game (reading only disagreements where `|ref|>3`, since books straddle zero on pick'em), and `spread + (home_points − away_points)` against the realised margin, which cancels to ~0 when home-relative and lands at ~2× the spread when inverted. Every cover rate 45–56%, every residual within ±2.5 of zero; an inversion would sit near 0%/100% and ±20. **Circa is a real outlier and not a sign problem** — 1.8% sign disagreement at `|ref|>3` against ~0.1% elsewhere and mean|diff| 2.46 against ~0.5, but cover 51.7% and residual 1.18; likely early posting or later capture timestamps, untested. Pinned three ways in `tests/test_core_merges.py`, including that the five AN books are in the graded set at all, so the bounds cannot pass vacuously. Bounds deliberately loose: they catch a flip, not a sharp book. **`spread_open` and the AN moneylines stay unchecked** — noted in the write-up's limits.
- [x] ~~`#refresh-rebuilds-games-csv` **`refresh_cfbd.py` never rebuilds `games.csv`**~~ <!-- id: refresh-rebuilds-games-csv --> — done 2026-09-10. `cli._build`'s body is extracted as `cfb_system_maker.cli.rebuild_processed_games(data_dir, seasons, provider)` — returning `(rebuilt, kept, total)` and keeping the per-season replacement contract — and `refresh_cfbd.py` calls it as `_rebuild_games_csv(args.season)` after the flattens, non-fatal like them. Ran it on the live tree: `data/processed/games.csv` was two days stale (09-08 01:05 vs `raw/games_2026.json` 09-10 05:00) and is now 326 games for 2026 plus 13,515 kept = 13,841. **Both standing `-m slow` failures are gone** — the coverage-drift one was pure staleness, and the `assert -57.5 == -55.5` one resolved to a single real divergence caused by `#core-fact-game-line-repoint`'s union (game 401868326: CFBD carries DraftKings with a null spread, the AN tape has −23.0). The agreement test now pins the permitted divergence rather than asserting equality — REST value must be NULL **and** `_source <> 'rest'` — so an overwritten REST value still fails. That game is also the one place `fact_game.selected_spread` (NULL) and `fact_game_line.spread_close` (−23.0) disagree; documented, not fixed, since fixing it moves the spread model's input.
- [x] ~~`#warehouse-drop-superseded` **Drop Bucket A's REST sides and the 7 dead columns**~~ <!-- id: warehouse-drop-superseded --> — done 2026-09-10, **2 of 3 pairs plus all 7 columns**. [`docs/warehouse-drop-superseded-2026-09-10.md`](docs/warehouse-drop-superseded-2026-09-10.md), `python scripts/verify_warehouse_plan.py`. R6 passed for `draft_positions` (29/29 keys) and `draft_teams` (32/32) — the latter **only on values**: `nickname` exists on both sides and means different things (REST `Bengals`, GraphQL `Cincinnati`), with REST's value carried by GraphQL's `mascot`. A false *shared*, mirror of §4's false *exclusive*; noted in `audit_pair_columns.py`'s docstring. **`predicted_points` fails R6 and stays** — its REST rows lose `down`/`distance` at the API (`raw/predicted_points.json` is a flat list of `{predictedPoints, yardLine}`), so 10,140 un-keyed rows cannot be aligned to GraphQL's 19,800-cell grid and only 13% match within 0.005; the pair leaves Bucket A. Implemented as loader behaviour, not `DROP TABLE`, since `stg` is rebuilt from `raw` every refresh: `_SUPERSEDED_REST` skips the two tables at explode time, `_DEAD_COLUMNS` sweeps six columns after every load with the existing fail-closed guard, and the seventh (`stg.an_team.overtime_losses`) was written by name in `_AN_TEAM_SQL` and removed there. `stg` 147 → 145 tables, all-NULL columns 11 → 4 (future-dated scores only). Verifier's check is now **inverted** — the 7 must be absent, and one that reappears fails. Proven self-healing by re-running `explode_payloads`/`explode_an_children` on a scratch copy: nothing came back. Pinned by `tests/test_drop_dead_columns.py` (10).
- [x] ~~`#predicted-points-grid-key` **REST `predicted_points` throws away `down`/`distance` at scrape time**~~ <!-- id: predicted-points-grid-key --> — **code fixed 2026-09-10; the R6 re-take waits on the next pull.** `_scrape_grid` now stamps the `(down, distance)` it called with onto every returned row, at the only place that still knows which grid cell produced it; the call parameters win over anything of the same name in the payload. The test fake was echoing `down`/`distance`, which made the defect untestable — it now behaves like CFBD and returns neither, and `test_grid_stamps_the_call_parameters_onto_every_row` asserts the full 4×30 cell set. **`raw/predicted_points.json` is still the old unkeyed dump** — 10,140 rows of `{predictedPoints, yardLine}`, and 10,140 does not divide by the 120 calls so the key is not recoverable from row order either. The next `scrape --only predicted_points` produces a keyed file; only then can `#warehouse-drop-superseded`'s R6 verdict be re-taken. Note REST's grid is distance 1–30 while GraphQL covers 1–99, so REST will remain a subset on coverage regardless.
- [x] ~~`#stg-gql-collapse` **Collapse `stg_gql` into `stg`, in one commit**~~ <!-- id: stg-gql-collapse --> <!-- plan: docs/superpowers/plans/2026-09-08-warehouse-rationalization-master.md --> — done 2026-09-10 in one commit. [`docs/stg-gql-collapse-2026-09-10.md`](docs/stg-gql-collapse-2026-09-10.md), `python scripts/collapse_stg_gql.py --dry-run`. 38 transport-tagged names became **3**. **The premise was wrong: zero suffixes was never reachable.** `predicted_points` failed R6 and kept its REST side, and merging a pair into `core` never retires its `stg` sources because `core` is built *from* them — `_build_dim_draft_pick` reads both on every `build_core`. So `calendar_gql`, `draft_picks_gql` and `predicted_points_gql` are **permanent**; ADR-0003 and plan §2 are amended to say so, and the collapse went ahead because §2's argument never rested on the prediction. 40 tables moved, 0 collisions; `stg.gameMedia` → `game_media` and `stg.gamePlayerStat` → `game_player_stat` rode along (a two-entry `_REST_STG_RENAMES` map, **not** a camelCase rule — both are GraphQL entity names that are also dump stems, which was the trap). Code surface was **95 qualified literals + 124 bare `"stg_gql"` strings across 20 files**, not the ~50 the plan measured. `scripts/migrate_gql_stg_names.py` deleted — with `stg_gql` gone it would recreate the schema. Verifier's structure checks **inverted**; `promote_to_motherduck.DEFAULT_SCHEMAS` drops `stg_gql` too. Verified beyond the suite by re-running `build_core`: all 18 `core` tables rebuilt at identical row counts. 896 tests pass, slow suite unchanged.
- [x] ~~`#rebuild-temp-path-is-fixed` **Two concurrent rebuilds destroy each other's `cfb.duckdb.building`**~~ <!-- id: rebuild-temp-path-is-fixed --> — done 2026-09-11. `build_duckdb` staged every rebuild at the **fixed** path `cfb.duckdb.building` and opened with `if tmp_path.exists(): tmp_path.unlink()`, so a second rebuild starting mid-write deleted the file the first was still writing into, and both then raced on `tmp_path.replace(db_path)`. The atomic replace is what made it easy to miss: it makes a *single* rebuild safe and says nothing about two. Fixed with an exclusive lock — `_exclusive_rebuild`, a DuckDB database held open for the life of the rebuild, so the OS drops it on process death and there is **no stale-lock case to get wrong**; the body moved to `_build_duckdb_locked` and clearing stale `.building` debris is now safe because the lock proves nothing else is writing it. A lock rather than per-process temp paths on purpose: unique paths would let two rebuilds burn ~20 minutes each so one could silently win, and two simultaneous rebuilds are never wanted. `refresh_cfbd.py` catches `RebuildInProgress` and exits 1 with one line instead of a traceback. **Cross-process only** — DuckDB shares an instance per file inside a process, which is the right scope but means `tests/test_rebuild_lock.py` proves the refusal through a real subprocess; an in-process `pytest.raises` passed for the wrong reason first time. 4 tests. **Note the cause of 2026-09-11's damage is NOT settled** — see `#rebuild-dropped-an-tables`; this race is real and worth closing on its own, but it does not explain the 05:00 run, which was alone.
- [ ] `#rebuild-dropped-an-tables` **A lone rebuild produced a `stg` with no `an_*` tables** <!-- id: rebuild-dropped-an-tables --> — found 2026-09-11. The 05:00 scheduled `refresh_cfbd.py` ran with **nothing else touching the warehouse** and still built a `stg` missing `an_scoreboard`, `an_market`, `an_team` and `an_linescore`; `stg.game_lines` came out in the bare GraphQL shape with no `period`, so `_backfill_gamelines` never ran, and `build_core` then died in `_merge_game_lines` on `WHERE l.period = 'game'` — leaving the live warehouse 16 tables short. **The explode is not the culprit**: re-running `explode_payloads` in isolation against the same `raw` rebuilt all five AN tables at identical counts (`an_scoreboard` 10,868, `an_market` 88,426), and `raw.an_scoreboard` was present and intact in the damaged DB. So the likeliest remaining path is `explode_an_children` failing and reporting into the list `refresh_cfbd.py` was discarding at `db_path, _ = build_duckdb(...)`. That discard is now fixed, so **the next failure prints its own cause** — which is why this item is open rather than guessed at. Same class as the rebuild that silently dropped `stg.an_scoreboard` before. **Done when** the failure is reproduced with its error message in hand, and either fixed or shown to be a one-off with a guard that makes a missing `an_*` fatal to the rebuild rather than to `build_core` 200 lines later. P1
- [ ] `#graphql-dumps-never-refreshed` **`refresh_cfbd.py` refreshes REST but never re-pulls `data/graphql/*.json`** <!-- id: graphql-dumps-never-refreshed --> — found 2026-09-11 by the refresh that landed `#lines-synthetic-provider-ids`. The run rebuilt `stg` from scratch and `stg.game` came back at **112,672 rows, byte-identical in count to before**, because the refresh scrapes REST and reflattens AN/PFF/odds but nothing on that path touches the GraphQL dumps. `data/graphql/game.json` is dated **Aug 28 11:09**, 14 days stale; most of the other 49 dumps are the same pull, `gamePlayerStat.json` is Jun 13, and only `coachSeason.json` (Sep 10) was re-pulled by hand. **This is not one stale table — every `core` builder that reads a `stg` GraphQL table is merging fresh REST against a dump of unknown age**, which is exactly the class of silent divergence the rationalization chain existed to close. Visible symptoms today: 412 of 2026's GraphQL games still say `status='scheduled'` with a start date in the past, and CFBD dropping game 401866625 from REST left a GraphQL-only orphan that broke `test_fact_game_did_not_grow_to_hold_graphql_rows` (test premise since corrected, and `test_graphql_only_games_stay_a_handful` now bounds the set at 25). **Audit half done 2026-09-11** — [`docs/graphql-dump-staleness-2026-09-11.md`](docs/graphql-dump-staleness-2026-09-11.md), `python scripts/audit_graphql_dump_age.py`. 10 of the 50 dumps feed `core`, split by whether a comparator the refresh actually re-scrapes exists. **Group A (measurable):** `game` lacks 4 in-span REST games and still calls 415 finished games `scheduled`; `gameLines` lacks 1,682 REST-lined games; `calendar` and `conference` are flush at 0. **Group B (6) has no fresh comparator and is unmeasured on divergence, not clean** — `linesProvider` at 52.5 days is the risk worth pricing, since a new book is how `dim_lines_provider` gains a row. **The scare was overstated: nothing reaches `core` today.** `stg.game` is read in exactly two places — the conference-FK repair takes only `homeConferenceId`/`awayConferenceId`, and `_build_fact_game_historical` reads `status` but only pre-2012, so the 415 stale statuses are unreachable (0 rows at `season>=2012`). The 4 missing games cost 4 unrepaired conference FKs out of 34,645, and `gameLines` cannot corrupt because `coalesce(rest, gql)` lets REST win. Also settled: CFBD did not delete game 401866625, it **re-issued** it as 401917058 after a postponement (2026-09-05 15:30 `scheduled` → 09-06 11:00 `completed` 28–19, same venue). **Still open: the cadence-and-wiring half.** Age is a proven-weak proxy — all four Group A dumps are 13.7d old and two have a zero gap — so cadence must come from how often an entity changes. Shape: put `gameLines` and `game` on the refresh path (or a weekly companion), price `linesProvider`, record the other seven as deliberately frozen. **Done when** `refresh_cfbd.py`'s output names the age of every GraphQL dump a `core` builder reads, and no dump a builder reads is older than its documented cadence. P1
- [ ] `#motherduck-drop-stale-stg-gql` **`md:cfb` still holds a pre-collapse `stg_gql`** <!-- id: motherduck-drop-stale-stg-gql --> — created 2026-09-10 by `#stg-gql-collapse`. `promote_to_motherduck.DEFAULT_SCHEMAS` dropped `stg_gql`, which means the next promote will neither overwrite it **nor remove it**. The mirror ends up with a fresh `stg` holding the collapsed tables *and* a stale `stg_gql` that still answers queries with pre-collapse data — silently, and forever. That is ADR-0002's original failure mode (two tables, one concept, no record of which is which) relocated to the mirror. Local `cfb.duckdb` is clean: the schema itself was dropped once empty. Shape: `DROP SCHEMA stg_gql CASCADE` on `md:cfb`, once, then promote. **The user runs this** — the mirror is manual and a CASCADE drop against shared state is not Claude's to issue. Done when `md:cfb` lists exactly `raw`, `stg`, `core`, `meta`. P1
- [x] ~~`#scraper-entry-cleanup` **Remove scraper entries for dropped sources**~~ <!-- id: scraper-entry-cleanup --> — done 2026-09-10: `Endpoint("draft_positions")` and `Endpoint("draft_teams")` removed from `cfb_system_maker/scrapers.py`, the two and only two sources `#warehouse-drop-superseded` retired under R6. `predicted_points` stays registered — it failed R6 (see `#predicted-points-grid-key`). A comment holds their place so they are not re-added without re-taking the verdict. `duckdb_load._SUPERSEDED_REST` **stays** regardless: `raw` is never deleted, so the dumps the scraper already wrote would otherwise still be exploded into `stg`. `tests/test_scrapers.py` base-endpoint count 78 → 76, now with an explicit assertion that neither name is back.

### PFF ingest — `docs/pff-ingest-plan.md` owns this

- [x] ~~`#pff-s5-commit` **Ship the PFF S5 loader wiring**~~ <!-- id: pff-s5-commit --> — done 2026-09-10: three commits — `94f7864` (FBS-only franchise coverage test), `6bbdd58` (S5 wiring: `cfb_system_maker/pff_schema.py`, loader jobs, `refresh_cfbd.py` reflatten, `scripts/check_pff_pin.py`, S5 worklog entry), `d80bc0d` (`jersey_number` carry-forward, 32.2% populated). 846 passed from a clean worktree at `d80bc0d`; `python scripts/check_pff_pin.py` exits 0 (21/21 tables, types ok, join ok).
- [x] ~~`#pff-s7-trim-pull` **Finish trimming the PFF pull plan against 2025**~~ <!-- id: pff-s7-trim-pull --> — done 2026-09-10: all three cuts applied, then corrected the same day in `scripts/pull_pff_modeling.py` (all 19 team reports dropped — 12 leaderboard re-cuts, `run-blocking` which `pass-blocking` dominates on 19,206 cells, and the last 6 by gate-3 decision, `team-rushing-direction` pulled once, `facet-passing-detail` pinned out); `audit_pff_pull.py` moved in lockstep. Planner delta measured at exactly −2,720 reads (3,997 → 1,277; 79 → 49 min a season, backfill 14.5 h → 9.0 h); `audit_pff_pull.py --season 2025` byte-identical to baseline, exit 0. Cost restated and the tier decision (all 19 dropped; `TEAM_REPORTS` empty not deleted, so restoring one is a one-line change) recorded in `docs/pff-ingest-plan.md`. Pinned by `tests/test_pff_audit.py`; 849 passed.
- [ ] `#pff-s6-backfill` **Backfill PFF 2014–2024 and finish 2026** <!-- id: pff-s6-backfill --> — S6. **All three gates are met as of 2026-09-10** — S3+S5 landed, S7 landed, and gate 3 (which team reports a backfill needs) is answered: **none**. The whole per-team report tier is dropped because nothing consumes it — no loader reads a `team_report_*` file and every target table in `docs/pff-warehouse-schema.md` comes from a leaderboard export. **Cost is now 9.0 h of metered calls, down from the 14.5 h originally recorded** (49 min a season × 11; 3,997 → 1,277 reads). Reversible: `TEAM_REPORTS` is an empty tuple, so restoring e.g. `pass-rush` for its `lhs_*`/`rhs_*` splits is one line plus 136 reads a season. **Held by decision 2026-09-10** — user said hold off; do not start without an explicit go. Also still blocked on `#rotate-secrets-after-compromise`: `PFF_API` was **not** part of the 2026-09-10 rotation, so nine hours of metered calls would run on a credential assumed disclosed. Done when `python scripts/audit_pff_pull.py` reports every backfilled season clean. P2 @needs-data
- [x] ~~`#pff-s8-player-tier` **Decide the PFF player tier**~~ <!-- id: pff-s8-player-tier --> — done 2026-09-10: deleted the 22-file stub (`data/raw/pff/player/`, one id, 102 K). Nothing read it — the flattener registry is leaderboard exports only and `audit_pff_pull.py` counts the tier without requiring it; the 2025 audit is unchanged apart from the tier leaving the census (4,657 → 4,637 files, 0 defects, exit 0). Re-pullable in ~15 s via `python scripts/pull_pff_modeling.py --seasons 2025 --player-ids 198077`. Recorded in `docs/pff-ingest-plan.md` S8.

### Deferred by decision — not blocking the chain

- [ ] `#core-dim-team` **Collapse the four vendor team-name maps onto one alias surface** <!-- id: core-dim-team --> — **restated 2026-09-10 against measurement** ([`docs/team-name-mapping-2026-09-10.md`](docs/team-name-mapping-2026-09-10.md), `python scripts/audit_team_name_maps.py --list`). The original premise does not hold: `core.dim_team` already exists (`cfb_system_maker/duckdb_core.py:135`), and all four consumers resolve **zero real gaps** — massey 137/137, pff 266/267, recruiting 262/268, coaches 137/137, where every residual is a school CFBD carries no team for (Chicago State, Albany, UTRGV...). `core.coach_season_unmatched` does not exist yet and is an artifact of `#core-merge-bucket-c`; the 118 multi-coach school-seasons are a grain problem, not a name one. So this is a **refactor for maintenance cost, not a data fix**: four rule sets each at 100% still cost four maintenances and a fifth vendor starts from scratch. Scope if picked up: one alias surface (CFBD `alternateNames` is collision-prone — see S4 — so it needs a rule, not a blind index) that `massey_teams`, `pff_franchise` and `OA_ALIASES` all resolve through. Done when a new vendor map is written against it without new normalize code. P3
- [x] ~~`#oddsapi-warehouse-wiring` **Wire the-odds-api snapshots into the warehouse**~~ <!-- id: oddsapi-warehouse-wiring --> — done 2026-09-10: `scripts/oddsapi_flatten.py` + `cfb_system_maker/oddsapi_schema.py` (pinned types, one name rule) → `stg.oa_odds_tick` (18,776 rows) and `stg.oa_snapshot`, resolved onto `game_id` in `core.fact_game_odds` — 98 events → 98 games, **zero NULL `game_id`**, no row multiplication. Resolution sits in `core` because `refresh_cfbd.py` flattens before it rebuilds; `_flatten_oddsapi()` added there. Design and evidence: [`docs/oddsapi-game-join-2026-09-10.md`](docs/oddsapi-game-join-2026-09-10.md) (pair first, kickoff only to split) and [`docs/oddsapi-team-name-join-2026-09-10.md`](docs/oddsapi-team-name-join-2026-09-10.md). `tests/test_oddsapi_flatten.py` pins the spreads sign convention; 873 passed.
- [x] ~~`#stg-camelcase-children` **Snake-case the 12 camelCase explode-child tables**~~ <!-- id: stg-camelcase-children --> — done 2026-09-10; **13, not 12** (`game_team__lineScores` joined the list when `#stg-gql-collapse` moved it into `stg`). One line: `_explode_nested_columns` builds `dest` as `f"{table}__{_snake(col)}"`. **Columns deliberately untouched** — `stg` preserves vendor field names (`gameId`, `homeTeam`, `alternateNames`) while a table name is the loader's to invent, so `stg.teams__alternate_names` has an `alternateNames` column and that is correct; snake-casing columns would mean renaming every camelCase field in the warehouse. **No migration script needed** — `explode_stg_lists` already drops every `__` child and rebuilds from the parents, so the change is self-migrating. Ran it on the live warehouse: 39 children before and after, zero real errors, `verify_warehouse_plan --camel` now empty. 4 files carried references; every remaining camelCase hit in the repo is a column or JSON key, which is the intended split. `tests/test_explode_stg_lists.py` pins both halves.

---

## 3. Totals model (`models/totals/`) `#sec-totals` <!-- section: sec-totals -->

*Main command: `python -m models.totals backtest --line ou_open --permute`.
Cite opening totals from `ou_open`, 2022-2025 prior-season folds, `|edge| >= 0`.
Never cite leaked-era 57% / +8.82% ROI.*

### Closed (changelog — folded in from `cfb-totals-model/TODO.md` on 2026-08-28; items were already done in the source repo, migrated here 2026-09-10)

- [x] ~~Drop leaked features (havoc, attendance, contaminated winProb)~~ `#totals-drop-leaked-features` <!-- id: totals-drop-leaked-features --> — done 2026-08-28 (per source repo; no commit hash carried over)
- [x] ~~Re-grade walk-forward on the clean feature set~~ `#totals-regrade-walkforward-clean` <!-- id: totals-regrade-walkforward-clean --> — done 2026-08-28
- [x] ~~Proper score vs the line, paired, with clustered CI + MDE~~ `#totals-proper-score-paired-mde` <!-- id: totals-proper-score-paired-mde --> — done 2026-08-28
- [x] ~~Paired open-vs-close test and CLV on this model's bets~~ `#totals-open-close-clv` <!-- id: totals-open-close-clv --> — done 2026-08-28
- [x] ~~Report 2021 week-expanding separately from 2022+ prior-season folds~~ `#totals-report-2021-separate` <!-- id: totals-report-2021-separate --> — done 2026-08-28
- [x] ~~Freeze one edge threshold for the ship/no-ship number~~ `#totals-freeze-edge-threshold` <!-- id: totals-freeze-edge-threshold --> — done 2026-08-28

**Later / out of scope** (carried over verbatim, not a queue): Bovada
takeability, prices other than −110, GBM tuning, a real P(under) model,
spread model, early-week strategy, swapping in `pregame_home_win_prob`.

---

## 4. Over-zero — Arscott, saturation, floor-bias (`models/over_zero/`) `#sec-over-zero` <!-- section: sec-over-zero -->

*(empty — no dedicated pytest suite for this unit; verify changed scripts
end-to-end against local data. See `models/over_zero/CLAUDE.md`.)*

---

## 5. Spread research — closing-line prediction (`research/spread/`) `#sec-spread` <!-- section: sec-spread -->

*Preregistration order is binding — `prereg-line-movement.md` and its
amendments. See `research/spread/CLAUDE.md` and `docs/plan-2026-09-08-master.md`.*

*(empty — nothing captured yet. `docs/superpowers/plans/2026-09-08-spread-next-steps.md`
is a recent plan doc in this area; consider `/todo-from-doc` against it.)*

---

## 6. Ops / infra `#sec-ops` <!-- section: sec-ops -->

*(`.planning/` (GSD) is cross-referenced only, not merged here — see
"What stays separate" in `docs/methodology/todo-system.md`.)*

- [ ] `#rotate-secrets-after-compromise` **Rotate the five credentials still on the old keys** <!-- id: rotate-secrets-after-compromise --> — confirmed admin-level malware ran ~6 h on this machine on 2026-09-09, so every secret readable from disk or environment is assumed disclosed. **CFBD rotated 2026-09-10** (verified: `find_cfbd_token()` resolves and the GraphQL re-scrape ran on it). **Still on the old keys: MotherDuck (`md:cfb`), Backblaze B2, pCloud, `PFF_API`, `ODDS_API`.** Rotate at each provider, update `env.env` and any scheduled-task environment, then re-run one pull per source to confirm. `PFF_API` gates `#pff-s6-backfill`; `ODDS_API` feeds the 6-hourly snapshots that now load to `stg.oa_odds_tick`. Until each lands, treat an auth failure on that source as suspected rotation, not a code bug. User-run: needs provider logins. P1 @needs-user

---

## 7. Housekeeping `#sec-housekeeping` <!-- section: sec-housekeeping -->

*(empty.)*

---

## Recently completed ✓ (context) `#sec-recent` <!-- section: sec-recent -->

*(nothing graduated here yet since the port — the totals-model changelog
above stays in its home section per the graduation rule; this section is
for closed items that don't fit any unit section.)*
