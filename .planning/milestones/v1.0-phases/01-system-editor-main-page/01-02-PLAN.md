---
phase: 01-system-editor-main-page
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - tests/test_web.py
autonomous: true
requirements: [EDIT-01, EDIT-04]

must_haves:
  truths:
    - "_cumulative_chart(result) returns {'points': [], 'polyline': '', 'zero_y': 75, 'min_x': None, 'max_x': None} when result.bet_details is empty — byte-identical shape to _range_chart's existing empty-input convention, so the SVG renders only the zero-line with no crash."
    - "_cumulative_chart orders result.bet_details by (season, week, game_id) ascending before computing the running sum, regardless of the input list's original order — game_id is a unique int, so this ordering is fully deterministic even when two bets share the same (season, week)."
    - "Each point in _cumulative_chart's output represents ONE bet (never merged) — two bets sharing the same (season, week) still produce two separate points, tie-broken by game_id ascending."
    - "_cumulative_chart's per-point 'profit' value is the RAW stake-unit running cumulative sum (never pre-multiplied by 100) — the x100 dollar conversion happens only inside templates/index.html's Jinja expressions, never inside web.py."
    - "GET / with no ?tab= query param defaults to tab='graph' (Results Graph); GET /?tab=matches renders the Past Matches table instead; any other or malformed ?tab= value (e.g. ?tab=foo) is normalized to tab='graph' before rendering, so template branching cannot accidentally treat it as Past Matches."
    - "Tab links (and every future query_href(...) call site) preserve every other active query-string parameter (load_system, filters, ff_enable, etc.) — built via werkzeug.datastructures.MultiDict + urllib.parse.urlencode, never by hand-concatenating '?key=value' strings."
    - "The pre-existing '.range-chart' Money Won (by-line) section in templates/index.html is preserved unchanged inside the Results Graph branch — it remains available for Phase 2/4's filter-distribution reuse, but does not leak into the Past Matches view."
    - "The Past Matches tab reuses the existing bets table and pill rendering (templates/index.html's pre-existing table-wrap section) completely unchanged — only its reachability changes, gated behind ?tab=matches."
    - "Each rendered cumulative-chart point exposes its $100-flat-stake value in an SVG <title> (for example raw profit 0.9091 renders as '+$90.91'); raw stake units remain unchanged in Python."
    - statement: "Tab query-param values are normalized with the exact expression `tab = 'matches' if request.args.get('tab') == 'matches' else 'graph'`; no numeric parsing or type coercion is applied."
      verification: backstop
  artifacts:
    - cfb_system_maker/web.py (_cumulative_chart, _query_href)
    - cfb_system_maker/templates/index.html (tab nav, Results Graph / Past Matches sections)
    - cfb_system_maker/static/styles.css (.tabs, .cumulative-chart)
  key_links:
    - "index() route normalizes tab to 'matches' or 'graph' and computes cumulative_chart = _cumulative_chart(result), passing both into render_template alongside the existing chart=_range_chart(result) kwarg -> templates/index.html's {% if tab == 'graph' %} / {% else %} block selects one complete view."
    - "app.jinja_env.globals['query_href'] = _query_href registered once in create_app() -> templates/index.html calls query_href(tab='graph')/query_href(tab='matches') to build the two tab hrefs without ever touching request.args directly in the template."
---

<objective>
Add the cumulative "Money Won Over Time" line chart (EDIT-01) and split the workspace into Results Graph / Past Matches tabs via a `?tab=` query-param full-page reload (EDIT-04, per CONTEXT.md's locked D-01 decision). Introduces a single shared, request-scoped query-string-preserving href helper (`_query_href`) that this plan's tab links use and that Plan 04 (`01-04-PLAN.md`) extends with a removing variant for filter-sentence remove-links.

Purpose: EDIT-01 and EDIT-04 are tightly coupled — the new cumulative chart lives inside the Results Graph tab, and the existing bets table moves inside the Past Matches tab — so they ship as one vertical slice.
Output: `_cumulative_chart()` and `_query_href()` in `web.py`; tab nav + Results Graph/Past Matches sections in `templates/index.html`; `.tabs`/`.cumulative-chart` CSS.

Review incorporation (`01-REVIEWS.md`, Codex): finding #4 (high) — the `?tab=<invalid>` fallback — and finding #5 (high) — the cumulative chart never surfacing dollar values — were already fixed in the prior revision pass (see Task 2's explicit `tab = "matches" if ... else "graph"` normalization and the per-point `<title>` with `point.profit * 100`). This pass additionally strengthens Task 2's acceptance criteria to actually follow a generated tab href with a second request rather than only inspecting the href string (01-REVIEWS.md's optional Plan 02 test-depth suggestion, adopted).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-editor-main-page/01-CONTEXT.md
@.planning/phases/01-system-editor-main-page/01-RESEARCH.md
@.planning/phases/01-system-editor-main-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _cumulative_chart() aggregation function to web.py</name>
  <files>cfb_system_maker/web.py, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/web.py (_range_chart, lines 447-483 — the exact dict-key shape and width/height/pad_x/pad_y/scale math to mirror; imports block, lines 1-18)
    cfb_system_maker/models.py (BacktestResult.bet_details, BetDetail fields including the new `margin`/`profit`/`season`/`week`/`game_id` from Plan 01)
    .planning/phases/01-system-editor-main-page/01-CONTEXT.md (Claude's Discretion: "Money-graph x-axis" — chronological bet order sorted by (season, week), one point per graded bet, since GameRecord has no per-game date field)
    .planning/phases/01-system-editor-main-page/01-RESEARCH.md (Pattern 1: Cumulative Chart, and Pitfall 3: dollar-conversion boundary — x100 only at render time, never inside backtest.py or this helper)
  </read_first>
  <behavior>
    - Test 1 (empty): _cumulative_chart(result) with result.bet_details == [] returns exactly {"points": [], "polyline": "", "zero_y": 75, "min_x": None, "max_x": None}.
    - Test 2 (single bet): one BetDetail with profit=0.9091, season=2023, week=1, game_id=1 -> returns one point with keys {"x", "y", "order", "profit"}, point["order"] == 0, point["profit"] == 0.9091 (NOT 90.91), min_x == 0, max_x == 0.
    - Test 3 (chronological ordering + running sum): three BetDetails passed to run_backtest in SHUFFLED (season, week, game_id) order with profits [0.9091, -1.0, 0.9091] in their TRUE chronological order — after sorting by (season, week, game_id), the returned points' "profit" values are the running sums [0.9091, -0.0909, 0.8182] (each round(..., 4)), proving both the sort and the running-sum math, independent of input order.
    - Test 4 (adjacency — equal (season, week), different game_id): two bets sharing the same (season, week) but different game_id both produce separate points (points list length == 2), ordered by game_id ascending — no merging occurs.
  </behavior>
  <action>
    Add a new function `_cumulative_chart(result: BacktestResult) -> dict[str, object]` in cfb_system_maker/web.py, placed immediately after the existing `_range_chart` function (after line 483). Empty-input branch returns the exact literal dict `{"points": [], "polyline": "", "zero_y": 75, "min_x": None, "max_x": None}` matching `_range_chart`'s empty shape exactly.
    Non-empty branch: `ordered = sorted(result.bet_details, key=lambda bet: (bet.season, bet.week, bet.game_id))`; maintain a running float `running = 0.0`, for each `index, bet` in `enumerate(ordered)`: `running = round(running + bet.profit, 4)`, append to a `points` list. Reuse `_range_chart`'s exact width=520, height=150, pad_x=28, pad_y=18 constants and x/y scaling formulas, substituting `_range_chart`'s per-bucket `line`/`profit` axis values with `order=index` (0-based) on the x-axis and the running `profit` sum on the y-axis (min/max span computed the same way `_range_chart` computes `min_profit`/`max_profit`/`span` over its values, but here over the running-sum series instead of per-line buckets). Each point dict has exactly the keys `{"x": float, "y": float, "order": int, "profit": float}` — `profit` stays in raw stake-units, never multiplied by 100 here. `polyline` is the same space-joined `"x,y"` string format as `_range_chart`. `zero_y` uses the identical formula `_range_chart` uses (height - pad_y - ((0 - min_profit) / span) * (height - pad_y * 2)). `min_x`/`max_x` are `0`/`len(ordered) - 1` when non-empty.
    Do not add any `?tab=` handling or template wiring in this task — that is Task 2's scope. This task only adds the pure aggregation function and its tests.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _cumulative_chart" cfb_system_maker/web.py` returns exactly one match, positioned after `_range_chart`.
    - New tests in tests/test_web.py directly import and call `_cumulative_chart` (constructing `BacktestResult`/`BetDetail` directly, matching the existing test_backtest.py dataclass-construction convention) and assert on all 4 behaviors above.
    - `.venv/Scripts/python.exe -m pytest tests/test_web.py -q` passes with 0 failures.
  </acceptance_criteria>
  <done>_cumulative_chart exists, is pure (no Flask/request dependency), matches _range_chart's empty-shape convention, sorts chronologically by (season, week, game_id), and keeps profit values in stake-units.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ?tab= query-param split and Results Graph / Past Matches sections</name>
  <files>cfb_system_maker/web.py, cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/web.py (index() route lines 25-81, especially the render_template call at lines 63-81 and the existing chart=_range_chart(result) kwarg at line 78; imports block lines 1-18)
    cfb_system_maker/templates/index.html (the .filter-detail/.range-chart section lines 277-289, and the table-wrap bets table section lines 291-324 — the section this task wraps in a tab conditional)
    .planning/phases/01-system-editor-main-page/01-RESEARCH.md (Pattern 3: Query-String-Preserving Links — the exact _query_href/MultiDict/urlencode code shape verified against werkzeug 3.1.8; T-01-02 mitigation)
    .planning/phases/01-system-editor-main-page/01-UI-SPEC.md (Tab labels copy: "Results Graph" / "Past Matches"; Color: active-tab indicator uses --accent; Spacing: sm 8px tab-item gap, md 16px cumulative-chart container padding)
  </read_first>
  <action>
    Add imports to cfb_system_maker/web.py: `from urllib.parse import urlencode` and `from werkzeug.datastructures import MultiDict`.
    Add a new function `_query_href(**overrides: str) -> str` in web.py (near `_range_chart`/`_cumulative_chart`): build `copy = MultiDict(request.args.items(multi=True))`, for each `key, value` in `overrides.items()` call `copy.setlist(key, [value])`, return `"?" + urlencode(list(copy.items(multi=True)))`. This is a request-scoped helper (reads Flask's `request.args`), unlike the pure `_cumulative_chart`.
    Inside `create_app()`, immediately after `app = Flask(__name__)` / `app.config["DATA_DIR"] = ...`, register `app.jinja_env.globals["query_href"] = _query_href` so the template can call `query_href(tab='graph')` directly.
    In the `index()` route, add `tab = "matches" if request.args.get("tab") == "matches" else "graph"`. This normalization is required: passing a raw unexpected value into a two-branch Jinja `{% if tab == 'graph' %}{% else %}` would otherwise open Past Matches. Add `tab=tab` and `cumulative_chart=_cumulative_chart(result)` as two new kwargs to the existing `render_template("index.html", ...)` call, alongside the unchanged `chart=_range_chart(result)` kwarg.
    In templates/index.html, insert a new `<nav class="tabs">` immediately after the stat-chip header with two links: `<a href="{{ query_href(tab='graph') }}" {% if tab == 'graph' %}aria-current="page"{% endif %}>Results Graph</a>` and `<a href="{{ query_href(tab='matches') }}" {% if tab == 'matches' %}aria-current="page"{% endif %}>Past Matches</a>`. Plan 04 will insert the active-filter list immediately before this nav; Plan 05 will insert theory before that list, yielding the UI-SPEC order chips -> theory -> filters -> tabs -> tab content.
    Wrap complete view content in the tab conditional. `{% if tab == 'graph' %}` contains the existing `.metrics.stats-panel`, `.season-breakdown`, `.coverage`, and `.filter-detail` / `.range-chart` sections (their internal markup stays unchanged), followed by a NEW `.cumulative-chart` section. `{% else %}` contains only the EXISTING bets `<section class="table-wrap">` unchanged. This keeps the by-line chart for later reuse without showing any graph in Past Matches.
    The new cumulative SVG uses viewBox `0 0 520 150`, zero-line, conditional polyline, and one non-self-closing `<circle>` per point. Each circle contains `<title>Bet {{ point.order + 1 }}: {% if point.profit >= 0 %}+{% endif %}${{ "%.2f"|format(point.profit * 100) }}</title>` so EDIT-01's $100 convention is visible and accessible while Python retains raw stake units. Add bet-count axis labels below the SVG.
    In cfb_system_maker/static/styles.css, add new rule blocks `.tabs` (flex row, gap 8px, margin-bottom matching the existing section-spacing convention), `.tabs a` (padding 8px 12px, color var(--muted), no underline), `.tabs a[aria-current]` (color var(--text), border-bottom 2px solid var(--accent)), and `.cumulative-chart`/`.cumulative-chart h3`/`.cumulative-chart svg`/`.cumulative-chart polyline`/`.cumulative-chart circle`/`.cumulative-chart .zero-line` mirroring the existing `.range-chart` equivalents (background var(--panel), border 1px solid var(--border), border-radius 6px) but with 16px padding (the UI-SPEC md spacing token) instead of `.range-chart`'s existing 14px — do not change `.range-chart`'s own padding.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_web.py -q` passes, including a new integration test that GETs `/?load_system=away-dogs&tab=matches` (reusing the existing save/load pattern from test_web_save_and_load_system) and asserts the response HTML contains `tab=graph` and `load_system=away-dogs` together inside the same href attribute (proving query_href preserves unrelated params) and that "Money Won Over Time" is NOT present while "Past Matches" tab is active but the bets table IS present. The same test then extracts the `tab=graph` href's query string (simple substring/regex extraction, no HTML parser needed) and issues a SECOND GET request to it, asserting the resulting page still shows `load_system=away-dogs` in its own tab links and renders "Money Won Over Time" — proving the tab switch actually round-trips the loaded system end-to-end, not just that the href string looks correct (01-REVIEWS.md's Plan 02 test-depth suggestion, adopted).
    - A second new integration test GETs `/` with no `?tab=` param and asserts "Money Won Over Time" and the preserved by-line chart ARE present, while the Past Matches table is absent.
    - A third integration test GETs `/?tab=garbage` and proves the response renders Results Graph, marks its tab `aria-current="page"`, and does not render the Past Matches table.
    - A chart-rendering test uses a known winning bet and asserts its SVG title contains the $100-scaled cumulative value (for example `+$90.91`), proving conversion is rendered rather than merely documented.
    - `grep -n "def _query_href" cfb_system_maker/web.py` and `grep -n "def _cumulative_chart" cfb_system_maker/web.py` both return exactly one match each.
    - `grep -n "class=\"range-chart\"" cfb_system_maker/templates/index.html` still returns exactly one match (the pre-existing by-line chart section was not removed or renamed).
  </acceptance_criteria>
  <done>GET / defaults to the complete Results Graph view; GET /?tab=matches shows only the existing Past Matches bets table; malformed tab values normalize to graph; tab links preserve query state; cumulative values expose $100-scaled SVG titles; the pre-existing by-line chart remains intact inside Results Graph.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `_cumulative_chart(result: BacktestResult) -> dict[str, object]` — new function, `cfb_system_maker/web.py`.
- `_query_href(**overrides: str) -> str` — new function, `cfb_system_maker/web.py`.
- `app.jinja_env.globals["query_href"]` — new Jinja global registration inside `create_app()`, `cfb_system_maker/web.py`.
- `tab` and `cumulative_chart` — new `render_template("index.html", ...)` context kwargs in `index()`.
- Tab nav + Results Graph / Past Matches conditional sections — `cfb_system_maker/templates/index.html`.
- `.tabs`, `.tabs a`, `.tabs a[aria-current]` — new CSS classes, `cfb_system_maker/static/styles.css`.
- `.cumulative-chart`, `.cumulative-chart h3`, `.cumulative-chart svg`, `.cumulative-chart polyline`, `.cumulative-chart circle`, `.cumulative-chart .zero-line` — new CSS classes, `cfb_system_maker/static/styles.css`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Query string -> href construction | This plan introduces `_query_href`, the first of the two shared query-string-preserving helpers (T-01-02's mitigation surface). Malicious/crafted query params (e.g. `?tab=<script>`) must never be reflected unescaped into an href attribute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-02 | Tampering | `_query_href()` in web.py; tab nav links in templates/index.html | high | mitigate | `_query_href` builds every href exclusively via `urllib.parse.urlencode(list(copy.items(multi=True)))` over a `werkzeug.datastructures.MultiDict` copy of `request.args` — values are always percent-encoded by `urlencode`, never string-interpolated raw into the href attribute. No template in this plan concatenates `request.args` values into an `href` by hand. |
| T-01-01 | Tampering / Information Disclosure | Theory field (not touched by this plan) | high | accept (N/A here) | No theory rendering in this plan — owned by Plan 05 (01-05-PLAN.md). |

</threat_model>

<verification>
Full suite: `.venv/Scripts/python.exe -m pytest -q` must pass. No new dependencies (werkzeug/urllib are already transitive Flask dependencies already installed).
</verification>

<success_criteria>
- `_cumulative_chart` and `_query_href` exist, tested, and wired into the index() route.
- The workspace shows mutually exclusive Results Graph / Past Matches views; default and malformed tab values select Results Graph, and switching tabs preserves the full query string.
- `.venv/Scripts/python.exe -m pytest -q` is green.
</success_criteria>

<output>
Create `.planning/phases/01-system-editor-main-page/01-02-SUMMARY.md` when done
</output>
