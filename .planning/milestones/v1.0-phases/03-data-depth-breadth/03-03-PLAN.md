---
phase: 03-data-depth-breadth
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cfbd-python/
  - scripts/probe_line_floor.py
autonomous: false
requirements: [DATA-01]
user_setup:
  - service: cfbd
    why: "Live re-confirmation of the CFBD betting-line floor against the current API"
    env_vars:
      - name: CFBD_API_KEY
        source: "collegefootballdata.com key, already present in env.env (resolved by find_cfbd_token)"

must_haves:
  truths:
    - "The vendored cfbd-python/ clone (currently empty) is restored so `import cfbd` resolves via cfbd_client._load_cfbd_module (needed only for live network calls)."
    - "A bounded live probe over seasons 2008–2013 calls BettingApi.get_lines(provider=consensus) and reports the earliest season with usable rows (spread or overUnder present), re-confirming or updating the on-disk 2013 floor (D-01)."
    - "The API token is resolved via find_cfbd_token only and is NEVER printed, logged, or committed."
    - "IF the probe returns usable pre-2013 lines, those seasons are backfilled via the existing fetch → build → enrich path (never scrapers.py); otherwise the step is a documented no-op."
  artifacts:
    - cfbd-python/
    - scripts/probe_line_floor.py
  key_links:
    - "Backfill MUST reuse `fetch --season` (writes games_/lines_{season}.json that build consumes), NOT scrapers.py (writes files build ignores)."
    - "The restored clone is gitignored (cfbd-python/ in .gitignore) — restoring it does not change tracked files."
  prohibitions:
    - never-log-echo-or-commit-the-env-env-token
    - never-edit-the-vendored-cfbd-python-clone-contents-restore-only
    - never-backfill-game-or-line-data-via-scrapers-py
    - never-add-a-game-level-column-to-games-csv
---

<objective>
Deliver the DATA-01 live re-confirmation: restore the empty vendored `cfbd-python` clone, run a bounded live line-coverage probe against the current CFBD API, and (only if the probe surfaces usable pre-2013 lines) backfill those seasons through the existing fetch → build → enrich path (D-01). Expected outcome per research: the 2013 floor is re-confirmed and no backfill is needed.

## Phase Goal (user story)

**As a** bettor building a system, **I want to** know the tool's historical coverage is confirmed against the live CFBD API (not just a dated on-disk snapshot), **so that** I can trust the earliest filterable season and know no reachable earlier lines are being missed.

> Note: derived from Success Criterion 1 (reframed 2026-07-17).

Purpose: this is the ONLY plan needing network + the `cfbd-python` clone + the `env.env` token, so it is isolated and gated (`autonomous: false`). It depends on 03-01 so that any (rare) backfilled pre-2013 season is enriched with the full, updated registry.
Output: a restored clone, a committed reproducible probe script, and a confirmed/updated floor.

## Artifacts this phase produces (this plan)

- Restored `cfbd-python/` working tree (gitignored; local only).
- New file `scripts/probe_line_floor.py` (bounded, token-safe live probe).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-data-depth-breadth/03-RESEARCH.md
@cfb_system_maker/cfbd_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the vendored cfbd-python clone</name>
  <files>cfbd-python/</files>
  <read_first>
    - cfb_system_maker/cfbd_client.py (`_load_cfbd_module` :51-58 — path-injects `cfbd-python/`; `find_cfbd_token` :34-48)
    - .planning/phases/03-data-depth-breadth/03-RESEARCH.md (Environment note + Environment Availability — clone is empty, pydantic v1)
    - CLAUDE.md (cfbd-python is a vendored dependency, not our code — do not edit it)
  </read_first>
  <action>Clone the canonical upstream client into the empty `cfbd-python/` dir: `git clone https://github.com/CFBD/cfbd-python cfbd-python` (pydantic v1). It is gitignored, so this changes no tracked files. Confirm `import cfbd` resolves through the path injection. Do NOT edit any file inside `cfbd-python/` and do NOT pip-install it (it is loaded by path injection).</action>
  <verify>
    <automated>python -c "from cfb_system_maker.cfbd_client import _load_cfbd_module; m=_load_cfbd_module(); assert hasattr(m,'BettingApi'); print('cfbd import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from cfb_system_maker.cfbd_client import _load_cfbd_module; _load_cfbd_module()"` exits 0.
    - `cfbd-python/` contains the upstream package (non-empty; `cfbd/__init__.py` present).
    - No tracked file changed (clone dir is gitignored).
  </acceptance_criteria>
  <done>The vendored client is restored and importable; live BettingApi calls are now possible.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded, token-safe live line-coverage probe</name>
  <files>scripts/probe_line_floor.py</files>
  <read_first>
    - cfb_system_maker/cfbd_client.py (`fetch_games_and_lines` :9-31 — Configuration/ApiClient/BettingApi.get_lines usage; `find_cfbd_token` :34-48)
    - cfb_system_maker/normalize.py (`_select_line` "usable" definition: spread or overUnder present)
    - .planning/phases/03-data-depth-breadth/03-RESEARCH.md (DATA-01 "What DATA-01 actually delivers" — bounded sweep 2008–2013, stop at earliest usable-line season)
  </read_first>
  <action>Write `scripts/probe_line_floor.py`: for each season in 2008..2013, call `BettingApi.get_lines(year=season, provider="consensus")` (reuse the Configuration/ApiClient pattern from `fetch_games_and_lines`; token via `find_cfbd_token`), count rows where any line has a non-None `spread` or `overUnder`, and print `season -> usable_count`. Report the earliest season with usable_count > 0 as the confirmed floor. The token MUST NEVER be printed or written to any file — print only season numbers and counts. Wrap each season call so one HTTP/validation error prints a skip line and continues (does not abort the sweep). Keep it a small standalone script (no pipeline import needed beyond cfbd_client helpers).</action>
  <verify>
    <automated>python scripts/probe_line_floor.py</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/probe_line_floor.py` runs, prints per-season usable-line counts for 2008–2013, and names the earliest usable-line season.
    - The script sources the token only via `find_cfbd_token` and its output contains no token substring.
    - A per-season network/validation error prints a skip and does not abort the remaining seasons.
  </acceptance_criteria>
  <done>The live probe has produced current-API usable-line counts for 2008–2013, re-confirming (or updating) the floor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm floor result and decide conditional backfill</name>
  <files>scripts/probe_line_floor.py (its Task 2 stdout)</files>
  <read_first>
    - scripts/probe_line_floor.py output from Task 2
    - .planning/phases/03-data-depth-breadth/03-RESEARCH.md (Open Question 1 — SC-1 vs floor; expected result: 2013 confirmed, no backfill)
    - cfb_system_maker/cli.py (`fetch` :56-62, `build` :65-73, `enrich` :76-79 — the backfill path)
  </read_first>
  <action>Blocking human checkpoint (no autonomous work). Present the Task 2 probe output and the confirmed earliest usable-line season, then follow the how-to-verify branch: approve as-is if 2013 is the earliest usable-line season (no backfill), or, only if a pre-2013 season shows usable lines, run the fetch→build→enrich backfill for that season set. Confirm no token leaked in probe output before resuming.</action>
  <what-built>Task 1 restored the `cfbd-python` clone; Task 2 ran a bounded live BettingApi probe over seasons 2008–2013 and reported per-season usable-line counts and the earliest usable-line season.</what-built>
  <how-to-verify>
    1. Read the Task 2 probe output. Confirm the earliest usable-line season it reports.
    2. Expected (per research): 2013 is the earliest usable-line season → no pre-2013 backfill; the floor is re-confirmed. Approve as-is.
    3. Only if the probe reports usable lines for a season BEFORE 2013: run the backfill for that season set, then re-enrich, e.g. `python -m cfb_system_maker fetch --season <yr> --provider consensus --data-dir data` then `python -m cfb_system_maker build --season <full 2013..latest plus new years> --data-dir data` then `python -m cfb_system_maker enrich --data-dir data`, and confirm the new season(s) now contribute non-zero rows. (Use `fetch`, never `scrapers.py`.)
    4. Confirm the probe output contains no API token / secret.
  </how-to-verify>
  <verify>
    <human-check>Human records the confirmed earliest usable-line season and the backfill decision (expected: no-op); confirms no token in output.</human-check>
  </verify>
  <acceptance_criteria>
    - Human has recorded the confirmed earliest usable-line season.
    - Either: no pre-2013 lines found (no-op, documented), OR pre-2013 seasons backfilled via fetch→build→enrich and now non-empty.
    - No token appeared in any probe/backfill output.
  </acceptance_criteria>
  <done>The live floor result is human-confirmed and the conditional-backfill decision is recorded (expected: 2013 re-confirmed, no-op).</done>
  <resume-signal>Type "approved" (2013 floor re-confirmed, no backfill) or describe the pre-2013 season(s) to backfill.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CFBD BettingApi (network) → probe script | Untrusted network responses parsed to count usable lines. |
| `env.env` token → probe script | Secret resolved via `find_cfbd_token`; must not leak into output/logs/commits. |
| `git clone` upstream → `cfbd-python/` | Third-party code pulled onto disk (path-injected, not pip-installed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | probe script output / logs | high | mitigate | Token resolved via `find_cfbd_token` only; script prints only season numbers + usable-line counts; token never printed, logged, or written to a file; `env.env` stays gitignored. |
| T-03-03-02 | Tampering / DoS | malformed BettingApi JSON | medium | mitigate | Usable-line count is None-safe (guards `spread`/`overUnder`); a per-season error prints a skip and does not abort the sweep. |
| T-03-03-SC | Tampering (supply chain) | `git clone cfbd-python` | medium | mitigate | Clone the canonical `github.com/CFBD/cfbd-python` (the project's already-vendored client, pydantic v1); no pip/npm/cargo install occurs (path injection only), so the package-legitimacy gate is N/A; do not edit clone contents. |
</threat_model>

<verification>
- `python -c "from cfb_system_maker.cfbd_client import _load_cfbd_module; _load_cfbd_module()"` succeeds (clone restored).
- Probe script runs and reports the earliest usable-line season with no token in output.
- Human checkpoint recorded the confirmed floor and the backfill decision (expected: no-op).
</verification>

<success_criteria>
- The CFBD betting-line floor is re-confirmed against the live API (D-01); pre-2013 seasons backfilled only if usable lines exist, else documented no-op.
- No token was disclosed at any step.
</success_criteria>

<output>
Create `.planning/phases/03-data-depth-breadth/03-03-SUMMARY.md` when done.
</output>
