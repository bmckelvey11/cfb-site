---
phase: 03-data-depth-breadth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_normalize.py
  - docs/data-line-floor.md
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "A game whose betting `lines` list is empty yields 0 GameRecords — the D-02 'require a usable line' floor gate (locks the clean pre-2013 behavior)."
    - "A game with a usable consensus line yields exactly 1 GameRecord (floor season non-empty)."
    - "The confirmed CFBD betting-line floor (2013) is documented with on-disk evidence, closing 'backfill earlier than 2013' as verification-gated rather than an unbounded search (D-01)."
    - "2012 (games present, 0 usable lines) contributes 0 rows; 2013 contributes non-empty rows."
  artifacts:
    - tests/test_normalize.py
    - docs/data-line-floor.md
  key_links:
    - "The 0-rows behavior is produced by the UNCHANGED normalize._select_line — the test asserts existing behavior; do not modify normalize.py."
  prohibitions:
    - never-add-a-game-level-column-to-games-csv
    - never-edit-the-vendored-cfbd-python-clone
    - never-backfill-game-or-line-data-via-scrapers-py
---

<objective>
Deliver the DATA-01 floor guard: lock the D-02 "require a usable line" behavior with a test (pre-floor seasons cleanly contribute 0 rows) and document the confirmed 2013 CFBD betting-line floor with evidence, so the "backfill earlier" requirement is closed by verification rather than an unbounded search (D-01, D-02).

## Phase Goal (user story)

**As a** bettor building a system, **I want to** trust that the tool handles the earliest line-covered season correctly and cleanly ignores pre-floor seasons that have games but no lines, **so that** backtests over the full available history stay clean and I know exactly how far back coverage goes.

> Note: derived from Success Criterion 1 (reframed 2026-07-17). The original SC-1 ("season earlier than 2013 → non-empty results") was unsatisfiable via CFBD (the only wired source) because 2013 is the betting-line floor.

Purpose: DATA-01 is thin and verification-shaped — 2013 already equals the CFBD line-coverage floor and the fetch/build path is fully parameterized. This plan needs NO network and NO `cfbd-python` clone (the live re-confirmation probe is 03-03).
Output: a normalize floor-behavior test + a durable floor-evidence doc.

## Artifacts this phase produces (this plan)

- New test functions in `tests/test_normalize.py` (empty-lines → 0 rows; usable-line → 1 row).
- New file `docs/data-line-floor.md` (confirmed floor + evidence + closure rationale).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-depth-breadth/03-RESEARCH.md
@cfb_system_maker/normalize.py
@tests/test_normalize.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the line-floor gate — empty lines yields 0 rows</name>
  <files>tests/test_normalize.py</files>
  <read_first>
    - cfb_system_maker/normalize.py (`normalize_games` :8-43; `_select_line` "require a usable line" :46-56)
    - tests/test_normalize.py (existing join tests :4-63)
    - .planning/phases/03-data-depth-breadth/03-RESEARCH.md (DATA-01 findings — 2012 = 805 games / 0 usable lines; 2013 = 806 usable)
    - CLAUDE.md (spread-sign convention; "require a usable line" is D-02)
  </read_first>
  <behavior>
    - A game joined to a betting row whose `lines` is `[]` (or all lines lack both spread and overUnder) produces 0 GameRecords.
    - A game joined to a betting row with a usable consensus line produces exactly 1 GameRecord.
  </behavior>
  <action>Add `test_line_less_game_contributes_zero_records`: build one game dict plus a betting row with `lines: []`, assert `normalize_games(games, lines, provider="consensus")` returns an empty list. Add `test_usable_line_contributes_one_record` (or reuse the existing join test as the positive case if it already covers this — do not duplicate) asserting a game with one usable consensus line returns a single record. These assert the UNCHANGED `_select_line` behavior — do NOT modify `cfb_system_maker/normalize.py`. Mirror the construct-dict style of the existing `test_normalize_joins_games_to_consensus_lines`.</action>
  <verify>
    <automated>python -m pytest tests/test_normalize.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_normalize.py` exits 0.
    - `tests/test_normalize.py` contains `test_line_less_game_contributes_zero_records` asserting an empty result list for a `lines: []` game.
    - `cfb_system_maker/normalize.py` is unchanged (git diff shows no modification to that file).
  </acceptance_criteria>
  <done>The D-02 floor gate (pre-floor / line-less seasons contribute 0 rows) is locked by an automated test.</done>
</task>

<task type="auto">
  <name>Task 2: Document the confirmed 2013 line floor with evidence</name>
  <files>docs/data-line-floor.md</files>
  <read_first>
    - .planning/phases/03-data-depth-breadth/03-RESEARCH.md (DATA-01 findings table + "What DATA-01 actually delivers")
    - .planning/phases/03-data-depth-breadth/03-CONTEXT.md (D-01 RESOLVED amendment; D-02)
    - .planning/ROADMAP.md (Phase 3 reframed Success Criterion 1)
  </read_first>
  <action>Write `docs/data-line-floor.md` recording: (1) the confirmed CFBD betting-line floor is 2013; (2) evidence — 2012 has 805 games / 0 usable lines across all providers, 2013 has ~806 usable consensus rows, built `games.csv` already spans 2013–2025 (12,964 games), verified on-disk 2026-06-13; (3) the binding constraint is line coverage, not game coverage (games exist back to 1992); (4) the fetch/build path is fully parameterized by `--season` with no hardcoded floor, so "backfill earlier" is a data-availability question, not a code change; (5) closure: the requirement is met by verification — a live re-confirmation probe (03-03) re-checks the current API, and pre-2013 seasons are backfilled ONLY if that probe returns usable lines; (6) the D-02 rule that a game needs a usable line to contribute a row, which is why pre-floor seasons cleanly contribute 0 rows. Do NOT include any API token or `env.env` content. Keep it concise (a reference doc, not a narrative).</action>
  <verify>
    <automated>test -f docs/data-line-floor.md && grep -q "2013" docs/data-line-floor.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/data-line-floor.md` exists and states 2013 as the confirmed floor with the 2012-zero-usable-lines evidence.
    - The doc references the live re-confirmation probe (03-03) as the closure mechanism.
    - The doc contains no token / no `env.env` secret material.
  </acceptance_criteria>
  <done>The confirmed floor and its evidence are durable, closing "backfill earlier than 2013" as verification-gated (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk betting `lines` JSON → normalize | Already-parsed local data; `_select_line` fails closed on line-less rows. No network, no token in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Tampering | `normalize._select_line` on empty/malformed `lines` | low | accept | Existing `_select_line` already returns None for line-less rows → 0 records; this plan only adds a test asserting that behavior. |
| T-03-02-02 | Information Disclosure | `docs/data-line-floor.md` | low | mitigate | Documentation must record floor evidence only — no API token, no `env.env` value (explicit action constraint). |
</threat_model>

<verification>
- `python -m pytest tests/test_normalize.py` green.
- `docs/data-line-floor.md` present and evidence-complete.
</verification>

<success_criteria>
- Pre-floor (line-less) seasons provably contribute 0 rows; floor season non-empty (SC-1, D-02).
- The 2013 floor is documented with evidence, closing the "backfill earlier" question (D-01).
</success_criteria>

<output>
Create `.planning/phases/03-data-depth-breadth/03-02-SUMMARY.md` when done.
</output>
