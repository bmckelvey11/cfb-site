---
phase: 02-integrity-fade-grade
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - cfb_system_maker/models.py
  - cfb_system_maker/backtest.py
  - cfb_system_maker/templates/index.html
  - tests/test_backtest.py
  - tests/test_web.py
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "BacktestResult.grade: str | None is a new trailing field (after average_margin) defaulting to None, so every existing direct BacktestResult(...) construction site (e.g. tests/test_web.py's _result_with_bets helper) keeps working unmodified."
    - "compute_grade(result, system) combines 5 sub-scores -- sample size (decided bet count), ROI significance (stats.z_score), season sign-consistency (sign_consistency(result.season_breakdown)), permutation p-value (stats.permutation_p_value), and overfitting penalty (count_overfit_filters(system)) -- via equal-weighted average, then buckets into one of A/B/C/D/F using the _GRADE_BANDS threshold table (D-04, D-05)."
    - "count_overfit_filters follows D-06's counting rule exactly: favorite/underdog/home/away/min_spread/max_spread/min_total/max_total/providers each count as at most 1 when populated; teams/conferences/seasons/weeks and each op='in' FeatureFilter count every individual selected value; system.fade itself is never counted (D-06)."
    - "run_backtest constructs BacktestResult once, then returns dataclasses.replace(result, grade=compute_grade(result, system)) -- grade is never computed by re-running matching or re-deriving stats/season_breakdown from scratch."
    - "The Grade chip renders result.grade (or the em-dash fallback) through normal Jinja auto-escaping, with no \\|safe filter anywhere near it (D-07)."
    - "When a system matches zero bets (fade toggled or not), BacktestResult.bets == 0 and the Grade chip renders the em-dash; no crash, no signal change from the empty-match path."
    - "Each of the 5 grade sub-score helpers returns the documented value exactly at and one integer/float step beyond each threshold boundary stated in 02-RESEARCH.md (verified by tests/test_backtest.py -k grade boundary tests)."
    - "compute_grade's equal-weighted average and explicit per-threshold boundaries (fully specified in 02-RESEARCH.md, no undefined rounding gap) are verified to match a reference calculation via tests/test_backtest.py -k compute_grade."
    - "compute_grade() returns exactly one of A/B/C/D/F/None (-> em-dash) -- a single character by construction, so the fixed-width .metrics article container (used identically for Record/Margin/Money Won/ROI) can never overflow."
    - "The same fixed single-character return set makes long-text structurally impossible, not just unlikely, for the Grade chip value."
  artifacts:
    - cfb_system_maker/models.py (BacktestResult.grade)
    - cfb_system_maker/backtest.py (_GRADE_BANDS, _sample_size_score, _roi_significance_score, _consistency_score, _permutation_score, _overfit_score, count_overfit_filters, compute_grade)
    - cfb_system_maker/templates/index.html (Grade chip conditional render)
  key_links:
    - "run_backtest (backtest.py) is the sole call site of compute_grade -- it already has both result (just-constructed) and system (its own parameter) in scope, so grade wiring needs no new plumbing through web.py; templates/index.html's Grade chip reads result.grade exactly like it already reads result.wins/result.profit/result.roi."
    - "compute_grade consumes result.stats.z_score/permutation_p_value and sign_consistency(result.season_breakdown) -- both already computed by compute_system_stats/compute_season_breakdown inside run_backtest -- never recomputing statistics independently (Don't Hand-Roll)."
  assumptions:
    - "Equal-weighted average (not 'worst-of') is the composite combination rule, and A-F letter bands (not a numeric score) are the output format -- both are D-05 discretionary choices, documented explicitly in 02-RESEARCH.md's Standard Stack > Alternatives Considered and Pattern 2, not researched facts. If a worst-of rule is preferred later, it is a one-line change (sum(scores)/len(scores) -> min(scores)) inside compute_grade."
  prohibitions:
    - statement: "Do not count system.fade as an active/overfit filter inside count_overfit_filters -- fade is a grading transform (which side is scored), not a matching filter (which games are included), and D-06's counting rule only covers fields that narrow the matched population."
      status: unverified
      verification: backstop
    - statement: "Do not render compute_grade()'s output (or the em-dash fallback) through the Jinja \\|safe filter in templates/index.html -- render result.grade through normal auto-escaping like every other stat chip, per the project's no-\\|safe convention."
      status: unverified
      verification: backstop
    - statement: "Do not return the letter F from compute_grade when result.bets == 0 -- zero matched bets must return None (rendered as the em-dash), distinct from a graded-and-weak system that legitimately earns F."
      status: unverified
      verification: backstop
---

<objective>
Implement the composite System Grade (INTG-02, D-04/D-05/D-06/D-07): a new `compute_grade(result, system) -> str | None` pure function combining 5 sub-scores -- sample size, ROI significance (z-score), season sign-consistency, permutation p-value, and a D-06 overfitting penalty -- via equal-weighted average into a letter A-F, or `None` (em-dash) for zero matched bets. Wired into `BacktestResult.grade` and rendered into the Grade chip slot Phase 1 reserved as a placeholder (D-07: this phase renders the value only, no breakdown panel).

Purpose: INTG-02 depends on `02-01-PLAN.md`'s `SystemFilter.fade` field only in the narrow sense that `count_overfit_filters` must know not to count it (D-06) -- the file-conflict-driven wave ordering (models.py, backtest.py, templates/index.html, tests/test_backtest.py, tests/test_web.py all shared with 02-01) is why this plan is Wave 2, not a content dependency.
Output: `BacktestResult.grade`; `compute_grade` + its 5 sub-score helpers + `count_overfit_filters` in `backtest.py`; the Grade chip rendering `result.grade` in `templates/index.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-integrity-fade-grade/02-CONTEXT.md
@.planning/phases/02-integrity-fade-grade/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compute_grade + 5 sub-score helpers + count_overfit_filters + BacktestResult.grade wiring</name>
  <files>cfb_system_maker/models.py, cfb_system_maker/backtest.py, tests/test_backtest.py</files>
  <read_first>
    cfb_system_maker/backtest.py (run_backtest lines 12-51 -- where to change the final `return BacktestResult(...)` into a bind-then-replace; compute_system_stats lines 162-195 and sign_consistency lines 94-96 -- the two functions compute_grade consumes; SystemFilter's full field list at the top of models.py for count_overfit_filters)
    cfb_system_maker/models.py (BacktestResult, lines 94-108 -- exact field order; `average_margin: float | None = None` is the current last field, `grade` appends after it)
    .planning/phases/02-integrity-fade-grade/02-RESEARCH.md (Pattern 2: Composite Grade as a Fixed, Testable Pure Function -- full verbatim reference implementation with exact threshold values and the documented rationale for z_score over roi_t_stat)
    .planning/phases/02-integrity-fade-grade/02-PATTERNS.md ("compute_grade + 5 sub-score helpers + count_overfit_filters" section -- the placement decision: call compute_grade AFTER the base BacktestResult is constructed, via dataclasses.replace, to avoid the circular dependency of compute_grade needing result.stats/season_breakdown before they exist)
    tests/test_backtest.py (test_system_stats_include_edge_and_wilson_bounds, test_sign_consistency_counts_profitable_and_total_seasons, test_run_backtest_populates_season_breakdown -- exact construction style to mirror for new grade fixtures)
  </read_first>
  <behavior>
    - Test test_grade_sample_size_score_boundaries: _sample_size_score returns 0.0/0.3/0.6/0.8/1.0 at decided counts one-below and at 30, 100, 300, 1000.
    - Test test_grade_roi_significance_score_boundaries: _roi_significance_score returns 0.0/0.2/0.5/0.75/1.0 at z_scores one-below and at 0, 1.0, 1.645, 1.96.
    - Test test_grade_consistency_score_boundaries: _consistency_score returns profitable/total_seasons for nonzero total_seasons, and 0.0 when total_seasons==0.
    - Test test_grade_permutation_score_boundaries: _permutation_score returns 1.0/0.8/0.5/0.25/0.0 at p_values one-below and at 0.01, 0.05, 0.10, 0.20.
    - Test test_grade_overfit_score_boundaries: _overfit_score returns 1.0/0.75/0.5/0.25/0.0 at active-filter-value counts 3, 7, 14, 24, and 25.
    - Test test_count_overfit_filters_follows_d06_counting_rule: a SystemFilter with favorite=True, min_spread set, providers non-empty, teams={3 values}, and a feature_filters entry with op="in" and a 4-value list produces the exact integer sum (1+1+1+3+4==10) per D-06; a second assertion confirms setting fade=True on the same SystemFilter does not change the count.
    - Test test_compute_grade_returns_none_for_zero_matched_bets: compute_grade on a BacktestResult with bets==0 returns None, not "F".
    - Test test_compute_grade_returns_a_for_all_high_subscores / test_compute_grade_returns_f_for_all_low_subscores: constructed BacktestResult/SystemStats/season_breakdown/system combinations that drive all 5 sub-scores to their max (or min) produce grade=="A" (or "F").
    - Test test_run_backtest_populates_grade_field: run_backtest(...).grade is a non-None letter for a system with matched bets, and every other field of the returned BacktestResult matches what run_backtest produced before this task (regression check).
  </behavior>
  <action>
    In models.py's BacktestResult dataclass, append `grade: str | None = None` as the new trailing field, directly after the existing `average_margin: float | None = None` field -- a safe append since every field from `stats` onward already carries a default.
    In backtest.py, add the module-level constant `_GRADE_BANDS` -- a tuple of (minimum composite score inclusive, letter) pairs: (0.85, "A"), (0.70, "B"), (0.55, "C"), (0.35, "D"), (0.00, "F"). Add `_sample_size_score(decided: int) -> float` returning 0.0 below 30, 0.3 below 100, 0.6 below 300, 0.8 below 1000, else 1.0. Add `_roi_significance_score(z_score: float) -> float` returning 0.0 below 0, 0.2 below 1.0, 0.5 below 1.645, 0.75 below 1.96, else 1.0 -- z_score is SystemStats.z_score (the hit-rate z-test vs break-even), not roi_t_stat, per 02-RESEARCH.md's documented rationale. Add `_consistency_score(profitable: int, total_seasons: int) -> float` returning 0.0 when total_seasons==0, else profitable/total_seasons. Add `_permutation_score(p_value: float) -> float` returning 1.0 below 0.01, 0.8 below 0.05, 0.5 below 0.10, 0.25 below 0.20, else 0.0. Add `_overfit_score(active_filter_value_count: int) -> float` returning 1.0 at count<=3, 0.75 at count<=7, 0.5 at count<=14, 0.25 at count<=24, else 0.0.
    Add `count_overfit_filters(system: SystemFilter) -> int` per D-06: for each of favorite/underdog/home/away, count 1 if truthy; for each of min_spread/max_spread/min_total/max_total, count 1 if not None; count 1 if providers is non-empty; for each of teams/conferences/seasons/weeks, count len(values); for each entry in feature_filters, count len(filt.value) when filt.op=="in" and filt.value is a list/tuple/set, else count 1. Do not reference system.fade anywhere in this function.
    Add `compute_grade(result: BacktestResult, system: SystemFilter) -> str | None`: return None immediately if result.bets == 0; otherwise compute decided = result.wins + result.losses, profitable/total_seasons = sign_consistency(result.season_breakdown), build a 5-element scores list from the helpers above (reading result.stats.z_score and result.stats.permutation_p_value from result.stats, which run_backtest always populates), average the 5 scores, then walk _GRADE_BANDS in the order given, returning the first letter whose threshold the composite average meets or exceeds.
    In run_backtest, change the existing `return BacktestResult(...)` (lines 37-51) to first bind the constructed value to a local named `result`, then return `replace(result, grade=compute_grade(result, system))` -- `replace` is already imported at the top of backtest.py (used by split_holdout) so no new import is needed.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_backtest.py -k "grade or overfit or compute_grade" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "grade: str | None = None" cfb_system_maker/models.py` returns exactly one match, as the last field of BacktestResult.
    - `grep -n "def compute_grade" cfb_system_maker/backtest.py` and `grep -n "def count_overfit_filters" cfb_system_maker/backtest.py` each return exactly one match.
    - Reading count_overfit_filters' function body confirms zero references to system.fade.
    - `.venv/Scripts/python.exe -m pytest tests/test_backtest.py -k "grade or overfit or compute_grade" -x` passes (all sub-score boundary tests, count_overfit_filters, compute_grade composite tests, run_backtest wiring).
  </acceptance_criteria>
  <done>compute_grade, its 5 sub-score helpers, and count_overfit_filters exist as pure functions with the exact documented thresholds; BacktestResult.grade is populated by run_backtest for every matched system and is None for zero matched bets.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Grade chip in the template and fix the breaking em-dash test</name>
  <files>cfb_system_maker/templates/index.html, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/templates/index.html (line 232 -- the exact current placeholder `&lt;article&gt;&lt;span&gt;Grade&lt;/span&gt;&lt;strong&gt;&amp;mdash;&lt;/strong&gt;&lt;/article&gt;`; line 229's Margin chip conditional-render style to mirror: `{% if result.average_margin is not none %}...{% else %}&amp;mdash;{% endif %}`)
    tests/test_web.py (lines 115-134 -- test_web_index_loads_filters_and_default_results, specifically line 128's `expected = run_backtest(...)` and line 134's hardcoded em-dash assertion that must change; test_web_money_won_chip_renders_unsigned_zero_for_no_matched_bets -- the `?season=2099` zero-bet pattern to reuse; import line 1-9 -- where to add compute_grade)
    .planning/phases/02-integrity-fade-grade/02-UI-SPEC.md ("Interaction Contract -- Grade Chip" section -- exact markup, no color, plain Jinja auto-escaping)
  </read_first>
  <action>
    In templates/index.html, replace the Grade chip's placeholder markup (line 232) with a conditional render mirroring the Margin chip's existing style: render result.grade when it is truthy, otherwise the literal em-dash entity -- through plain Jinja substitution only, with no filter applied to result.grade (Grade is always one of a fixed 6-value set produced entirely server-side, consistent with how every other chip value in this section is already rendered).
    In tests/test_web.py's import block, add compute_grade to the existing `from cfb_system_maker.backtest import run_backtest` line.
    In test_web_index_loads_filters_and_default_results, after the existing `expected = run_backtest(games, SystemFilter(side="home"))` line, add `expected_grade = compute_grade(expected, SystemFilter(side="home"))` and replace the old hardcoded em-dash assertion with one that asserts the Grade chip's rendered HTML contains `expected_grade` (computed, not a hardcoded literal letter, so the test doesn't drift if thresholds ever change).
    Add a new test test_web_grade_chip_renders_computed_letter_for_default_system that requests `/` on the same sample data, computes the expected grade the same way, and asserts the Grade chip HTML contains it -- a dedicated test whose name contains "grade" for reliable `-k grade` matching, independent of the pre-existing test above.
    Add a new test test_web_grade_chip_renders_em_dash_for_zero_matched_bets that requests `/?season=2099` (the same zero-bet pattern already used by test_web_money_won_chip_renders_unsigned_zero_for_no_matched_bets) and asserts the Grade chip renders the literal em-dash.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -k grade -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\|safe" cfb_system_maker/templates/index.html` shows no occurrence anywhere in the file, unchanged from before this phase (project-wide convention, not newly introduced by this task).
    - The Grade chip's markup still contains the literal `&mdash;` entity as its empty-state fallback text, now inside an `{% if %}/{% else %}` conditional rather than unconditionally.
    - `.venv/Scripts/python.exe -m pytest tests/test_web.py -k grade -x` passes (three grade-named tests: the updated default-system assertion's test, the new computed-letter test, and the new zero-bet em-dash test).
    - `.venv/Scripts/python.exe -m pytest -q` (full suite) passes with zero failures.
  </acceptance_criteria>
  <done>The Grade chip renders the computed letter for systems with matched bets and the em-dash for systems with zero matched bets; the full test suite is green.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `BacktestResult.grade: str | None = None` -- new dataclass field, `cfb_system_maker/models.py`.
- `_GRADE_BANDS`, `_sample_size_score`, `_roi_significance_score`, `_consistency_score`, `_permutation_score`, `_overfit_score`, `count_overfit_filters`, `compute_grade` -- new functions, `cfb_system_maker/backtest.py`.
- `run_backtest`'s return statement modified to wire grade via `dataclasses.replace` -- `cfb_system_maker/backtest.py`.
- Grade chip conditional render -- `cfb_system_maker/templates/index.html`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-computed Grade value -> Jinja template render | compute_grade's output is always one of a fixed 6-value set (A/B/C/D/F/None), never user-supplied text -- but it is rendered through the same template surface as free-text fields like theory, so the project's no-pipe-safe convention must be followed consistently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-03 | Tampering (reflected/stored XSS pattern family, per 02-RESEARCH.md Security Domain) | Grade chip render in templates/index.html | low | mitigate | Render result.grade through normal Jinja auto-escaping -- no safe-filter piping. Even though Grade's value space is fixed and server-computed (not user text), this keeps the pattern consistent with the project's established convention (STATE.md Phase 1 decision) and forecloses any future regression if Grade's value space ever changes. Task 2 acceptance criteria include a file-wide grep confirming the pattern is absent. |

Canon-security prohibitions beyond V5 Input Validation were considered and dropped: this phase has no auth/session/network/crypto surface (established in Phase 1's `01-SECURITY.md` and reaffirmed by `02-RESEARCH.md`'s Security Domain -- V2/V3/V4/V6 all marked "no"), so no STRIDE category besides the one Tampering pattern above applies.

</threat_model>

<verification>
`.venv/Scripts/python.exe -m pytest tests/test_backtest.py tests/test_web.py -q` then full suite `.venv/Scripts/python.exe -m pytest -q`.
</verification>

<success_criteria>
- compute_grade and its 5 sub-score helpers produce the documented, boundary-tested values.
- count_overfit_filters follows D-06's counting rule exactly and never counts fade.
- The Grade chip renders the computed letter (or em-dash for zero matched bets) in the web UI.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/02-integrity-fade-grade/02-02-SUMMARY.md` when done
</output>
