---
phase: 07-integrity-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cfb_system_maker/describe.py
  - cfb_system_maker/web.py
  - tests/test_describe.py
  - tests/test_web.py
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "An active filter whose (op, control) combination is not covered by describe.py's four hardcoded branches renders a visibly-distinct fallback sentence instead of being silently dropped."
    - "The fallback sentence's remove link (x) actually removes that filter when clicked — key round-trips through _query_href_removing's existing ff: prefix handling."
    - "The fallback row never renders an Edit button — index.html's {% if row.edit %} guard resolves to falsy for the fallback case specifically, not just for branches that happen to lack editable metadata."
    - "Both render surfaces that reuse describe() — the system editor's active-filter list and the Current Matches dashboard panel — show the fallback sentence for the same uncovered (op, control) combo."
  artifacts:
    - cfb_system_maker/describe.py
    - cfb_system_maker/web.py
  key_links:
    - "describe.py's _feature_group_sentence fallback branch -> web.py's edit_metadata_for_sentence suppression -> index.html's {% if row.edit %} guard"
    - "fallback sentence key (ff:{feature_key}) -> web.py _query_href_removing -> working remove href"
</must_haves>
---

<objective>
Close T-01-03: `describe()` must never silently drop an active filter's sentence while `feature_ok()` still applies it. Add a fallback branch to `_feature_group_sentence` (`cfb_system_maker/describe.py`) for any `(op, control)` combination the four existing branches don't cover, per D-locked wording `f"{label} filter applied (value: {value})"` (07-CONTEXT.md, 07-UI-SPEC.md Copywriting Contract). Wire suppression of the Edit affordance for fallback rows in `web.py` so the Remove-only rendering contract holds.

Purpose: an active filter that narrows results must always have a visible sentence and a working remove link — a filter that silently applies with no UI trace is a correctness/trust bug, not a cosmetic one.
Output: `_feature_group_sentence` fallback branch; `web.py` fallback-aware edit-suppression; tests proving both render surfaces and the remove-link round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-integrity-fixes/07-CONTEXT.md
@.planning/phases/07-integrity-fixes/07-UI-SPEC.md

@cfb_system_maker/describe.py
@cfb_system_maker/web.py
@tests/test_describe.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end fallback sentence — render, remove-link round-trip, Edit suppression</name>
  <files>cfb_system_maker/describe.py, cfb_system_maker/web.py, tests/test_describe.py, tests/test_web.py</files>
  <behavior>
    - `_feature_group_sentence` currently has 4 branches (`numeric` range, `bool` `eq`, `categorical` `eq`, `categorical` `in`) then `return None` at the end (describe.py:86). `feature_ok()` (features.py:557-566) supports `eq`/`in`/`gte`/`lte` on ANY control — so `op="in"` on a `bool` control, or `op="in"`/`op="eq"` on a `numeric` control, are real uncovered combos that `feature_ok()` still applies. Use `FeatureFilter(key="neutralSite", op="in", value=[True])` (bool + in) as the concrete uncovered-combo test fixture — `neutralSite` is `control="bool"`; `in` is not one of the 4 branches.
    - Test 1 (describe.py, RED first): `describe(SystemFilter(feature_filters=(FeatureFilter(key="neutralSite", op="in", value=[True]),)))` returns a sentence dict for key `"ff:neutralSite"` with text `f"{label} filter applied (value: {value})"` where `label = FEATURE_BY_KEY["neutralSite"].label` and `value = [True]` (the raw `filt.value`, per D-locked wording — do not reformat/join the value). Sentence must NOT equal any of the 4 hand-written wording shapes (`"is Yes/No"`, `"is {value}"`, `"is one of ..."`, range wording) — assert the fallback text is distinguishable by construction (it contains the literal substring `"filter applied"`, which none of the 4 branches ever produce — confirm by grepping the 4 branches' f-strings first).
    - Test 2 (describe.py): existing 21 tests in `test_describe.py` remain green unmodified — the fallback branch must not change output for any `(op, control)` combo already covered by the 4 branches or the "Unknown filter" key-not-found branch.
    - Test 3 (web.py, index route via Flask test client): for a system whose sole active filter is the uncovered `neutralSite`/`in` combo, `GET /` (or the appropriate route under test) renders `describe()`'s fallback text in the active-filters list AND the fallback row's `edit` value is falsy (assert directly on the row dict returned by the enrichment loop, or assert `data-candidate-id` attribute is absent from the rendered fallback `<li>` in the response HTML — pick whichever the existing web test suite's established pattern uses; grep `tests/test_web.py` for an existing `{% if row.edit %}`-adjacent assertion pattern first and match it).
    - Test 4 (web.py): the fallback sentence's remove href, when applied (parse `remove_href`'s query string and re-parse it into a `SystemFilter`, or hit the route with those args), yields a system where `describe()` no longer contains the `ff:neutralSite` sentence — the remove link actually works, not just renders.
  </behavior>
  <action>
    In `cfb_system_maker/describe.py`'s `_feature_group_sentence` (describe.py:58-86), after the existing bool/categorical `for filt in filts` loop's `return None` fallthrough — add one more branch before the function-level `return None`: iterate `filts`, and for the first filter return `{"text": f"{label} filter applied (value: {filt.value!r})", "key": f"ff:{key}"}` using the already-in-scope `label` and `key` variables (D-01 wording, per 07-CONTEXT.md and 07-UI-SPEC.md Copywriting Contract — deliberately distinct from the 4 hand-written branches, matching the precedent already set by the "Unknown filter" branch at describe.py:63). Do not touch the `numeric` branch (line 70-73) or the existing bool/categorical branches (line 76-85) — this is purely additive, appended after them. Plain f-string interpolation only — never wrap in `|safe` or any HTML-building helper (Jinja auto-escapes `{{ row.text }}` in index.html; this must stay true for the fallback exactly as it already is for the "Unknown filter" branch).

    In `cfb_system_maker/web.py`, the index route's sentence-enrichment loop (web.py:547-550: `sentences = describe(system); for row in sentences: row["remove_href"] = ...; row["edit"] = edit_metadata_for_sentence(system, row)`) unconditionally calls `edit_metadata_for_sentence`, which — for any `ff:{key}` row referencing a real registry feature — builds `candidate_id = f"feature:{key}"`, looks up `filter_descriptor(candidate_id)` (which returns non-None whenever the feature exists in `FEATURE_BY_KEY`, regardless of whether the row's specific `(op, control)` combo is one `describe()` can actually render into an editable modal), and returns populated edit metadata. This means the new fallback row would get a truthy `edit` value and `index.html`'s `{% if row.edit %}` guard (index.html:363) would incorrectly render an Edit button for a filter with no modal representation to edit into — violating the UI-SPEC's locked "must NOT include an edit field" contract. Fix by making the suppression explicit rather than relying on an accidental gap: in `edit_metadata_for_sentence` (web.py:814-882), for the `candidate_id.startswith("feature:")` branch (web.py:860-881), after resolving `filts` and `control`, check whether the filter's `(op, control)` combination is one of the renderable shapes the modal can actually reconstruct (numeric: has `gte` or `lte`; bool: has `op == "eq"`; categorical: has `op in ("eq", "in")`) — if none of the filters for this key match a renderable shape, `return None` instead of building `meta`. This is the same shape-check `_feature_group_sentence` already performs implicitly via its 4 branches; do not duplicate wording logic, just gate on `op`/`control` matching one of the 4 known combos before building edit metadata.
  </action>
  <verify>
    <automated>python -m pytest tests/test_describe.py tests/test_web.py -x -q</automated>
  </verify>
  <done>Fallback sentence renders for an uncovered (op, control) combo with distinct wording, its remove link actually clears the filter, and it never carries an edit affordance. All existing describe.py tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Current Matches dashboard panel — fallback sentence on the second render surface</name>
  <files>tests/test_web.py</files>
  <action>
    `_current_matches_panel` (web.py:1811-1848) already reuses `describe(saved.system)` and extracts only `row["text"]` per sentence (web.py:1846) — no `edit`/`remove_href` enrichment happens on this surface (confirmed: Current Matches is read-only, no per-row modal launcher). Because Task 1's fix lives entirely inside `describe()`, this surface picks up the fallback automatically with zero code changes here. Add a focused test proving it: build a `SavedSystem` whose `system.feature_filters` contains the same uncovered `neutralSite`/`op="in"` combo used in Task 1, call `_current_matches_panel` (or hit the dashboard route if `_current_matches_panel` isn't directly test-imported elsewhere — check existing Current Matches tests in `tests/test_web.py` for the established call pattern and match it) with upcoming-games fixtures that let the system match at least one row, and assert the fallback text (containing `"filter applied"`) appears in that row's `details` list.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web.py -x -q -k "current_matches or fallback"</automated>
  </verify>
  <done>Current Matches dashboard panel test confirms the fallback sentence renders identically on this second describe()-reusing surface, with no production code changes required beyond Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SystemFilter query params -> describe() -> Jinja template | User-controlled `ff_key`/`ff_op`/`ff_value` query params flow into `FeatureFilter.value`, which the new fallback branch interpolates directly into rendered sentence text |
| describe() sentence `key` -> `_query_href_removing` -> remove-link href | The fallback's `key` field must resolve through existing href-building logic without introducing an unhandled key shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `_feature_group_sentence` fallback text rendering | high | mitigate | Plain Jinja f-string interpolation only, never `\|safe` — matches the existing "Unknown filter" branch (describe.py:63) already proven safe in production. Test asserts the fallback path introduces no `\|safe` usage (grep-based test on describe.py source, mirroring `test_describe_module_has_no_flask_dependency`'s pattern). |
| T-07-02 | Tampering | Fallback sentence `key` -> remove-link href generation | high | mitigate | Fallback emits `key=f"ff:{key}"`, identical shape to all 4 existing branches, which `_query_href_removing`'s `key.startswith("ff:")` prefix check (web.py:981) already handles generically — no new key shape introduced. Task 1 Test 4 asserts the remove href, when applied, actually clears the filter from a subsequent `describe()` call — not just that a link renders. |
| T-07-03 | Elevation of Privilege | `edit_metadata_for_sentence` building edit metadata for an unrenderable (op, control) combo | medium | mitigate | Explicit renderable-shape gate added in Task 1 before building `meta` for `feature:` candidates — prevents a broken/misleading Edit-launcher button rather than relying on an accidental absence of editable state. |

</threat_model>

<verification>
Run `python -m pytest tests/test_describe.py tests/test_web.py -q` — all tests pass, including the new fallback-branch, remove-link-roundtrip, edit-suppression, and Current Matches panel tests. Manually confirm no `|safe` was introduced anywhere in the diff (`git diff cfb_system_maker/describe.py cfb_system_maker/web.py | grep safe` returns nothing).
</verification>

<success_criteria>
Every active filter — including any `(op, control)` combination not covered by an existing hardcoded sentence branch — renders a human-readable, visibly-distinct sentence with a working remove link, verified on both the system editor and the Current Matches dashboard panel.
</success_criteria>

<output>
Create `.planning/phases/07-integrity-fixes/07-01-SUMMARY.md` when done
</output>
