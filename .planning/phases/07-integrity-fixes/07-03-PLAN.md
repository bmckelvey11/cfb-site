---
phase: 07-integrity-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "PROJECT.md's Out of Scope section no longer describes Hide Duplicates as a reachable, pending deferral -- it states the architectural finding: run_backtest is 1:1 on game_id, there is no top-line duplicate-bet condition to toggle away."
    - "The Out of Scope bullet's wording is consistent with the Key Decisions table row already recorded for this same finding during the Phase 6 transition -- no contradiction between the two sections."
    - "The corrected bullet references FIX-02 as the real bug that was addressed instead (the /filter-detail double-count), so a future reader lands on the actual fix rather than a dead end."
  artifacts:
    - .planning/PROJECT.md
  key_links:
    - "PROJECT.md Out of Scope Hide Duplicates bullet <-> PROJECT.md Key Decisions table Hide Duplicates row -- both state the same finding"
</must_haves>
---

<objective>
Close the "Hide Duplicates" deferral in `PROJECT.md`'s Out of Scope section with the accurate architectural finding, matching what the Key Decisions table already records (done during the Phase 6 transition — this task confirms/finalizes the Out of Scope bullet specifically, which still carries the stale pre-Phase-6 reasoning).

Purpose: `PROJECT.md` currently has two sections describing the same topic inconsistently — the Key Decisions table (row: "Hide Duplicates deferred") already has the corrected finding ("Superseded — Phase 6 research confirmed `run_backtest` is structurally 1:1 on `game_id`..."), but the Out of Scope bullet still reads as if the toggle is a reachable, pending feature waiting on a mechanism ("...this waits until that mechanism exists"). A reader scanning only Out of Scope gets the wrong (stale) picture.
Output: Updated Out of Scope bullet in `.planning/PROJECT.md`, consistent with the existing Key Decisions table entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-integrity-fixes/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md's Out of Scope Hide Duplicates bullet to match the Key Decisions finding</name>
  <files>.planning/PROJECT.md</files>
  <action>
    Read `.planning/PROJECT.md`'s current Out of Scope section (the bullet reading: "Hide Duplicates toggle — only matters once a system can match both sides of one game (e.g. `bet_side`-identifying systems); current side-fixed systems can't self-collide, so this waits until that mechanism exists") and its Key Decisions table row for "Hide Duplicates deferred" (Rationale: "Only relevant once systems can match both sides of a single game; not true of side-fixed systems today" / Outcome: "Superseded — Phase 6 research confirmed `run_backtest` is structurally 1:1 on `game_id`; there is no top-line duplication to toggle away. The real bug is a `/filter-detail` modal per-value double-count, scoped to close formally in Phase 7 (FIX-02/FIX-03)").

    Replace the Out of Scope bullet with wording that states the corrected architectural finding directly (not the stale "waits until that mechanism exists" framing), consistent with the Key Decisions table's Outcome column, and references FIX-02 as the real fix. Suggested replacement text (adjust only if needed to match the surrounding bullet list's tone/format exactly): "Hide Duplicates toggle as originally specified — architecture research confirmed `run_backtest` is structurally 1:1 on `game_id`; a total system's either-side team/conference filter matches more games, never two candidate bets on the same `game_id`. There is no top-line duplication to toggle away. The real bug this deferral was gesturing at was the `/filter-detail` modal's per-value double-count, fixed directly in FIX-02 (Phase 7)." This is a documentation-only edit — do not touch the Key Decisions table row itself (already correct), and do not add a new row; only the Out of Scope bullet changes. Use a scoped `Edit` (not a whole-file `Write`) to replace exactly this bullet.
  </action>
  <verify>
    <automated>python -c "content = open('.planning/PROJECT.md', encoding='utf-8').read(); import sys; idx = content.index('## Out of Scope'); section = content[idx:idx+2000]; sys.exit(0 if ('run_backtest' in section and 'FIX-02' in section and 'waits until that mechanism exists' not in section) else 1)"</automated>
  </verify>
  <done>PROJECT.md's Out of Scope section states the corrected run_backtest 1:1-on-game_id finding for Hide Duplicates, references FIX-02 as the real fix, and no longer contains the stale "waits until that mechanism exists" framing. Consistent with the existing Key Decisions table row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Documentation-only change to `.planning/PROJECT.md`; no code path, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | N/A | `.planning/PROJECT.md` Out of Scope section | low | accept | Documentation-only edit, no trust boundary crossed, no runtime behavior affected. |

</threat_model>

<verification>
Run the automated check above, and manually diff `.planning/PROJECT.md` to confirm only the Out of Scope Hide Duplicates bullet changed — Key Decisions table and every other section untouched.
</verification>

<success_criteria>
`PROJECT.md` no longer describes Hide Duplicates as a reachable, pending deferral in either the Out of Scope bullet or the Key Decisions table — both are consistent and record the correct architectural finding (`run_backtest` is 1:1 on `game_id`; no top-line duplicate bets exist to toggle away; the real bug was the filter-detail double-count fixed by FIX-02).
</success_criteria>

<output>
Create `.planning/phases/07-integrity-fixes/07-03-SUMMARY.md` when done
</output>
