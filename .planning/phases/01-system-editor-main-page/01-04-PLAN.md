---
phase: 01-system-editor-main-page
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - tests/test_web.py
autonomous: true
requirements: [EDIT-03]

must_haves:
  truths:
    - "Every active filter on the currently loaded/built system renders as one plain-English sentence row (from describe(system)) with a visible remove control, in the workspace, positioned directly after the stat-chip header."
    - "When describe(system) returns [], the active-filter panel renders the exact copy 'No filters applied yet — every game in the dataset is included.' instead of an empty list."
    - "Clicking a filter sentence's remove control produces an href that is the CURRENT query string with only that one filter's param(s) removed — every other active filter, load_system, tab, and any other param is preserved unchanged."
    - "Each core-field remove key (favorite, underdog, home, away, spread_range, total_range, seasons, weeks, teams, conferences, providers) maps to its real query-param name(s) via _REMOVE_PARAM_MAP: spread_range removes BOTH min_spread and max_spread; total_range removes BOTH min_total and max_total; seasons/weeks/teams/conferences/providers map to filter_seasons/filter_weeks/filter_teams/filter_conferences/filter_providers (not their bare describe() key names, which differ from the actual query-param names)."
    - "A feature_filters remove key (formatted 'ff:<feature_key>' by describe()) removes only that one feature_key from the ff_enable multi-value list — it does not touch ff_key/ff_op/ff_value/ff_perspective, since the form always emits exactly one row per registry feature and ff_enable alone controls which are active."
    - "The remove control's accessible name includes the actual sentence text (aria-label='Remove filter: {sentence text}'), not just a bare 'remove'."
    - statement: "The active-filter sentence list uses display:grid; gap:8px with no max-height/scroll region — verify visually with 10+ simultaneously active filters (core + several feature filters) that the list doesn't visually collide with the sections rendered below it; if it does, a scroll region should be added in a follow-up, not silently assumed away."
      verification: backstop
  artifacts:
    - cfb_system_maker/web.py (_query_href_removing, _REMOVE_PARAM_MAP)
    - cfb_system_maker/templates/index.html (.active-filters section)
    - cfb_system_maker/static/styles.css (.active-filters, .remove-filter, .no-active-filters)
  key_links:
    - "index() route calls describe(system) (imported from cfb_system_maker.describe, built in 01-03-PLAN.md) and passes sentences=describe(system) into render_template -> templates/index.html iterates `sentences` to render one <li> per active filter, calling query_href_removing(row.key) for each remove href."
    - "_REMOVE_PARAM_MAP's keys must exactly match the string 'key' values describe() produces (favorite/underdog/home/away/spread_range/total_range/seasons/weeks/teams/conferences/providers) — this plan and 01-03-PLAN.md must never drift on this shared key contract."
---

<objective>
Wire `describe(system)` (built in `01-03-PLAN.md`) into the `index()` route and render the active-filter plain-English sentence list in the workspace, with a per-row remove control built from a second query-string-preserving helper, `_query_href_removing()`, per EDIT-03's remaining (UI-facing) half.

Purpose: completes EDIT-03 — the pure sentence-generation logic from Plan 03 has no user-visible effect until it is rendered and made interactive with correct remove-links.
Output: `_query_href_removing()` + `_REMOVE_PARAM_MAP` in `web.py`; `.active-filters` section in `templates/index.html`; supporting CSS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-editor-main-page/01-RESEARCH.md
@.planning/phases/01-system-editor-main-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _query_href_removing() + _REMOVE_PARAM_MAP and wire describe() into index()</name>
  <files>cfb_system_maker/web.py, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/web.py (current state after Plan 02's edits — read the file fresh; specifically the imports block, the `_query_href` function Plan 02 added, the `index()` route's render_template call, and `_form_values()`'s query-param names for seasons/weeks/teams/conferences/providers at lines 225-229 — these are the REAL query-param names, e.g. `filter_seasons` not `seasons`)
    cfb_system_maker/describe.py (built in 01-03-PLAN.md — the exact `key` strings it produces: favorite, underdog, home, away, spread_range, total_range, seasons, weeks, teams, conferences, providers, ff:&lt;feature_key&gt;)
    cfb_system_maker/templates/index.html (the ff_enable/ff_key hidden-input pattern at lines 130, 150 — confirms one ff_key row per registry feature, so removal only needs to drop the key from ff_enable)
    .planning/phases/01-system-editor-main-page/01-RESEARCH.md (Pattern 3: "removing a feature filter is just dropping its key from the ff_enable list — not an index-based array-splice"; T-01-02 mitigation)
  </read_first>
  <action>
    Add `from cfb_system_maker.describe import describe` to web.py's imports block.
    Add a module-level constant `_REMOVE_PARAM_MAP: dict[str, tuple[str, ...]] = {"favorite": ("favorite",), "underdog": ("underdog",), "home": ("home",), "away": ("away",), "spread_range": ("min_spread", "max_spread"), "total_range": ("min_total", "max_total"), "seasons": ("filter_seasons",), "weeks": ("filter_weeks",), "teams": ("filter_teams",), "conferences": ("filter_conferences",), "providers": ("filter_providers",)}` near the top of web.py, after the imports.
    Add a new function `_query_href_removing(key: str) -> str`: build `copy = MultiDict(request.args.items(multi=True))`; if `key.startswith("ff:")`, extract `feature_key = key[3:]` and `copy.setlist("ff_enable", [v for v in copy.getlist("ff_enable") if v != feature_key])`; else, for each `param in _REMOVE_PARAM_MAP.get(key, (key,))`, call `copy.poplist(param)`; return `"?" + urlencode(list(copy.items(multi=True)))`.
    Register `app.jinja_env.globals["query_href_removing"] = _query_href_removing` inside `create_app()`, next to the existing `query_href` global registration from Plan 02.
    In the `index()` route, add `sentences=describe(system)` as a new kwarg to the existing `render_template("index.html", ...)` call (the `system` variable is already computed earlier in the route from either `_system_from_form` or the loaded system).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_REMOVE_PARAM_MAP" cfb_system_maker/web.py` shows the constant with exactly 11 core-field keys.
    - `grep -n "def _query_href_removing" cfb_system_maker/web.py` returns exactly one match.
    - A new integration test in tests/test_web.py GETs `/?side=away&underdog=on&min_spread=3` and asserts the response HTML contains an href with `min_spread` and `max_spread` both absent when removing the "spread_range" filter, i.e. asserts the "spread_range" row's remove-href does NOT contain `min_spread=` (query the href attribute text directly), while `side=away` remains present in that same href.
    - A second new integration test enables one feature filter via `?ff_key=neutralSite&ff_enable=neutralSite&ff_op=eq&ff_value=true&ff_perspective=single` and asserts the corresponding remove-href drops `ff_enable` for that key while other `ff_key`/`ff_op`/`ff_value` params for OTHER (unset) features are unaffected.
  </acceptance_criteria>
  <done>describe(system) is wired into the render context; _query_href_removing correctly maps every describe() key to its real query-param(s) and produces a query string with only that filter removed.</done>
</task>

<task type="auto">
  <name>Task 2: Render the active-filter sentence list with remove controls</name>
  <files>cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/templates/index.html (current state after Plan 01/02's edits — read fresh; specifically the restyled `.metrics` chip header block from Plan 01 and the `.tabs`/tab-conditional block from Plan 02, to find the correct insertion point directly after the chip header and before `.metrics.stats-panel`)
    cfb_system_maker/static/styles.css (the `.check`/`.segmented label` min-height: 36px convention at lines 112-123 — the remove control must reuse this interactive-target-size convention, not a bare unpadded inline link)
    .planning/phases/01-system-editor-main-page/01-UI-SPEC.md (Copywriting Contract: empty-state body text verbatim; remove control glyph "✕" and aria-label pattern; Color: remove control uses --loss; Spacing: sm 8px sentence-row internal gap, md 16px sentence-row padding, lg 24px active-filter panel padding)
  </read_first>
  <action>
    Insert a new `<section class="active-filters-panel">` directly after the closing `</section>` of the restyled `.metrics` stat-chip header (Plan 01's output) and before the `{% if result.stats %}` stats-panel block. Inside it: `{% if sentences %}<ul class="active-filters">{% for row in sentences %}<li>{{ row.text }} <a class="remove-filter" href="{{ query_href_removing(row.key) }}" aria-label="Remove filter: {{ row.text }}">&times;</a></li>{% endfor %}</ul>{% else %}<p class="no-active-filters">No filters applied yet — every game in the dataset is included.</p>{% endif %}`. `row.text` renders via Jinja's default auto-escaping (do not add `|safe` anywhere here).
    In cfb_system_maker/static/styles.css, add `.active-filters-panel` (padding 24px, the lg spacing token, background var(--panel), border 1px solid var(--border), border-radius 6px, margin-bottom 18px matching the existing section-spacing convention), `.active-filters` (display: grid, gap: 8px — the sm token, list-style: none, padding: 0, margin: 0), `.active-filters li` (display: flex, align-items: center, justify-content: space-between, gap: 8px, padding: 16px — the md token, background var(--panel-strong), border-radius: 6px, font-size: 14px), `.remove-filter` (display: inline-flex, align-items: center, justify-content: center, min-height: 36px, min-width: 36px, color: var(--loss), border-radius: 6px, text-decoration: none, font-size: 16px), `.no-active-filters` (color: var(--muted), font-size: 14px).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A new integration test GETs `/` with no filters set (default index, no query params beyond none) and asserts "No filters applied yet — every game in the dataset is included." is present in the HTML.
    - A new integration test GETs `/?side=home&favorite=on` and asserts "the team is a favorite" is present in the HTML alongside an `aria-label="Remove filter: the team is a favorite"` attribute.
    - `.venv/Scripts/python.exe -m pytest -q` (full suite) passes.
  </acceptance_criteria>
  <done>The workspace shows one sentence row per active filter with a working, correctly-scoped remove link, or the documented empty-state copy when no filters are active.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `_query_href_removing(key: str) -> str` — new function, `cfb_system_maker/web.py`.
- `_REMOVE_PARAM_MAP: dict[str, tuple[str, ...]]` — new module-level constant, `cfb_system_maker/web.py`.
- `app.jinja_env.globals["query_href_removing"]` — new Jinja global registration inside `create_app()`, `cfb_system_maker/web.py`.
- `sentences` — new `render_template("index.html", ...)` context kwarg in `index()` (from `describe(system)`, built in 01-03-PLAN.md).
- `.active-filters-panel` section — `cfb_system_maker/templates/index.html`.
- `.active-filters-panel`, `.active-filters`, `.active-filters li`, `.remove-filter`, `.no-active-filters` — new CSS classes, `cfb_system_maker/static/styles.css`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Query string -> href construction (remove-links) | This plan's `_query_href_removing()` is the second query-href-building surface (T-01-02) alongside Plan 02's `_query_href()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-02 | Tampering | `_query_href_removing()` in web.py; remove-filter links in templates/index.html | high | mitigate | Same mitigation pattern as Plan 02: hrefs are built exclusively via `urllib.parse.urlencode` over a `MultiDict` copy, never by string-interpolating `request.args` values directly. `describe()`'s sentence `text` is rendered via Jinja's default auto-escaping (no `|safe`), covering the case where a feature label or filter value contains user-influenced characters. |
| T-01-01 | Tampering / Information Disclosure | Theory field (not touched by this plan) | high | accept (N/A here) | Owned by Plan 05. |

</threat_model>

<verification>
`.venv/Scripts/python.exe -m pytest tests/test_web.py -q` then full suite `.venv/Scripts/python.exe -m pytest -q`.
</verification>

<success_criteria>
- Every active filter renders as a plain-English sentence with a correctly-scoped remove control; the empty state renders the documented copy when no filters are active.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/01-system-editor-main-page/01-04-SUMMARY.md` when done
</output>
