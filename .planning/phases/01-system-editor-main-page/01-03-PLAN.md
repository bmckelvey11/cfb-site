---
phase: 01-system-editor-main-page
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - cfb_system_maker/describe.py
  - tests/test_describe.py
autonomous: true
requirements: [EDIT-03]

must_haves:
  truths:
    - "describe(system) returns [] for a SystemFilter with every field at its default (no active constraints) — the empty-input case."
    - "describe(system) is a pure function: it imports nothing from flask, takes no request object, touches no module-level mutable state, and calling it twice on the same (frozen-dataclass, immutable) SystemFilter returns two structurally-equal lists."
    - "describe(system) emits sentences in a fixed, deterministic order regardless of which fields are set: favorite, underdog, home, away, spread_range, total_range, seasons, weeks, teams, conferences, providers, then feature_filters in system.feature_filters' own tuple order."
    - "favorite/underdog/spread_range sentences are emitted ONLY when system.bet_type == 'spread' — matches_system() in backtest.py never checks system.favorite/underdog/min_spread/max_spread when bet_type == 'total' (those checks live inside the spread-only else branch), so describe() must not claim a constraint is active when matches_system() would silently ignore it."
    - "total_range (min_total/max_total) sentences are emitted regardless of system.bet_type — matches_system() checks min_total/max_total in BOTH the total-bet and spread-bet branches, so this constraint is genuinely always active when set."
    - "seasons/weeks/teams/conferences/providers are Python sets on SystemFilter, so duplicate values are structurally impossible — describe() renders them sorted and comma-joined with no separate dedup step needed."
    - "Numeric filter values (spread/total range endpoints, feature gte/lte thresholds) are formatted by stripping a trailing '.0' for whole numbers while preserving fractional values as-is (e.g. -14.0 renders as '-14', -14.5 stays '-14.5') via a private _fmt_num() helper — the underlying stored float is never re-rounded or truncated for the match itself, only for display."
    - "When min_spread == max_spread (or min_total == max_total), describe() renders 'the spread is exactly {v}' / 'the total is exactly {v}' instead of 'is between {v} and {v}'."
    - "An unknown feature_filters key (FEATURE_BY_KEY.get(filt.key) is None) renders a visible warning sentence — text f'Unknown filter \"{filt.key}\" is unavailable', key f'ff:{filt.key}' — never raises and is never silently dropped. This matches features.feature_ok()'s real behavior (returns False for an unknown key, so the system matches ZERO games); describe() must surface that instead of implying every game is included. Per 01-REVIEWS.md finding #2 (blocker)."
    - "Team-scoped feature sentences (FeatureDef.team_scoped is True) prefix the label with a perspective word based on FeatureFilter.perspective — home->'Home', away->'Away', bet_side->'Bet-side', opponent->'Opponent', either->'Either team's' (e.g. 'Bet-side Returning PPA is at least 0.5'). Non-team-scoped features, and team-scoped features left at the default perspective ('single' or ''), get no prefix — matching resolve_feature_value()'s own pass-through branch in features.py, so describe() never implies a perspective the real value resolution ignores. Per 01-REVIEWS.md finding #3."
  artifacts:
    - cfb_system_maker/describe.py
    - tests/test_describe.py
  key_links:
    - "describe(system) is imported and called by web.py's index() route in Plan 04 (01-04-PLAN.md), which pairs each sentence's stable string 'key' (e.g. 'favorite', 'spread_range', 'seasons', 'ff:<feature_key>') with a _REMOVE_PARAM_MAP lookup to build remove-links. The exact key strings this plan produces are a hard contract Plan 04 depends on verbatim — do not rename them without updating Plan 04."
  prohibitions:
    - statement: "Do NOT let describe()'s plain-English sentence wording drift from matches_system()'s actual filter semantics (which fields are checked, sign conventions, which bet_type each field applies under). The sentence list is this betting tool's primary user-facing trust surface — a system whose displayed description doesn't match what it actually filters on could lead a user to trust or distrust a system for the wrong reason."
      status: unverified
      verification: backstop
    - statement: "Do NOT silently skip an unknown feature_filters key (continue with no sentence emitted). A silent skip lets the UI claim a system covers 'every game' while feature_ok() is actually zeroing out all matches for that unrecognized key — this exact silent-drop pattern was flagged as a blocker by 01-REVIEWS.md finding #2. Unknown keys MUST render a visible 'Unknown filter ... is unavailable' sentence instead."
      status: unverified
      verification: automated
---

<objective>
Implement `describe(system: SystemFilter) -> list[dict[str, object]]` as a new, framework-independent pure function mapping every active `SystemFilter` constraint (core dataclass fields + `feature_filters`) to a plain-English sentence with a stable removal `key`, per EDIT-03 and the op-phrase mapping table in `01-RESEARCH.md` Pattern 2. Built test-first (RED -> GREEN) because it is a pure function with fully defined input/output pairs across ~12 branches.

Purpose: this is the backend half of EDIT-03's "plain-English filter sentences" requirement. It has zero dependency on web.py/templates/index.html (no Flask import, per RESEARCH's explicit reuse requirement for Phase 4's "Current Matches" view), so it ships independently of Plan 01/02's shared-file work in Wave 1.
Output: `cfb_system_maker/describe.py` with `describe()`; `tests/test_describe.py` (the Wave 0 test-scaffold `01-VALIDATION.md` calls out as missing).

Review incorporation (`01-REVIEWS.md`, Codex): finding #2 (blocker) — unknown feature_filters keys must never be silently hidden, since `feature_ok()` zero-matches on them; fixed by rendering a visible "Unknown filter ... is unavailable" sentence (Task 2, step 8). Finding #3 (high) — feature sentences must name which team's value drives the filter; fixed by a perspective-word prefix on team-scoped feature labels (Task 2, step 8) and the sentence-ordering conflict Codex flagged (spread_range vs. home/away) was already resolved in the prior pass via two separate `bet_type=="spread"` gates (Task 2, step 2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-editor-main-page/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write the full failing test suite for describe()</name>
  <files>tests/test_describe.py</files>
  <read_first>
    cfb_system_maker/models.py (SystemFilter lines 30-48, FeatureFilter lines 22-27 — exact field names/types/defaults)
    cfb_system_maker/features.py (FeatureDef lines 30-40, FEATURE_BY_KEY line 176, and the FEATURE_REGISTRY entries for a bool feature e.g. "neutralSite", a categorical feature e.g. "venue", and a numeric feature e.g. "weather_temperature" — use these real keys/labels in tests rather than inventing fixture features)
    cfb_system_maker/backtest.py (matches_system lines 98-158 — the exact branch structure proving favorite/underdog/min_spread/max_spread only apply when bet_type != "total", while min_total/max_total apply in both branches; home/away/seasons/weeks/teams/conferences/providers apply unconditionally)
    tests/test_backtest.py (existing dataclass-construction test style — no fixtures/mocks, direct SystemFilter(...)/FeatureFilter(...) construction)
  </read_first>
  <action>
    Create tests/test_describe.py importing `from cfb_system_maker.describe import describe` and `from cfb_system_maker.models import FeatureFilter, SystemFilter`. Write the following tests (describe.py does not exist yet, so this file is expected to fail at collection/import time — that is the RED state):
    test_describe_empty_system_returns_empty_list: `describe(SystemFilter())` == `[]`.
    test_describe_favorite_and_underdog_sentences: `SystemFilter(bet_type="spread", favorite=True)` produces a sentence dict `{"text": "the team is a favorite", "key": "favorite"}` in the result; separately `SystemFilter(bet_type="spread", underdog=True)` produces `{"text": "the team is an underdog", "key": "underdog"}`.
    test_describe_home_and_away_sentences: `home=True` -> `{"text": "home games only", "key": "home"}`; `away=True` -> `{"text": "away games only", "key": "away"}` (both apply regardless of bet_type).
    test_describe_spread_range_between_at_least_at_most_and_exact: with `bet_type="spread"` — `min_spread=-14, max_spread=-3` -> text "the spread is between -14 and -3", key "spread_range"; `min_spread=-14` only -> "the spread is at least -14"; `max_spread=-3` only -> "the spread is at most -3"; `min_spread=-7, max_spread=-7` -> "the spread is exactly -7" (not "between -7 and -7").
    test_describe_spread_fields_ignored_for_total_bet_type: `SystemFilter(bet_type="total", favorite=True, min_spread=-14, max_spread=-3)` produces NO sentence with key "favorite" or "spread_range" anywhere in the result (list comprehension assertion over `[row["key"] for row in result]`) — this is the drift-prevention case from matches_system's branch structure.
    test_describe_total_range_applies_regardless_of_bet_type: `SystemFilter(bet_type="spread", min_total=45, max_total=55)` DOES produce `{"text": "the total is between 45 and 55", "key": "total_range"}` even though bet_type is "spread"; same assertion repeated with `bet_type="total"`.
    test_describe_seasons_weeks_teams_conferences_providers_join_sorted_and_comma_separated: `SystemFilter(seasons={2023, 2021, 2022})` -> text "the season is 2021, 2022, 2023", key "seasons" (sorted ascending, comma-space joined); one analogous case each for weeks (key "weeks"), teams (key "teams", string values), conferences (key "conferences"), providers (key "providers").
    test_describe_feature_filter_bool_eq_yes_no: `feature_filters=(FeatureFilter(key="neutralSite", op="eq", value=True),)` -> text "Neutral Site is Yes", key "ff:neutralSite" (use `FEATURE_BY_KEY["neutralSite"].label` for the expected label text rather than hardcoding, so the test tracks the registry); repeat with `value=False` -> "... is No".
    test_describe_feature_filter_categorical_eq: `FeatureFilter(key="venue", op="eq", value="Michigan Stadium")` -> text "{label} is Michigan Stadium", key "ff:venue".
    test_describe_feature_filter_categorical_in_list: `FeatureFilter(key="venue", op="in", value=["Michigan Stadium", "The Horseshoe"])` -> text "{label} is one of Michigan Stadium, The Horseshoe".
    test_describe_feature_filter_numeric_gte_and_lte: `FeatureFilter(key="weather_temperature", op="gte", value=50.0)` -> "{label} is at least 50"; `op="lte", value=32.5` -> "{label} is at most 32.5" (proves the _fmt_num trailing-.0-strip: 50.0 -> "50", 32.5 stays "32.5"). `weather_temperature` is NOT team_scoped, so this also proves the no-prefix default path.
    test_describe_unknown_feature_key_renders_warning_sentence (01-REVIEWS.md finding #2, blocker): `feature_filters=(FeatureFilter(key="does_not_exist", op="eq", value=True),)` -> `describe(system)` returns a ONE-element list `[{"text": 'Unknown filter "does_not_exist" is unavailable', "key": "ff:does_not_exist"}]` — never `[]`, never an exception. This is the fix for the blocker: feature_ok() in features.py returns False (zero matching games) for an unrecognized key, so describe() must say so rather than silently implying every game is included.
    test_describe_feature_filter_perspective_prefixes_team_scoped_label (01-REVIEWS.md finding #3): `returning_ppa` is team_scoped=True (see features.py FEATURE_REGISTRY). For each `(perspective, expected_prefix)` in `[("home", "Home"), ("away", "Away"), ("bet_side", "Bet-side"), ("opponent", "Opponent"), ("either", "Either team's")]`: `FeatureFilter(key="returning_ppa", op="gte", value=0.5, perspective=perspective)` -> text == f"{expected_prefix} {FEATURE_BY_KEY['returning_ppa'].label} is at least 0.5" (e.g. "Bet-side Returning PPA % is at least 0.5").
    test_describe_feature_filter_perspective_single_default_has_no_prefix_even_when_team_scoped: `FeatureFilter(key="returning_ppa", op="gte", value=0.5)` (perspective defaults to "single", not passed explicitly) -> text == "Returning PPA % is at least 0.5" (no prefix) — matches resolve_feature_value()'s own perspective in {"single", ""} passthrough branch in features.py, which does not select a home_/away_-prefixed value either.
    test_describe_feature_filter_perspective_ignored_for_non_team_scoped: `venue` is NOT team_scoped. `FeatureFilter(key="venue", op="eq", value="Michigan Stadium", perspective="home")` -> text == "Venue is Michigan Stadium" (no "Home" prefix applied) — a non-team-scoped feature's perspective value is inert in features.py's own resolve_feature_value(), so describe() must not fabricate a prefix that implies otherwise.
    test_describe_output_order_is_fixed_and_deterministic: a system with favorite=True, min_total=45, seasons={2023} all set simultaneously (bet_type="spread") -> `[row["key"] for row in describe(system)]` == `["favorite", "total_range", "seasons"]` exactly (proves the canonical field-check order: favorite, underdog, home, away, spread_range, total_range, seasons, weeks, teams, conferences, providers, then feature_filters).
    test_describe_is_pure_no_side_effects_call_twice_returns_equal_lists: build one non-trivial SystemFilter, call `describe(system)` twice, assert the two results are `==` (structurally equal) — no test uses a Flask app or request context anywhere in this file, which is itself the statelessness/concurrency-safety proof.
    test_describe_module_has_no_flask_dependency: `import inspect, cfb_system_maker.describe as describe_module; source = inspect.getsource(describe_module); assert "flask" not in source.lower()` and `assert "request" not in source.lower()`.
    test_describe_non_finite_numbers_do_not_crash: use `min_spread=float("nan")` and a numeric feature value `float("inf")`; assert describe() returns sentence strings instead of raising. Crafted query values can reach `_optional_float()` even though browser number inputs normally reject them.
    Run `.venv/Scripts/python.exe -m pytest tests/test_describe.py -q` and confirm it fails (import error, since cfb_system_maker/describe.py does not exist yet) — this is the expected RED state, not a bug to fix in this task.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_describe.py -q 2>&1 | grep -q "ModuleNotFoundError\|ImportError\|error"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_describe.py exists with at least the 19 test functions enumerated above.
    - `.venv/Scripts/python.exe -m pytest tests/test_describe.py -q` fails at collection (import error) — confirms RED state before Task 2 implements describe.py.
  </acceptance_criteria>
  <done>A complete, failing (RED) test suite exists for describe() covering every op-phrase branch, the bet_type gating rule, ordering, empty input, the unknown-key warning sentence, perspective-prefix wording, numeric formatting, and purity.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement describe() to pass the full test suite</name>
  <files>cfb_system_maker/describe.py</files>
  <read_first>
    tests/test_describe.py (just written in Task 1 — the exact contract to satisfy)
    cfb_system_maker/models.py (SystemFilter, FeatureFilter — note FeatureFilter.perspective defaults to "single")
    cfb_system_maker/features.py (FEATURE_BY_KEY, FeatureDef.label/control/team_scoped; resolve_feature_value lines 210-226 and _perspective_to_side lines 229-238 — the exact perspective vocabulary: "home"/"away"/"bet_side"/"opponent"/"either"/"single"; feature_ok lines 253-273 — confirms an unknown key returns False, i.e. zero matching games)
    cfb_system_maker/backtest.py (matches_system lines 98-158 — re-confirm the bet_type=="spread"-only gating for favorite/underdog/min_spread/max_spread before writing the corresponding describe() branches)
  </read_first>
  <action>
    Create cfb_system_maker/describe.py with module header `from __future__ import annotations` then `from cfb_system_maker.features import FEATURE_BY_KEY` and `from cfb_system_maker.models import SystemFilter` — no `flask` import, no `request` reference anywhere in this file (framework-independence is a hard requirement, verified by Task 1's test_describe_module_has_no_flask_dependency).
    Add a private helper `_fmt_num(value: float) -> str`: normalize with `numeric = float(value)`, return `str(int(numeric))` when `numeric.is_integer()`, otherwise `str(numeric)`. Using `is_integer()` avoids `int(float('nan'))` / `int(float('inf'))` crashes from crafted query values while preserving normal whole-number formatting.
    Add a module-level constant `_PERSPECTIVE_PREFIX: dict[str, str] = {"home": "Home", "away": "Away", "bet_side": "Bet-side", "opponent": "Opponent", "either": "Either team's"}` (01-REVIEWS.md finding #3) — the perspective vocabulary here must stay exactly aligned with `features._perspective_to_side`'s own recognized values; do not invent additional perspective strings.
    Implement `describe(system: SystemFilter) -> list[dict[str, object]]` building a `sentences: list[dict[str, object]] = []` list and appending, in this EXACT fixed order (per must_haves' ordering truth):
    1. Under `if system.bet_type == "spread":`, append `{"text": "the team is a favorite", "key": "favorite"}` when favorite is enabled.
    2. `if system.underdog:` -> `{"text": "the team is an underdog", "key": "underdog"}`.
    Close that first spread-only block before evaluating home/away. Use a second `if system.bet_type == "spread":` block for spread_range after home/away. Two explicit gates are required to satisfy both semantics and canonical output order; one block wrapping steps 1, 2, and 5 would force spread_range before home/away and contradict this plan's own ordering truth. Do not gate home/away/total_range/seasons/weeks/teams/conferences/providers.
    3. `if system.home:` -> `{"text": "home games only", "key": "home"}`.
    4. `if system.away:` -> `{"text": "away games only", "key": "away"}`.
    5. Spread range (inside the bet_type=="spread" gate): if both `min_spread`/`max_spread` are not None and equal -> `"the spread is exactly {_fmt_num(min_spread)}"`; both set and different -> `"the spread is between {_fmt_num(min_spread)} and {_fmt_num(max_spread)}"`; only min set -> `"the spread is at least {_fmt_num(min_spread)}"`; only max set -> `"the spread is at most {_fmt_num(max_spread)}"`; key `"spread_range"` for all four sub-cases; append nothing if neither is set.
    6. Total range (NOT gated by bet_type — always evaluated): same four sub-case structure as spread range but using `min_total`/`max_total`, text prefix "the total is ...", key `"total_range"`.
    7. For each of `(system.seasons, "season", "seasons")`, `(system.weeks, "week", "weeks")`, `(system.teams, "team", "teams")`, `(system.conferences, "conference", "conferences")`, `(system.providers, "provider", "providers")` in that exact order: if the set is non-empty, append `{"text": f"the {label} is {', '.join(str(v) for v in sorted(values))}", "key": key}`.
    8. For each `filt in system.feature_filters` (in tuple order): look up `feature = FEATURE_BY_KEY.get(filt.key)`. If `feature is None` (01-REVIEWS.md finding #2, BLOCKER — do NOT `continue`/skip silently here): append `{"text": f'Unknown filter "{filt.key}" is unavailable', "key": f"ff:{filt.key}"}` and move to the next filter. This surfaces the same unrecognized key that `features.feature_ok()` would otherwise silently zero-match on (it returns `False` for every game when `FEATURE_BY_KEY.get(filt.key)` is `None`), so the UI never implies a system covers "every game" when it is actually excluding all of them.
    Otherwise, compute an optional perspective-prefixed label: `label = f"{_PERSPECTIVE_PREFIX[filt.perspective]} {feature.label}"` when `feature.team_scoped` is `True` and `filt.perspective in _PERSPECTIVE_PREFIX`; otherwise `label = feature.label` unchanged (covers non-team-scoped features and the default `perspective == "single"`/`""` case) — this exactly mirrors `resolve_feature_value()`'s own `not feature.team_scoped or perspective in {"single", ""}` passthrough branch in features.py, so describe() never names a perspective the real value resolution ignores (01-REVIEWS.md finding #3).
    Branch on `(filt.op, feature.control)`, substituting `label` for `feature.label` everywhere below: `("eq", "bool")` -> `f"{label} is {'Yes' if filt.value else 'No'}"`; `("eq", "categorical")` -> `f"{label} is {filt.value}"`; `("in", "categorical")` -> `f"{label} is one of {', '.join(filt.value)}"`; `("gte", "numeric")` -> `f"{label} is at least {_fmt_num(float(filt.value))}"`; `("lte", "numeric")` -> `f"{label} is at most {_fmt_num(float(filt.value))}"`. Append `{"text": <text>, "key": f"ff:{filt.key}"}` for whichever branch matched; if none of the five (op, control) combinations match, skip silently (defensive, should not occur given the registry's fixed control/op vocabulary).
    Return `sentences`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_describe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_describe.py -q` passes with 0 failures (all 19+ tests from Task 1 GREEN, including the unknown-key-warning and perspective-prefix tests).
    - `grep -n "^import flask\|^from flask\|request\." cfb_system_maker/describe.py` returns no matches.
    - `grep -n "_PERSPECTIVE_PREFIX" cfb_system_maker/describe.py` returns at least two matches (the constant definition plus its use inside `describe()`).
    - `grep -n "continue" cfb_system_maker/describe.py` — if present, confirm by reading context that it is NOT reached when `feature is None` (the unknown-key branch must append a warning sentence, never bare-continue past it).
    - `.venv/Scripts/python.exe -m pytest -q` (full suite) passes, confirming no regression elsewhere.
  </acceptance_criteria>
  <done>describe.py exists, is framework-independent, unknown feature keys render a visible warning sentence instead of being dropped, team-scoped feature sentences carry a perspective prefix, and the full test_describe.py suite is green.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `cfb_system_maker/describe.py` — new file.
- `describe(system: SystemFilter) -> list[dict[str, object]]` — new function, `cfb_system_maker/describe.py`.
- `_fmt_num(value: float) -> str` — new private helper function, `cfb_system_maker/describe.py`.
- `_PERSPECTIVE_PREFIX: dict[str, str]` — new module-level constant, `cfb_system_maker/describe.py` (01-REVIEWS.md finding #3).
- `tests/test_describe.py` — new file.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new in this plan | describe() is a pure function over an already-validated in-process SystemFilter — it performs no I/O, no rendering, no query-string handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering / Information Disclosure | Theory field (not touched by this plan) | high | accept (N/A here) | Not applicable — describe() never touches the theory field; owned by Plan 05. |
| T-01-02 | Tampering | Query-string href construction (not touched by this plan) | high | accept (N/A here) | describe() returns plain data, no hrefs; query-href construction happens in Plan 04, which consumes this plan's `key` strings. |
| T-01-03 | Tampering / Information Disclosure | describe()'s sentence-text generation vs. matches_system()/feature_ok()'s actual filter semantics | high | mitigate | Three code-level mitigations: (1) the bet_type=="spread" gate around favorite/underdog/spread_range (Task 2, step 2) keeps sentence gating aligned with matches_system()'s branch structure — proven by test_describe_spread_fields_ignored_for_total_bet_type; (2) an unknown feature_filters key renders a visible 'Unknown filter "..." is unavailable' sentence instead of being silently dropped (Task 2, step 8), matching features.feature_ok()'s own False-for-unknown-key behavior — proven by test_describe_unknown_feature_key_renders_warning_sentence (01-REVIEWS.md finding #2, blocker); (3) team-scoped feature sentences carry a perspective prefix (Home/Away/Bet-side/Opponent/Either team's) naming which team's value drives the filter, matching resolve_feature_value()'s own perspective branching — proven by test_describe_feature_filter_perspective_prefixes_team_scoped_label (01-REVIEWS.md finding #3). Severity raised from medium to high in this revision because mitigation (2) closes a blocker-rated finding. |

</threat_model>

<verification>
`.venv/Scripts/python.exe -m pytest tests/test_describe.py -q` then full suite `.venv/Scripts/python.exe -m pytest -q`.
</verification>

<success_criteria>
- describe() exists, is pure and framework-independent, and every op-phrase/ordering/gating rule from 01-RESEARCH.md Pattern 2 is implemented and tested.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/01-system-editor-main-page/01-03-SUMMARY.md` when done
</output>
