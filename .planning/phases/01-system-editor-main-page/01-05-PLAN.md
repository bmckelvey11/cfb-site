---
phase: 01-system-editor-main-page
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - cfb_system_maker/models.py
  - cfb_system_maker/storage.py
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - tests/test_storage_systems.py
  - tests/test_web.py
autonomous: true
requirements: [EDIT-05]

must_haves:
  truths:
    - "SavedSystem.theory defaults to '' and is the last field on the frozen dataclass, so every existing direct SavedSystem(...) construction site keeps working unmodified."
    - "storage.save_system(name, system, data_dir, theory='') persists theory into the saved JSON's top-level 'theory' key, alongside the existing 'name'/'saved_at'/'system' keys."
    - "storage.load_saved_system(name, data_dir) returns the full SavedSystem including theory, reading it via payload.get('theory', ''); storage.load_system(name, data_dir)'s existing signature and bare-SystemFilter return type are completely unchanged, so cli.py:162 and the existing test_storage_systems.py assertions keep passing untouched."
    - "A saved-system JSON file written before this phase existed (no 'theory' key at all) loads via load_saved_system() with theory=='' — never a KeyError."
    - "The sidebar's editable <textarea name=\"theory\"> is bundled into the existing GET filter form near Save Name/Save System (per CONTEXT.md's locked D-02), and is saved only via the existing POST /save endpoint — no new route is introduced."
    - "On a fresh GET request (no ?load_system=), the theory textarea is blank ('') — theory only round-trips into the form when a saved system is loaded, exactly mirroring how save_name already behaves today."
    - "The workspace shows a read-only theory display panel directly above the active-filter sentence list (built in 01-04-PLAN.md) ONLY when the current system's theory is non-empty; it is omitted entirely (no empty <section>) when theory is ''."
    - "Theory text is rendered in both the sidebar textarea and the workspace read-only panel via Jinja's default auto-escaping only — no |safe filter is applied anywhere to theory text, per T-01-01."
    - statement: "Theory text has no server-side length/encoding limit this phase; multi-byte/unicode theory text (emoji, non-Latin scripts) round-trips through the JSON (UTF-8) storage and Jinja auto-escaping without truncation or mojibake, and the workspace panel wraps long text via white-space: normal; overflow-wrap: anywhere with no horizontal overflow — verify manually by saving a theory string containing non-ASCII characters and a theory string over 280 characters, then reloading."
      verification: backstop
  artifacts:
    - cfb_system_maker/models.py (SavedSystem.theory)
    - cfb_system_maker/storage.py (load_saved_system, save_system theory param)
    - cfb_system_maker/web.py (_form_values/_form_values_from_post/_form_from_system theory plumbing, index()/save() routes)
    - cfb_system_maker/templates/index.html (sidebar textarea, workspace theory-panel)
    - cfb_system_maker/static/styles.css (.theory-field, .theory-panel)
  key_links:
    - "index() route: when a system is loaded, storage.load_saved_system(loaded_name, data_dir) replaces storage.load_system(...) as the read path, and its .theory is threaded into _form_from_system(system, loaded_name, saved.theory) -> form['theory'] -> both the sidebar textarea value and the workspace theory-panel's displayed text."
    - "save() route: request.form.get('theory', '') (already collected into _form_values_from_post()'s returned dict) is passed as save_system(..., theory=form.get('theory', '')) -> storage._system_to_dict persists it."
    - "The workspace theory-panel (this plan) is inserted immediately ABOVE the .active-filters-panel section built in 01-04-PLAN.md, satisfying EDIT-05's 'shown above its filter list' requirement text — this is distinct from, and in addition to, the sidebar textarea's location mandated by CONTEXT.md's D-02."
---

<objective>
Add a persisted, backward-compatible free-text "theory" field to `SavedSystem` (EDIT-05): an editable textarea in the sidebar form (per CONTEXT.md's locked D-02 — bundled into the existing form, saved via the existing `POST /save`) plus a read-only display panel in the workspace positioned directly above the active-filter sentence list built in `01-04-PLAN.md` (satisfying EDIT-05's literal "shown above its filter list" requirement text, which in `bet-labs-parity-plan.md`'s own vocabulary refers to the filter list, not the sidebar form).

Purpose: last of the five EDIT-0x requirements; depends on Plan 04's `.active-filters-panel` markup existing so the theory panel can be inserted directly above it.
Output: `SavedSystem.theory`; `storage.load_saved_system()`; theory round-trip through `web.py`'s form helpers and the `save()`/`index()` routes; sidebar textarea + workspace theory-panel in `templates/index.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-editor-main-page/01-CONTEXT.md
@.planning/phases/01-system-editor-main-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SavedSystem.theory + backward-compatible storage persistence</name>
  <files>cfb_system_maker/models.py, cfb_system_maker/storage.py, tests/test_storage_systems.py</files>
  <read_first>
    cfb_system_maker/models.py (SavedSystem, lines 109-113 — exact current field list)
    cfb_system_maker/storage.py (save_system lines 79-88, load_system lines 91-94, _system_to_dict lines 104-136, _system_from_dict lines 139-168)
    tests/test_storage_systems.py (existing test_save_load_list_system_round_trip — the exact assertion style to mirror; confirms load_system's current bare-SystemFilter contract that must stay unchanged)
    .planning/phases/01-system-editor-main-page/01-RESEARCH.md (Pitfall 1: do not change load_system's signature/return type — add a new function instead; Pitfall 2: always payload.get("theory", "") never payload["theory"])
  </read_first>
  <behavior>
    - Test 1 (round-trip): save_system("with-theory", SystemFilter(side="away"), tmp_path, theory="Fade elite teams late season.") then load_saved_system("with-theory", tmp_path).theory == "Fade elite teams late season.".
    - Test 2 (default on save): save_system("no-theory", SystemFilter(), tmp_path) (theory param omitted) then load_saved_system("no-theory", tmp_path).theory == "".
    - Test 3 (backward compatibility): manually write tmp_path/systems/legacy.json containing a valid saved-system payload with NO "theory" key at all (simulating a system saved before this phase); load_saved_system("legacy", tmp_path).theory == "" (no KeyError).
    - Test 4 (regression): the existing test_save_load_list_system_round_trip test continues to pass unmodified — load_system(...) still returns a bare SystemFilter with the same attributes it always has.
  </behavior>
  <action>
    In cfb_system_maker/models.py's SavedSystem dataclass, append `theory: str = ""` as a new trailing field after the existing `system: SystemFilter` field (SavedSystem's existing fields — name, saved_at, system — are all non-default, so this is a safe append with no reordering).
    In cfb_system_maker/storage.py's `save_system()`: add a `theory: str = ""` keyword parameter to its signature, and thread `theory=theory` into the `SavedSystem(...)` construction inside it (alongside the existing `name=`/`saved_at=`/`system=` kwargs). Existing callers that omit `theory` (e.g. cli.py) keep working via the default.
    In `_system_to_dict()`: add `"theory": saved.theory,` as a new top-level key in the returned dict, alongside the existing `"name"`/`"saved_at"`/`"system"` keys (not nested inside the `"system"` sub-dict).
    Do NOT modify `load_system()` or `_system_from_dict()`'s existing behavior/signature in any way — `_system_from_dict()` continues to return a bare `SystemFilter` with no knowledge of theory.
    Add a new function `load_saved_system(name: str, data_dir: str | Path) -> SavedSystem`: read the same `data/systems/{name}.json` path `load_system()` reads, parse the same JSON payload, and construct `SavedSystem(name=str(payload.get("name", name)), saved_at=str(payload.get("saved_at", "")), system=_system_from_dict(payload), theory=str(payload.get("theory", "")))` — reusing the existing `_system_from_dict(payload)` call for the `system` field rather than duplicating its logic.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_storage_systems.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "theory: str = \"\"" cfb_system_maker/models.py` returns exactly one match, inside SavedSystem.
    - `grep -n "def load_saved_system" cfb_system_maker/storage.py` returns exactly one match.
    - `grep -n "def load_system" cfb_system_maker/storage.py` still shows the original `(name: str, data_dir: str | Path) -> SystemFilter` signature, unchanged.
    - `.venv/Scripts/python.exe -m pytest tests/test_storage_systems.py -q` passes with 0 failures, including the 3 new tests plus the unmodified pre-existing test.
  </acceptance_criteria>
  <done>SavedSystem.theory exists with a "" default; save_system/load_saved_system persist and read it backward-compatibly; load_system's contract is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire theory through web.py's form round-trip and render both the sidebar field and workspace panel</name>
  <files>cfb_system_maker/web.py, cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/web.py (current state after Plans 01/02/04's edits — read fresh; specifically `_form_values()` lines 215-236, `_form_values_from_post()` lines 239-260, `_form_from_system()` lines 274-302 and its one call site inside `index()` around line 51, the `index()` route's loaded-system branch lines 46-59, the `save()` route lines 83-90, and the storage import line)
    cfb_system_maker/templates/index.html (current state after Plans 01/02/04's edits — read fresh; specifically the `.form-actions` div and its preceding `{% endif %}` that closes the `features_enabled` conditional (existing lines 183-191), and the `.active-filters-panel` section built in 01-04-PLAN.md, to find the exact insertion point directly above it)
    .planning/phases/01-system-editor-main-page/01-CONTEXT.md (D-02: theory textarea bundled into the existing GET filter form next to Save Name/Save System, saved via existing POST /save)
    .planning/phases/01-system-editor-main-page/01-UI-SPEC.md (Copywriting Contract: theory placeholder text verbatim "Add the theory behind your system."; theory field label "Theory"; long-text backstop: white-space: normal; overflow-wrap: anywhere)
  </read_first>
  <action>
    In web.py's storage import line, add `load_saved_system` alongside the existing `list_systems, load_processed_games, load_system, save_system` imports.
    In `_form_values()` (the GET-path helper), add `"theory": ""` to its returned dict, mirroring the existing `"save_name": ""` entry — theory is never populated from `request.args` on a fresh GET, matching how save_name already behaves.
    In `_form_values_from_post()` (the POST-path helper), add `"theory": request.form.get("theory", "")` to its returned dict.
    Change `_form_from_system(system: SystemFilter, loaded_name: str)`'s signature to `_form_from_system(system: SystemFilter, loaded_name: str, theory: str = "")`, and add `"theory": theory,` to its returned dict.
    In `index()`'s loaded-system branch (the `if loaded_name:` block): replace `system = load_system(loaded_name, app.config["DATA_DIR"])` / `form = _form_from_system(system, loaded_name)` with `saved = load_saved_system(loaded_name, app.config["DATA_DIR"])`, `system = saved.system`, `form = _form_from_system(system, loaded_name, saved.theory)` — keep the surrounding `try/except FileNotFoundError` block and its fallback behavior (blank form, load_error message) exactly as-is.
    In `save()`: change `save_system(name, _system_from_form(form), app.config["DATA_DIR"])` to `save_system(name, _system_from_form(form), app.config["DATA_DIR"], theory=form.get("theory", ""))`.
    In templates/index.html's sidebar: insert `<label class="theory-field full-width">Theory<textarea name="theory" placeholder="Add the theory behind your system.">{{ form.theory }}</textarea></label>` immediately before the existing `<div class="form-actions">` opening tag (i.e. right after the `{% endif %}` that closes the `features_enabled`/`feature-disabled` conditional).
    In templates/index.html's workspace: insert `{% if form.theory %}<section class="theory-panel"><h3>Theory</h3><p>{{ form.theory }}</p></section>{% endif %}` immediately ABOVE (i.e. directly before, in document order) the `<section class="active-filters-panel">` section built in 01-04-PLAN.md. `{{ form.theory }}` in both locations renders via Jinja's default auto-escaping — do not add `|safe` to either.
    In cfb_system_maker/static/styles.css, add `.theory-field textarea` (min-height: 80px, matching the existing `input`/`select` min-height convention scaled up for a textarea, resize: vertical, font: inherit) and `.theory-panel` (background var(--panel), border 1px solid var(--border), border-radius 6px, padding 16px — the md spacing token, margin-bottom 18px, white-space: normal, overflow-wrap: anywhere — the long-text backstop mitigation) and `.theory-panel h3` (margin: 0 0 8px, font-size: 14px, matching the existing `.range-chart h3` convention).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A new integration test `test_web_save_and_load_system_preserves_theory` POSTs to `/save` with `"theory": "Fade the public."` alongside existing save fields, then GETs `/?load_system=<that-name>`, and asserts both `'name="theory"' in html` and `"Fade the public." in html` are true.
    - A new integration test confirms a FRESH GET `/` (no `?load_system=`) does NOT render a `.theory-panel` section (theory is empty by default) — e.g. assert `"theory-panel" not in html` or equivalent.
    - `grep -n "|safe" cfb_system_maker/templates/index.html` shows no match anywhere near the theory textarea or theory-panel additions (grep the whole file — the project has no pre-existing `|safe` usage to conflict with).
    - `.venv/Scripts/python.exe -m pytest -q` (full suite) passes.
  </acceptance_criteria>
  <done>Theory can be entered in the sidebar, saved via the existing Save System button, and on reload appears both in the sidebar textarea and in a read-only panel directly above the active-filter sentence list; empty theory shows neither panel content nor a stray empty section.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `SavedSystem.theory: str = ""` — new dataclass field, `cfb_system_maker/models.py`.
- `storage.load_saved_system(name: str, data_dir: str | Path) -> SavedSystem` — new function, `cfb_system_maker/storage.py`.
- `storage.save_system(name, system, data_dir, theory: str = "")` — modified signature (new keyword param), `cfb_system_maker/storage.py`.
- `_form_from_system(system, loaded_name, theory: str = "")` — modified signature (new keyword param), `cfb_system_maker/web.py`.
- `"theory"` key in `_form_values()` / `_form_values_from_post()`'s returned dicts — `cfb_system_maker/web.py`.
- Sidebar `<textarea name="theory">` field and workspace `.theory-panel` section — `cfb_system_maker/templates/index.html`.
- `.theory-field textarea`, `.theory-panel`, `.theory-panel h3` — new CSS classes, `cfb_system_maker/static/styles.css`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Free-text theory input -> stored JSON -> re-rendered HTML | User-supplied text now persists to disk and is re-rendered on every subsequent page load for that saved system — the canonical stored-XSS surface this phase introduces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering / Information Disclosure | Theory textarea (sidebar) + theory-panel (workspace) in templates/index.html | high | mitigate | Both render locations use bare Jinja `{{ form.theory }}` expressions, relying on Flask/Jinja2's default auto-escaping (confirmed ON, no `{% autoescape false %}` anywhere in this template). Neither location applies `\|safe`. Task 2's acceptance criteria include an explicit `grep -n "\|safe"` check to prevent regression. |
| T-01-02 | Tampering | Query-string href construction (not touched by this plan) | high | accept (N/A here) | This plan adds no new query-href construction — it reuses the existing `_form_values`/`_form_values_from_post` request-argument reading pattern, not href-building. Owned by Plans 02/04. |

</threat_model>

<verification>
`.venv/Scripts/python.exe -m pytest tests/test_storage_systems.py tests/test_web.py -q` then full suite `.venv/Scripts/python.exe -m pytest -q`.
</verification>

<success_criteria>
- Theory can be written, saved, and reloaded; pre-existing saved-system files without a theory key load with theory="".
- Theory renders in both the sidebar (editable) and workspace (read-only, above the filter list) locations, escaped, never via `|safe`.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/01-system-editor-main-page/01-05-SUMMARY.md` when done
</output>
