---
phase: 01-system-editor-main-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cfb_system_maker/models.py
  - cfb_system_maker/backtest.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - tests/test_backtest.py
  - tests/test_web.py
autonomous: true
requirements: [EDIT-02]

must_haves:
  truths:
    - "For a system with bet_type=='spread', BacktestResult.average_margin equals round(sum(bet.margin for bet in bet_details) / bets, 4), where each bet.margin is the signed cover margin (team_points + side_spread - opponent_points) computed in grade_bet's spread path."
    - "For a system with bet_type=='total', BacktestResult.average_margin is None and every BetDetail.margin defaults to 0.0 (no margin formula is invented for totals), per CONTEXT.md's discretion decision."
    - "The stat-chip header renders exactly 5 chips in this order: Record, Margin, Money Won, ROI, Grade — replacing the prior 6-chip Bets/ROI/Profit/Hit Rate/W-L-P/Avg Line row."
    - "Record chip value is formatted as '{wins}-{losses}-{pushes}, {hit_rate to 1 decimal}%' (e.g. '2-0-0, 100.0%'), matching the Bet Labs reference format '177-137-12, 56.4%'."
    - "Money Won chip value is formatted as a signed, comma-grouped whole-dollar amount with a '$' sign (e.g. '+$3,114' or '-$450'), computed from result.profit * 100 at template-render time only — result.profit itself is never multiplied by 100 in Python. A zero result.profit (e.g. a zero-bet or all-push system) renders the unsigned '$0', never the misleading '+$0'/'-$0' (01-REVIEWS.md's Plan 01 optional zero-money-won suggestion, adopted)."
    - "Grade chip always renders the literal placeholder text '—' (em dash) this phase, in the neutral (non-accent, non-loss) color, for every system regardless of bet_type or result — no heuristic or computed value is substituted."
    - statement: "Money Won and ROI chips apply the 'positive' class (accent green) when the value is > 0 and 'negative' class (loss red) when < 0, reusing the existing .positive/.negative CSS rules already defined in styles.css; Record and Grade chips never receive either class."
      verification: backstop
  artifacts:
    - cfb_system_maker/models.py
    - cfb_system_maker/backtest.py
    - cfb_system_maker/templates/index.html
    - cfb_system_maker/static/styles.css
  key_links:
    - "grade_bet()'s spread branch threads cover_margin into BetDetail.margin -> run_backtest() aggregates it into BacktestResult.average_margin -> templates/index.html's Margin chip reads result.average_margin directly (no new web.py plumbing needed since result is already passed to render_template)."
  assumptions:
    - "EDIT-02 ('User sees a stat-chip header...') classified to zero edge-probe shape cues (unclassified — prose didn't match numeric-range/collection/text/stateful/io cues), so no auto-covered/backstop edge was generated for it. Flagged for manual review, not auto-resolved: the chip header's behavior for degenerate results (zero bets matched, all-push system, negative Margin) is NOT separately probed here. Resolution adopted: Grade always renders the '—' placeholder regardless of result shape (no branch needed); Margin/Money Won/ROI already degrade gracefully for a zero-bet system because result.average_margin/result.profit/result.roi already default to None/0.0/0.0 in existing backtest.py code, unchanged by this plan — verify this holds during Task 1's automated tests rather than assuming it."
  prohibitions:
    - statement: "Do NOT compute or display a fabricated or heuristic Grade value (e.g. a placeholder letter derived from ROI sign or hit rate) in the Grade chip this phase. Render only the literal neutral placeholder text '—'. Real Grade computation is explicitly out of scope until Phase 2 (INTG-02) per ROADMAP.md and CONTEXT.md's discretion decision."
      status: unverified
      verification: backstop
---

<objective>
Restyle the plain metrics row in `cfb_system_maker`'s system editor page into a 5-chip Bet Labs-style stat header — Record, Margin, Money Won, ROI, Grade — per EDIT-02 (D-02/D-01 are not implicated here; this plan implements the Grade-chip-placeholder and Margin-for-totals discretion decisions recorded in `.planning/phases/01-system-editor-main-page/01-CONTEXT.md`). Adds a new `margin` field to `BetDetail` and `average_margin` to `BacktestResult`, computed only for spread-bet systems.

Purpose: EDIT-02 is the primary visual-hierarchy anchor of the whole phase (per `01-UI-SPEC.md`, the chip header is what a returning user checks first) and has no dependency on the tabs/graph/sentences/theory work in the other plans, so it ships first and independently.
Output: `BetDetail.margin`, `BacktestResult.average_margin` computed in `backtest.py`; restyled `.metrics` chip block in `templates/index.html`; new scoped `.metrics.stat-chips` CSS grid rule in `static/styles.css` (the shared `.metrics` rule itself is left untouched).

Review incorporation (`01-REVIEWS.md`, Codex): finding #6 (medium) — `.metrics`/`.metrics.stats-panel` share one CSS class, so a naive `.metrics` grid change would leak into the untouched stats panel; Task 2 already scopes the fix via a new `.metrics.stat-chips` class rather than editing `.metrics` directly (this plan's "Artifacts This Phase Produces" section is corrected below to match). Optional suggestion — zero Money Won should render unsigned `$0`, not `+$0`/`-$0`; adopted in Task 2's Money Won formatting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-editor-main-page/01-CONTEXT.md
@.planning/phases/01-system-editor-main-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Margin computation to BetDetail/BacktestResult (spread bets only)</name>
  <files>cfb_system_maker/models.py, cfb_system_maker/backtest.py, tests/test_backtest.py</files>
  <read_first>
    cfb_system_maker/models.py (BetDetail lines 67-79, BacktestResult lines 93-106 — exact current field order and existing trailing defaults)
    cfb_system_maker/backtest.py (grade_bet lines 197-251, _grade_total_bet lines 254-294, run_backtest lines 12-50)
    tests/test_backtest.py (existing _bet() helper at line 134, test_home_favorite_cover_wins_at_minus_110 at line 5, test_over_under_bets_grade_against_total_points at line 85)
    .planning/phases/01-system-editor-main-page/01-CONTEXT.md (Claude's Discretion section: "Margin chip for total-bet systems" — Margin is scoped to bet_type=='spread' only, '—' placeholder for totals)
  </read_first>
  <behavior>
    - Test 1 (extend test_home_favorite_cover_wins_at_minus_110): the existing game (home_points=30, spread=-14.5, away_points=14, side='home') produces bet_details[0].margin == 1.5 (30 + (-14.5) - 14) and result.average_margin == 1.5.
    - Test 2 (new test_average_margin_is_none_for_total_bet_systems): reuse the two games from test_over_under_bets_grade_against_total_points; run_backtest with SystemFilter(bet_type='total', total_side='over'); assert result.average_margin is None and every bet_details[i].margin == 0.0 (the BetDetail default, since _grade_total_bet never sets margin).
    - Test 3 (new test_average_margin_averages_across_multiple_spread_bets): two spread games with different cover margins (e.g. margins 1.5 and -3.0 for a home-side system); assert result.average_margin == round((1.5 + -3.0) / 2, 4).
    - Test 4 (regression): the existing _bet() helper at tests/test_backtest.py:134 constructs BetDetail(...) without a margin= kwarg — must continue to work unmodified because margin gets a default value.
  </behavior>
  <action>
    In cfb_system_maker/models.py: append `margin: float = 0.0` as a new trailing field on `BetDetail` (after the existing `profit: float` field — `profit` and all fields before it stay non-default, `margin` is the first/only defaulted field so no reordering is needed). Append `average_margin: float | None = None` as a new trailing field on `BacktestResult` (after the existing `season_breakdown: tuple[SeasonRecord, ...] = ()` field, which is already the last/defaulted field).
    In cfb_system_maker/backtest.py's grade_bet() spread path (the BetDetail(...) construction that currently ends at `profit=round(profit, 4),`): add `margin=round(cover_margin, 4),` as a new kwarg, reusing the `cover_margin` local variable already computed two lines above (`cover_margin = team_points + spread - opponent_points`) — do not recompute it from a stored field later.
    In cfb_system_maker/backtest.py's _grade_total_bet(): make NO change to its BetDetail(...) construction — leave it without a margin= kwarg so it uses the 0.0 default. This is the explicit CONTEXT.md discretion decision: no margin formula is invented for total bets.
    In cfb_system_maker/backtest.py's run_backtest(): add `average_margin=(round(sum(bet.margin for bet in details) / bets, 4) if bets and system.bet_type == "spread" else None),` as a new kwarg to the BacktestResult(...) constructor call, placed after the existing `season_breakdown=...` kwarg. The `system.bet_type == "spread"` guard is load-bearing — it is what keeps average_margin at None for total-bet systems even though every individual bet.margin defaults to 0.0 (0.0 is not the same signal as "not applicable"; None is what the template checks).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_backtest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "margin: float = 0.0" cfb_system_maker/models.py` returns exactly one match, inside the BetDetail class body.
    - `grep -n "average_margin: float | None = None" cfb_system_maker/models.py` returns exactly one match, inside the BacktestResult class body.
    - `.venv/Scripts/python.exe -m pytest tests/test_backtest.py -q` passes with 0 failures, including the 3 new/extended assertions from the behavior block above.
    - `_grade_total_bet`'s BetDetail(...) call in cfb_system_maker/backtest.py contains no `margin=` kwarg (source assertion — grep the function body).
  </acceptance_criteria>
  <done>BetDetail.margin and BacktestResult.average_margin exist with the documented defaults; margin is populated for spread bets and stays 0.0/None for total bets; full test_backtest.py suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the stat-chip header to Record/Margin/Money Won/ROI/Grade</name>
  <files>cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, tests/test_web.py</files>
  <read_first>
    cfb_system_maker/templates/index.html (the .metrics block, lines 225-232, and the untouched .metrics.stats-panel block immediately after it, lines 234-245 — do not modify the stats-panel)
    cfb_system_maker/static/styles.css (lines 242-266 .metrics/.metrics article/.metrics span/.metrics strong; lines 340-346 .positive/.negative; line 407 responsive .metrics breakpoint)
    tests/test_web.py (test_web_index_loads_filters_and_default_results line 7-19 and test_web_filters_apply_to_results line 22-35 — both currently assert `"Bets" in html`, which will break once the "Bets" chip label is removed)
    .planning/phases/01-system-editor-main-page/01-UI-SPEC.md (Color section: Record/Grade are neutral, never accent/loss-colored; Typography section: Display 22px/700 reused for all 5 chip values; Spacing exceptions note on pre-existing .metrics deviations — do not refactor unrelated spacing)
  </read_first>
  <action>
    Change the primary block to `<section class="metrics stat-chips" aria-label="Backtest metrics">` and replace its 6 `<article>` children (Bets, ROI, Profit, Hit Rate, W-L-P, Avg Line) with exactly 5 in this order: Record, Margin, Money Won, ROI, Grade. Do not touch the sibling `.metrics.stats-panel` markup or layout, `.season-breakdown`, or `.coverage`.
    Record: `<span>Record</span>` label; value markup `{{ result.wins }}-{{ result.losses }}-{{ result.pushes }}, {{ "%.1f"|format(result.hit_rate * 100) }}%` inside `<strong>` with no positive/negative class (neutral).
    Margin: `<span>Margin</span>` label; value `{% if result.average_margin is not none %}{{ "%.1f"|format(result.average_margin) }}{% else %}&mdash;{% endif %}` inside `<strong class="{{ 'positive' if result.average_margin and result.average_margin > 0 else 'negative' if result.average_margin and result.average_margin < 0 else '' }}">`.
    Money Won: `<span>Money Won</span>` label; value computed entirely in the template (no web.py change) as: `{% if result.profit > 0 %}+${{ "{:,.0f}".format(result.profit * 100) }}{% elif result.profit < 0 %}-${{ "{:,.0f}".format(-result.profit * 100) }}{% else %}$0{% endif %}` inside `<strong class="{{ 'positive' if result.profit > 0 else 'negative' if result.profit < 0 else '' }}">`. A zero-profit result renders the unsigned `$0` rather than a misleading `+$0`/`-$0` (01-REVIEWS.md's Plan 01 optional suggestion, adopted). The `result.profit` Python value itself is never multiplied by 100 anywhere outside this template expression.
    ROI: keep the existing markup exactly as-is (`<span>ROI</span><strong class="{{ 'positive' if result.roi > 0 else 'negative' if result.roi < 0 else '' }}">{{ "%.2f%%"|format(result.roi * 100) }}</strong>`), just reposition it as the 4th article.
    Grade: `<span>Grade</span><strong>&mdash;</strong>` — literal placeholder, no conditional, no color class.
    In cfb_system_maker/static/styles.css, leave the shared `.metrics { grid-template-columns: repeat(6, ...) }` rule unchanged because `.metrics.stats-panel` also uses it. Add `.metrics.stat-chips { grid-template-columns: repeat(5, minmax(120px, 1fr)); }`. This scopes the five-column change to the primary header and honors the instruction not to alter the quality-stat panel indirectly.
    In tests/test_web.py, replace the two weak `assert "Bets" in html` checks with assertions scoped to the `aria-label="Backtest metrics"` section. Verify exactly five articles, exact label order, Record formatting, Money Won dollar formatting, and Grade placeholder. Add a total-system case proving Margin renders an em dash, positive and negative cases proving sign classes and currency output, and a zero-profit case (e.g. an all-push system, or a system matching zero games) proving Money Won renders the unsigned `$0` rather than `+$0`/`-$0` (01-REVIEWS.md's Plan 01 zero-money-won suggestion). Use existing parsing dependencies only; if no HTML parser is installed, isolate the section substring and inspect it without adding a package.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<article>" cfb_system_maker/templates/index.html` within the `.metrics` (non-stats-panel) block shows exactly 5 article elements for that section (verify by reading the section, not a blind file-wide count since `.metrics.stats-panel` also uses `<article>`).
    - `grep -n "Record\|Margin\|Money Won\|Grade" cfb_system_maker/templates/index.html` shows all four new labels present.
    - `grep -n "repeat(5, minmax" cfb_system_maker/static/styles.css` returns a match.
    - `.venv/Scripts/python.exe -m pytest tests/test_web.py -q` passes with 0 failures.
    - `.venv/Scripts/python.exe -m pytest -q` (full suite) passes, confirming no other test file references the removed "Bets"/"Profit"/"Hit Rate"/"W-L-P"/"Avg Line" chip labels.
  </acceptance_criteria>
  <done>The workspace header shows exactly 5 chips (Record, Margin, Money Won, ROI, Grade) in that order, Grade always shows "—", Margin shows "—" for total-bet systems, and the full test suite is green.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `BetDetail.margin: float = 0.0` — new dataclass field, `cfb_system_maker/models.py`.
- `BacktestResult.average_margin: float | None = None` — new dataclass field, `cfb_system_maker/models.py`.
- Restyled `.metrics.stat-chips` header markup (Record/Margin/Money Won/ROI/Grade) — `cfb_system_maker/templates/index.html`, reuses existing `.positive`/`.negative` classes for signed chips.
- `.metrics.stat-chips { grid-template-columns: repeat(5, minmax(120px, 1fr)); }` — new scoped CSS rule, `cfb_system_maker/static/styles.css`. The pre-existing `.metrics { grid-template-columns: repeat(6, ...) }` rule is left unmodified so it continues to serve `.metrics.stats-panel` unaffected (01-REVIEWS.md finding #6 — the two sections share the base `.metrics` class and must not both be forced onto the new 5-column layout).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new in this plan | This plan touches only computed numeric fields (margin) and static template markup; no new user-input surface or query-string construction is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering / Information Disclosure | Theory field rendering (not touched by this plan) | high | accept (N/A here) | No new trust boundary in this plan — theory field lands in Plan 05 (01-05-PLAN.md), which owns this threat's mitigation. |
| T-01-02 | Tampering | Query-string href construction (not touched by this plan) | high | accept (N/A here) | No query-href construction in this plan — Plans 02 and 04 introduce `_query_href`/`_query_href_removing` and own this threat's mitigation. |

</threat_model>

<verification>
Full suite: `.venv/Scripts/python.exe -m pytest -q` must pass (baseline 94 passed + this plan's new assertions). No new dependencies introduced; no changes to storage.py or web.py.
</verification>

<success_criteria>
- BetDetail.margin and BacktestResult.average_margin exist, computed correctly for spread bets and left at 0.0/None for total bets.
- The workspace's stat-chip header shows exactly 5 chips (Record, Margin, Money Won, ROI, Grade) matching the documented format strings.
- `.venv/Scripts/python.exe -m pytest -q` is green.
</success_criteria>

<output>
Create `.planning/phases/01-system-editor-main-page/01-01-SUMMARY.md` when done
</output>
