---
phase: 04-filter-popup-modal
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - cfb_system_maker/describe.py
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - cfb_system_maker/static/filter_modal.js
  - tests/test_filter_modal.py
  - tests/test_describe.py
autonomous: true
requirements: [MODAL-03, MODAL-06]

must_haves:
  truths:
    - "Numeric filters render synchronized dual native range handles plus BETWEEN [min] AND [max] number inputs; Save stays disabled while bounds are invalid or reversed."
    - "New numeric filters open at observed domain bounds; editing opens at committed bounds; Save Filter writes paired FeatureFilter gte+lte or canonical min/max range controls into filters-form and submits GET; Cancel leaves those controls unchanged (D-04 / D-07)."
    - "Numeric default view is a per-value money chart (SVG); Chart/List toggle switches to the exact-value performance table without changing selected bounds or live metrics."
    - "Core spread distribution uses side-relative spread via _side_spread; total uses GameRecord.total."
    - "describe() coalesces same-key numeric gte+lte into one BETWEEN sentence with one edit identity."
    - statement: "Chart downsampling is visual only and must not alter selected bounds or live header metrics."
      verification: backstop
    - statement: "Empty domains and no-match candidate ranges show neutral chips and No values in range / No bets match this draft copy."
      verification: backstop
  artifacts:
    - path: cfb_system_maker/describe.py
      provides: "Coalesced numeric feature BETWEEN sentences + single edit key"
    - path: cfb_system_maker/web.py
      provides: "Numeric /filter-detail rows + deterministic chart_points cap"
    - path: cfb_system_maker/static/filter_modal.js
      provides: "Dual-range sync, Chart/List toggle, SVG money chart"
  key_links:
    - from: "filter_modal.js dual-range draft"
      to: "/api/backtest"
      via: "candidate gte+lte or min/max spread/total substituted into query"
      pattern: "/api/backtest"
    - from: "/filter-detail numeric"
      to: "aggregate_filter_value_rows"
      via: "exact observed values; chart_points may downsample"
      pattern: "chart_points"
    - from: "filter_modal.js Save Filter (numeric)"
      to: "filters-form"
      via: "valid draft writes paired gte+lte FeatureFilters or canonical min/max range controls then form.submit GET; Cancel leaves controls unchanged"
      pattern: "filters-form|form\\.submit|Save Filter"
---

## Phase Goal

**As a** system builder, **I want to** open any filter in a live Bet Labs-style popup with performance evidence and About text before I commit, **so that** I can explore values without changing my system until I explicitly Save.

## Artifacts this phase produces

| Symbol | Module | Introduced in |
|--------|--------|---------------|
| `describe()` gte+lte coalesce | `describe.py` | 04-03 |
| Numeric `chart_points` on `/filter-detail` | `web.py` | 04-03 |
| Dual-range + BETWEEN UI | `filter_modal.js` | 04-03 |
| Chart/List segmented control | `filter_modal.js` / `index.html` | 04-03 |
| SVG money-by-value renderer | `filter_modal.js` | 04-03 |
| (carries forward) launchers, `/filter-detail` rows, dialog shell | from 04-01/02 | — |

<objective>
Deliver numeric range exploration: dual handles, BETWEEN inputs, per-value money chart/list, paired FeatureFilter persistence, and coalesced sentences (D-06–D-10, D-07 model reuse).

Purpose: Complete MODAL-03 and the numeric Save path for MODAL-06.
Output: describe coalesce, numeric detail/chart payloads, dual-range UI synchronized with live chips.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-filter-popup-modal/04-CONTEXT.md
@.planning/phases/04-filter-popup-modal/04-RESEARCH.md
@.planning/phases/04-filter-popup-modal/04-UI-SPEC.md
@.planning/phases/04-filter-popup-modal/04-PATTERNS.md
@.planning/phases/04-filter-popup-modal/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paired numeric filters, describe coalesce, numeric detail/chart payload</name>
  <files>cfb_system_maker/describe.py, cfb_system_maker/web.py, tests/test_describe.py, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-06, D-07, D-08, D-09)
    .planning/phases/04-filter-popup-modal/04-RESEARCH.md (Pattern 5 Numeric Range as Existing Filters; Core Value Resolution; chart downsample)
    .planning/phases/04-filter-popup-modal/04-PATTERNS.md (describe.py coalesce pattern; _range_chart downsample precedent)
    cfb_system_maker/describe.py (feature loop currently emits separate gte and lte sentences)
    cfb_system_maker/web.py (remove_candidate_filters, aggregate_filter_value_rows, _side_spread usage needs)
    cfb_system_maker/backtest.py (_side_spread)
    tests/test_describe.py (core BETWEEN examples)
  </read_first>
  <behavior>
    - describe() with same-key numeric gte+lte (same perspective) yields one BETWEEN sentence and one key ff:{key} (or namespaced id consistent with remove/edit).
    - /filter-detail for feature:weather_temperature and core:spread_range returns domain.min/max from exact observed values and rows for each exact value.
    - chart_points length is capped deterministically (planner lock: max 60 points, same stride style as _range_chart) while domain bounds remain exact unsampled min/max.
    - Spread rows use side-relative spreads; total rows use GameRecord.total.
    - remove_candidate_filters clears both gte and lte for a feature key and both min/max for core ranges.
  </behavior>
  <action>
    Update describe() to group feature_filters by (key, perspective), coalesce numeric gte+lte into one BETWEEN (or exactly / at least / at most when one bound missing) sentence, and emit a single edit identity. Keep bool/categorical wording. Extend tests/test_describe.py.

    Extend /filter-detail numeric path: populate rows from exact observed values; set chart_points to a deterministic downsample for rendering only (cap 60); never evaluate live range against sampled points. Ensure core:spread_range resolves via _side_spread (import/reuse from backtest — do not duplicate sign logic in web.py). Wire serialization helpers that represent a numeric draft as either min_spread/max_spread, min_total/max_total, or a replaced FeatureFilter pair (gte, lte) with shared perspective — atomic replace of all same-key rows (D-07).

    Tests: coalesce; spread sign; chart_points cap with intact domain; candidate removal clears both bounds.
  </action>
  <verify>
    <automated>python -m pytest tests/test_describe.py tests/test_filter_modal.py -x -k "between or numeric or spread_range or chart_points or coalesce"</automated>
  </verify>
  <acceptance_criteria>
    - test_describe covers paired gte+lte → one sentence.
    - /filter-detail numeric JSON includes domain.min, domain.max, rows, chart_points.
    - A high-cardinality fixture with more than 60 unique values returns len(chart_points) less than or equal to 60 while domain.min/max match the unsampled extremes.
    - Focused pytest command above passes.
  </acceptance_criteria>
  <done>Numeric server contracts and coalesced sentences are green.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-range UI, Chart/List toggle, SVG money chart</name>
  <files>cfb_system_maker/static/filter_modal.js, cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-UI-SPEC.md (Numeric Filters contract; BETWEEN copy; Chart/List; spacing for dual-range hit targets)
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-06, D-07, D-09, D-10, D-23)
    cfb_system_maker/static/filter_modal.js (categorical path from 04-02)
    cfb_system_maker/templates/index.html (dialog shell regions)
    cfb_system_maker/static/styles.css (.range-chart svg height 150px precedent)
  </read_first>
  <action>
    For numeric candidates in filter_modal.js: render two overlaid native input[type=range] handles plus BETWEEN [min] AND [max] number inputs; keep both directions in sync; prevent crossing by clamping; disable Save Filter while invalid/reversed and show optional hint Max must be greater than or equal to min (D-06 / UI-SPEC).

    Open defaults: new filter → full observed domain from /filter-detail; edit/prefilled → committed bounds (edit wiring finalized in 04-04, but accept data-min/data-max or draft seed attrs now) (D-07).

    Default view Chart: draw SVG dots from chart_points using existing .positive/.negative and zero-line strokes; height 150px. Toggle Chart/List (segmented control copy exact); List shows exact rows table (same columns as categorical) without changing bounds or retriggering a different metric authority (D-09). Live header continues to call /api/backtest with the draft range applied.

    Empty domain: show No values in range + body copy from UI-SPEC; chips 0-0-0 / $0 / 0%. No-match draft: No bets match this draft. with neutral chips (D-10).

    CSS: dual-range track 4px / thumb 16px with ≥36px hit area; selected span uses accent; no new chart library (D-23).

    Numeric Save Filter commit path (MODAL-06 / D-04 / D-07): when bounds are valid, wire Save to the Task 1 serialization helpers so the draft is written into filters-form as either (a) canonical min_spread+max_spread or min_total+max_total for core range candidates, or (b) an atomic rewrite of same-key FeatureFilter gte+lte pairs (shared perspective) for feature:* numeric candidates; then call filters-form.submit() for the existing full-page GET. Cancel, dialog cancel, and close must leave all canonical range/ff_* controls unchanged (no partial writes). Do not leave Save as validation-only — helpers from Task 1 must be invoked from the Save click handler for numeric drafts.

    Tests: rendered/source contracts for two range inputs, BETWEEN/AND chrome, Chart/List controls, Save disabled when the lower bound exceeds the upper; plus a focused serialization/commit test that asserts a valid numeric draft Save writes the paired/canonical controls into a filters-form fixture and that Cancel leaves the form controls untouched.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x -k "range or chart or between or numeric or commit or serialize or save"</automated>
  </verify>
  <acceptance_criteria>
    - Dialog markup or JS templates include two input type=range elements and BETWEEN / AND labels.
    - Chart and List toggle strings exist exactly as Chart and List.
    - A focused test proves numeric Save writes paired gte+lte or canonical min/max controls into filters-form and triggers submit; Cancel leaves those controls unchanged.
    - python -m pytest tests/test_filter_modal.py -x passes.
    - python -m pytest tests/test_describe.py tests/test_web.py -q passes.
  </acceptance_criteria>
  <done>Numeric filters are explorable via dual-range + chart/list, and Save commits paired/canonical range constraints through filters-form GET.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser numeric draft → API | min/max floats and feature keys |
| Chart points → SVG | Server-provided numbers only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | numeric bounds | mitigate | Server rejects non-finite / reversed bounds on Save validation and API parse (D-06, D-18) |
| T-04-11 | Denial of Service | high-cardinality numeric domains | mitigate | One-pass aggregation; chart_points capped at 60; exact rows may lazy-render in List but metrics stay server-authoritative (D-09) |
| T-04-12 | Tampering | spread sign | mitigate | Reuse _side_spread only; never plot raw home spread for side-aware systems |
| T-04-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
python -m pytest tests/test_filter_modal.py tests/test_describe.py tests/test_web.py -x
</verification>

<success_criteria>
MODAL-03 satisfied: dual handles, BETWEEN inputs, per-value money chart/list; numeric Save path writes paired/canonical range constraints (MODAL-06).
</success_criteria>

<output>
Create `.planning/phases/04-filter-popup-modal/04-03-SUMMARY.md` when done
</output>
