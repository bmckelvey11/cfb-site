---
phase: 04-filter-popup-modal
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - cfb_system_maker/web.py
  - cfb_system_maker/describe.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - cfb_system_maker/static/filter_modal.js
  - tests/test_filter_modal.py
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-06]

must_haves:
  truths:
    - "Active-filter sentences expose an Edit control that opens the same modal prefilled; Remove remains a separate one-click action."
    - "Live chip requests debounce at about 250ms, abort prior fetches, ignore stale generations, keep last success visible with Updating…, and show inline Couldn’t update live stats. plus Retry on failure without closing or mutating the form."
    - "Save Filter enables only when the draft is locally valid and the latest live validation succeeded; Cancel / Escape / close discard; backdrop clicks do neither."
    - "Team-scoped features expose Bet-side / Opponent / Either; new spread filters default Bet-side; new total filters default Either; edits preserve committed perspective."
    - "Desktop layout is wide content + right About; narrow screens stack About below and keep actions reachable."
    - statement: "Keyboard users can open, operate sort/range controls, Escape-cancel, and regain focus on the launching control."
      verification: backstop
    - statement: "Stale response races cannot overwrite a newer draft or enable Save on outdated validation."
      verification: backstop
  artifacts:
    - path: cfb_system_maker/templates/index.html
      provides: "Edit controls on active-filter sentences"
    - path: cfb_system_maker/web.py
      provides: "edit metadata on describe rows (candidate_id, perspective, bounds/values)"
    - path: cfb_system_maker/static/filter_modal.js
      provides: "debounce, AbortController, generation guard, Retry, perspective control, focus lifecycle"
    - path: cfb_system_maker/static/styles.css
      provides: "Responsive modal stack ≤900px + focus-visible rules"
  key_links:
    - from: "active-filter Edit"
      to: "filter_modal.js open(prefill)"
      via: "data-candidate-id + committed draft attrs"
      pattern: "Edit filter"
    - from: "filter_modal.js"
      to: "/api/backtest"
      via: "250ms debounce + generation check before render/enable Save"
      pattern: "AbortController"
---

## Phase Goal

**As a** system builder, **I want to** open any filter in a live Bet Labs-style popup with performance evidence and About text before I commit, **so that** I can explore values without changing my system until I explicitly Save.

## Artifacts this phase produces

| Symbol | Module | Introduced in |
|--------|--------|---------------|
| Active-filter `Edit` control + `edit` metadata | `index.html` / `web.py` | 04-04 |
| Perspective segmented control | `filter_modal.js` | 04-04 |
| Debounced live refresh with generation guard | `filter_modal.js` | 04-04 |
| Retry + Updating… status line | `filter_modal.js` / UI-SPEC copy | 04-04 |
| Responsive About stack + focus-visible | `styles.css` | 04-04 |
| (phase complete) full MODAL-01…06 surface | — | 04-01…04-04 |

<objective>
Finish the modal workflow: Edit-from-sentence, perspective defaults, live-request reliability, accessibility, and responsive layout (D-03 Edit, D-14, D-16, D-17, D-21, D-22).

Purpose: Close remaining success criteria so Save/Cancel/Edit and live chips match Bet Labs interaction quality.
Output: Prefill Edit path, debounce/stale/retry behavior, perspective UI, responsive/focus CSS, source-contract tests + end-of-phase human checks.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-filter-popup-modal/04-CONTEXT.md
@.planning/phases/04-filter-popup-modal/04-RESEARCH.md
@.planning/phases/04-filter-popup-modal/04-UI-SPEC.md
@.planning/phases/04-filter-popup-modal/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Active-filter Edit prefill + perspective defaults</name>
  <files>cfb_system_maker/web.py, cfb_system_maker/describe.py, cfb_system_maker/templates/index.html, cfb_system_maker/static/filter_modal.js, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-03, D-14)
    .planning/phases/04-filter-popup-modal/04-UI-SPEC.md (Edit copy; Perspective labels Bet-side / Opponent / Either)
    .planning/phases/04-filter-popup-modal/04-PATTERNS.md (sentence enrichment pattern attaching remove_href; atomic ff_* rewrite)
    cfb_system_maker/web.py (sentences loop attaching remove_href)
    cfb_system_maker/describe.py (coalesced keys from 04-03)
    cfb_system_maker/templates/index.html (active-filter sentence list + Remove)
  </read_first>
  <behavior>
    - Each editable filter sentence renders visible Edit text with aria-label Edit filter: {sentence text}; Remove ✕ remains separate.
    - Clicking Edit opens the shared dialog prefilled with committed bounds/selection/perspective.
    - New team-scoped filter on a spread system defaults perspective bet_side; on a total system defaults either; editing preserves committed perspective (D-14).
    - Favorite/underdog/home/away position toggles are not Edit-modal filters (remain global) — Edit appears only for D-01 modal candidates.
  </behavior>
  <action>
    Enrich describe()/web sentence rows with edit metadata: candidate_id, perspective (when applicable), and committed value payload (min/max or selected values). Template: add Edit control beside Remove without changing remove_href behavior (D-03).

    In filter_modal.js: support open from launcher (clean draft) and open from Edit (prefill). Perspective segmented control for team_scoped features with labels Bet-side / Opponent / Either; changing perspective refetches /filter-detail and refreshes live chips. Enforce defaults for new filters from system.bet_type (D-14).

    Tests: rendered Edit controls for seasons and a feature filter; edit metadata present; perspective default assertions via descriptor helper or JS data attributes; Remove still present and independent.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x -k "edit or perspective or prefill"</automated>
  </verify>
  <acceptance_criteria>
    - Active-filter list HTML includes the visible text Edit for at least one core and one feature sentence in tests.
    - remove_href links still exist alongside Edit.
    - Perspective default helper/tests encode spread→bet_side and total→either for new team-scoped filters.
    - Focused pytest selector above passes.
  </acceptance_criteria>
  <done>Users can Edit any modal filter from its sentence with correct perspective defaults/preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Live debounce/stale/retry, a11y focus, responsive layout</name>
  <files>cfb_system_maker/static/filter_modal.js, cfb_system_maker/static/styles.css, cfb_system_maker/templates/index.html, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-16, D-17, D-21, D-22)
    .planning/phases/04-filter-popup-modal/04-UI-SPEC.md (Live Metrics; Modal Shell focus/Escape/backdrop; narrow layout; copy for Updating… / Retry / error)
    .planning/phases/04-filter-popup-modal/04-RESEARCH.md (Pattern 2 Draft/Commit; Stale-Request Cancellation; Pitfall 6 Cancel leaks; Pitfall 7 a11y)
    .planning/phases/04-filter-popup-modal/04-VALIDATION.md (Manual-Only Verifications table)
    cfb_system_maker/static/filter_modal.js (current fetch path)
  </read_first>
  <action>
    Implement ~250ms debounce on live /api/backtest calls; abort prior fetch via AbortController; increment a generation counter and ignore responses whose generation is not current (D-16). While in flight, keep last successful chip values and show status text Updating…. On failure: show Couldn’t update live stats. with Retry button; do not close modal, do not mutate filters-form, do not replace last success with zeros; Save stays disabled until a successful validation for the current draft (D-17).

    Harden dialog a11y (D-21): aria-labelledby on title; move focus into dialog on open; on close restore focus to the launching control (launcher or Edit); route cancel event to discard; Escape equals Cancel; visible :focus-visible using 2px solid accent; backdrop click does nothing.

    Responsive CSS (D-22): ≥901px main~70%/About~30% with 32px gap; ≤900px stack header→controls→exploration→About→actions; table wrap scrolls horizontally; footer Cancel then Save Filter right-aligned on desktop.

    Source-contract tests in test_filter_modal.py assert presence of debounce delay constant (~250), AbortController usage, generation guard, Retry label, Updating… string, and backdrop non-dismiss behavior hooks. Do not add a browser test framework (D-23).
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x -k "debounce or stale or retry or focus or edit or perspective"</automated>
    <human-check>
      1. Start web UI with processed data; open a categorical filter from the sidebar — dialog opens, focus is inside, chips update after short pause.
      2. Drag/edit a numeric filter; confirm BETWEEN sync, Chart/List toggle, and live chips; Save commits URL; Cancel leaves URL unchanged.
      3. Edit from an active-filter sentence; Escape discards and focus returns to Edit; backdrop click leaves dialog open.
      4. Force a live request failure (DevTools offline or block /api/backtest); confirm error+Retry, last chips remain, Save gated; Retry recovers.
      5. Resize to narrow width: About stacks below; table scrolls horizontally; actions reachable.
    </human-check>
  </verify>
  <acceptance_criteria>
    - filter_modal.js contains a 250 debounce (literal 250 or named constant equal to 250), AbortController, and a generation counter check before applying metrics.
    - Exact UI copy strings Updating…, Couldn’t update live stats., and Retry appear in JS or template.
    - CSS includes a max-width 900px (or matching existing 900px breakpoint) stacking rule for the modal layout.
    - python -m pytest tests/test_filter_modal.py tests/test_web.py tests/test_web_features.py -x passes.
    - Full suite python -m pytest -q passes before phase verification.
  </acceptance_criteria>
  <done>Live reliability, Edit prefill, perspective, a11y, and responsive behavior complete the phase success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Async fetch responses → UI state | Stale/wrong generation must not commit into draft validation |
| Edit metadata → modal open | candidate_id must remain server-authored allowlisted ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | stale fetch race | mitigate | AbortController + generation identity before render/enable Save (D-16/D-17) |
| T-04-14 | Tampering | failed live request | mitigate | Error+Retry path never mutates filters-form; Save gated on successful validation (D-17) |
| T-04-15 | Elevation / XSS | Edit aria-labels / sentence text | mitigate | Jinja auto-escape on aria-label and sentence text; no safe filter |
| T-04-16 | Spoofing | edit candidate_id attrs | mitigate | Open path still resolves ids through server allowlists on /filter-detail and /api/backtest |
| T-04-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
Automated: python -m pytest tests/test_filter_modal.py tests/test_web.py tests/test_web_features.py -x && python -m pytest -q
Human (end-of-phase): keyboard lifecycle, slider sync, stale/retry, narrow layout per VALIDATION.md manual table.
</verification>

<success_criteria>
MODAL-01 Edit path, MODAL-02 live reliability, and MODAL-06 Save validation gating are complete; phase success criteria 1–5 are achievable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/04-filter-popup-modal/04-04-SUMMARY.md` when done
</output>
