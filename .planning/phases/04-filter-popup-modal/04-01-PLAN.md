---
phase: 04-filter-popup-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_filter_modal.py
  - cfb_system_maker/features.py
  - cfb_system_maker/backtest.py
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - cfb_system_maker/static/filter_modal.js
  - tests/test_features.py
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-05, MODAL-06]

must_haves:
  truths:
    - "As a system builder, I can open the Season filter from the sidebar into a native dialog instead of expanding it only as inline details."
    - "While the Season modal draft changes, the header Record / Money Won / ROI chips update from server JSON before Save (same grading path as the main page, including Fade)."
    - "The modal About Filter panel shows non-empty plain-text Season definition text (escaped / textContent — never trusted HTML)."
    - "Save Filter writes the draft into filters-form and submits the existing GET flow; Cancel / Escape / close leave filters-form and the URL unchanged."
    - statement: "Empty/no-match live draft shows neutral chips 0-0-0, $0, 0% with explanatory copy rather than a broken UI."
      verification: backstop
    - statement: "Live recalculation keeps last successful chip values visible with Updating… status rather than flashing zeros."
      verification: backstop
  artifacts:
    - path: tests/test_filter_modal.py
      provides: "Wave 0 focused suite for modal contracts"
    - path: cfb_system_maker/features.py
      provides: "FeatureDef.description on every registry feature"
      contains: "description"
    - path: cfb_system_maker/backtest.py
      provides: "run_backtest_summary lightweight Record/Money/ROI seam"
      exports: ["run_backtest_summary"]
    - path: cfb_system_maker/web.py
      provides: "GET /api/backtest + core filter descriptors + strict parse helpers"
    - path: cfb_system_maker/templates/index.html
      provides: "One labelled dialog + Season launcher hooks"
    - path: cfb_system_maker/static/filter_modal.js
      provides: "Draft/commit dialog state machine for Season slice"
  key_links:
    - from: "cfb_system_maker/static/filter_modal.js"
      to: "cfb_system_maker/web.py"
      via: "debounced GET /api/backtest with form draft query"
      pattern: "/api/backtest"
    - from: "cfb_system_maker/web.py"
      to: "cfb_system_maker/backtest.py"
      via: "run_backtest_summary -> matches_system + grade_bet"
      pattern: "run_backtest_summary"
    - from: "cfb_system_maker/static/filter_modal.js"
      to: "filters-form"
      via: "Save writes canonical season controls then form.submit()"
      pattern: "filters-form"
---

## Phase Goal

**As a** system builder, **I want to** open any filter in a live Bet Labs-style popup with performance evidence and About text before I commit, **so that** I can explore values without changing my system until I explicitly Save.

## Artifacts this phase produces

| Symbol | Module | Introduced in |
|--------|--------|---------------|
| `FeatureDef.description` | `features.py` | 04-01 |
| `CORE_FILTER_META` | `web.py` | 04-01 |
| `filter_descriptor(candidate_id)` | `web.py` | 04-01 |
| `parse_system_strict(args)` / strict mode on form parse | `web.py` | 04-01 |
| `run_backtest_summary(...)` | `backtest.py` | 04-01 |
| `GET /api/backtest` | `web.py` | 04-01 |
| `GET /filter-detail` | `web.py` | 04-02 |
| `aggregate_filter_value_rows(...)` | `web.py` | 04-02 |
| `remove_candidate_filters(...)` | `web.py` | 04-02 |
| `describe()` numeric gte+lte coalesce + edit identity | `describe.py` | 04-03 |
| `static/filter_modal.js` (full interactions) | static | 04-01…04-04 |
| Sidebar launchers + progressive enhancement | `index.html` | 04-01…04-02 |
| Active-filter Edit controls | `index.html` / `web.py` | 04-04 |

<objective>
Ship the thinnest end-to-end Bet Labs filter popup for Season: launcher → dialog → live chips via `/api/backtest` → About text → Save commits / Cancel discards (D-01 Season, D-03/D-04/D-15/D-18/D-19/D-23).

Purpose: Prove the draft/commit architecture and authoritative live metrics before expanding to every filter type.
Output: Wave 0 tests, registry/core descriptions, `run_backtest_summary`, `/api/backtest`, Season modal shell + JS module.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-filter-popup-modal/04-CONTEXT.md
@.planning/phases/04-filter-popup-modal/04-RESEARCH.md
@.planning/phases/04-filter-popup-modal/04-UI-SPEC.md
@.planning/phases/04-filter-popup-modal/04-PATTERNS.md
@.planning/phases/04-filter-popup-modal/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests, descriptions, and lightweight summary seam</name>
  <files>tests/test_filter_modal.py, cfb_system_maker/features.py, cfb_system_maker/backtest.py, tests/test_features.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-15, D-18, D-19, D-20)
    .planning/phases/04-filter-popup-modal/04-RESEARCH.md (Pattern 7 Lightweight Shared Backtest Summary; Endpoint Contracts for /api/backtest; Validation Architecture Wave 0)
    .planning/phases/04-filter-popup-modal/04-PATTERNS.md (features.py FeatureDef pattern; backtest.py match+grade head)
    cfb_system_maker/features.py (FeatureDef fields; FEATURE_REGISTRY rows; FEATURE_BY_KEY)
    cfb_system_maker/backtest.py (run_backtest match→grade→aggregate head before stats/grade; matches_system; grade_bet)
    tests/test_web.py (Flask client fixture patterns)
    tests/test_features.py (registry completeness assertions)
  </read_first>
  <behavior>
    - Every FEATURE_REGISTRY entry has a non-empty strip()'d description string; result_lookahead features include analysis-only wording.
    - run_backtest_summary on a tiny fixture returns wins/losses/pushes/hit_rate/profit/money_won/roi and does not compute permutation/system-stats/grade fields.
    - Fade flips graded side in the summary the same way grade_bet does.
    - Zero matched bets → wins=losses=pushes=0, profit=0, money_won=0, roi=0, hit_rate=0.
    - Failing integration stubs exist for GET /api/backtest (200 JSON chips) and Season dialog markup (dialog + launcher data attributes) — they fail until Task 2.
  </behavior>
  <action>
    Create tests/test_filter_modal.py with deterministic GameRecord fixtures (do not load the full local dataset). Assert FeatureDef.description completeness (MODAL-05 / D-19 / D-20) and run_backtest_summary contracts (MODAL-02 / D-15). Add failing client tests for /api/backtest and Season dialog hooks that Task 2 will green.

    In features.py: add trailing `description: str = ""` on FeatureDef (keep existing positional fields stable). Populate a concrete non-empty plain-text description on every FEATURE_REGISTRY row explaining units, directionality, perspective when team_scoped, and entering-game timing where relevant; result_lookahead rows must mention analysis-only / lookahead. Extend tests/test_features.py with the non-empty description assertion.

    In backtest.py: add `run_backtest_summary(games, system, *, stake=1.0, american_odds=-110, feature_map=None) -> dict` that runs the same matches_system + grade_bet loop as run_backtest but returns only `{wins, losses, pushes, hit_rate, profit, money_won, roi}` where money_won is profit*100 (flat-stake dollars) and hit_rate/roi follow existing rounding. Do not call compute_system_stats, season breakdown, permutation testing, or compute_grade. Leave default run_backtest behavior unchanged for the main page.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py tests/test_features.py -x -k "description or summary or fade or zero"</automated>
  </verify>
  <acceptance_criteria>
    - FeatureDef declares description as a trailing defaulted str field.
    - python -m pytest tests/test_features.py -k description -q passes (every registry description non-empty).
    - python -m pytest tests/test_filter_modal.py -k "summary or fade or zero" -q passes.
    - run_backtest_summary is importable from cfb_system_maker.backtest and its return dict keys are exactly wins, losses, pushes, hit_rate, profit, money_won, roi.
    - Existing python -m pytest tests/test_backtest.py -q still passes (full path unchanged).
  </acceptance_criteria>
  <done>Descriptions and lightweight summary seam exist with focused green tests; dialog/API integration tests remain red until Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/backtest + Season dialog Save/Cancel slice</name>
  <files>cfb_system_maker/web.py, cfb_system_maker/templates/index.html, cfb_system_maker/static/styles.css, cfb_system_maker/static/filter_modal.js, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-UI-SPEC.md (Interaction Contract — Modal Shell; Copywriting Contract; Color/Typography/Spacing for modal)
    .planning/phases/04-filter-popup-modal/04-RESEARCH.md (Patterns 2–4; /api/backtest contract; progressive enhancement)
    cfb_system_maker/web.py (_form_values, _system_from_form, _feature_filters_from_request, create_app routes, missing-data handling)
    cfb_system_maker/templates/index.html (filters-form; season controls; metrics chips; active-filter sentences)
    cfb_system_maker/static/styles.css (:root tokens; .positive/.negative; chip rules)
  </read_first>
  <behavior>
    - GET /api/backtest with valid form query returns 200 JSON chips from run_backtest_summary; unknown/illegal feature keys return 400; missing games file returns 503.
    - Neutral no-match is 200 with numeric zeros (not an error).
    - Index HTML includes one dialog#filter-modal with aria-labelledby, Save Filter / Cancel / close ✕, live chip slots, About Filter region, and a Season launcher button with data-candidate-id="core:season".
    - Canonical season controls remain in filters-form for no-JS use; JS adds a root .js class only after init and then prefers launchers (D-05).
    - Cancel path does not mutate season inputs; Save path writes selected seasons then submits filters-form GET.
  </behavior>
  <action>
    In web.py (per D-18): add CORE_FILTER_META for at least core:season (and stubs/dicts for the other core IDs D-01 will need next plan — season/week/team/conference/provider/spread_range/total_range) with label, control, description, canonical param mapping. Add filter_descriptor(candidate_id) resolving core:* from CORE_FILTER_META and feature:* from FEATURE_BY_KEY + description. Harden parsing used by the API: reject unknown namespaced IDs / unknown feature keys / illegal perspectives / non-finite numbers with 400 JSON `{error, message}`; reuse _system_from_form after validation rather than inventing a second SystemFilter builder.

    Register GET /api/backtest inside create_app: parse request.args through the strict path, build SystemFilter, call run_backtest_summary on loaded games/features, return the summary dict. Do not return bet_details or significance blocks.

    In index.html: keep sidebar + workspace layout (D-02). Add one reusable native dialog shell matching UI-SPEC copy (Save Filter, Cancel, Close filter dialog, About Filter, Record/Money Won/ROI chips). Add a Season launcher control with data-candidate-id="core:season". Keep existing season conventional controls in the form as the progressive-enhancement fallback (visible until JS init adds .js).

    Create static/filter_modal.js: open Season via showModal(); copy committed seasons into a draft; on draft change call /api/backtest (simple fetch OK this plan — debounce lands in 04-04); render chips from server JSON only (never compute ROI client-side); About text via textContent from descriptor/embedded data-description; Save writes draft into canonical season inputs and submits filters-form; Cancel, the dialog cancel event, and the close control all discard the draft and close without form mutation; restore focus to the Season launcher on close (D-21). Backdrop clicks must not close (D-21).

    Style modal with existing tokens per UI-SPEC (max width 960px, backdrop rgba from --text at 45%, accent only on Save Filter and signed money/ROI). Green Task 1's failing API/dialog tests and add cancel-leaves-form-untouched plus save-serializes-seasons assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/backtest returns application/json with keys wins, losses, pushes, hit_rate, profit, money_won, roi on a valid query.
    - Malformed/unknown feature key yields status 400 (not 500).
    - Rendered index contains dialog#filter-modal, button/link with data-candidate-id="core:season", and the exact CTA strings Save Filter and Cancel.
    - python -m pytest tests/test_filter_modal.py -x passes.
    - python -m pytest tests/test_web.py -x -q still passes (no regression to main editor).
  </acceptance_criteria>
  <done>User can open Season in a live modal, see About + server chips, Save via GET or Cancel without mutating the form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/backtest | Untrusted query args (keys, ops, values, perspectives) |
| Server → Browser JSON/HTML | Definitions and labels must be treated as untrusted text at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | /api/backtest args | mitigate | Strict allowlist parse; reject unknown keys/ops/perspectives with 400 before compute (D-18) |
| T-04-02 | Tampering | Client metrics | mitigate | Browser never computes Record/ROI/Money; only displays server JSON (D-15) |
| T-04-03 | Information Disclosure / XSS | About Filter / chip labels | mitigate | Plain-text descriptions; Jinja auto-escape; DOM textContent only (D-19) |
| T-04-04 | Denial of Service | /api/backtest | mitigate | Use run_backtest_summary (skip permutation/stats); no N×full backtests |
| T-04-05 | Tampering | GET handlers | accept | Endpoints are read-only; no storage writes (phase boundary) |
| T-04-SC | Tampering | pip installs | accept | No new packages this phase |
</threat_model>

<verification>
python -m pytest tests/test_filter_modal.py tests/test_features.py tests/test_web.py -x
</verification>

<success_criteria>
Season opens in a dialog with live server chips and About text; Save commits through filters-form GET; Cancel discards; MODAL-01/02/05/06 proven for the Season vertical slice.
</success_criteria>

<output>
Create `.planning/phases/04-filter-popup-modal/04-01-SUMMARY.md` when done
</output>
