---
phase: 04-filter-popup-modal
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - cfb_system_maker/web.py
  - cfb_system_maker/templates/index.html
  - cfb_system_maker/static/styles.css
  - cfb_system_maker/static/filter_modal.js
  - tests/test_filter_modal.py
autonomous: true
requirements: [MODAL-01, MODAL-04, MODAL-05]

must_haves:
  truths:
    - "Every D-01 sidebar filter constraint (registry features plus season, week, team, conference, provider, and spread/total ranges) launches the shared modal instead of being the primary inline editor under JS enhancement."
    - "Categorical filters show a searchable, sortable checkbox table of Description / Record / ROI / Money with multi-select saved as existing in semantics."
    - "Boolean filters show exactly two single-choice rows Yes and No; Save requires one choice."
    - "Per-value rows come from GET /filter-detail computed with the candidate removed and other system filters applied; header chips remain system+draft via /api/backtest."
    - "Registry groups and result_lookahead quarantine styling remain; lookahead filters repeat the analysis-only warning inside the modal."
    - statement: "Empty observed domains render No values in range copy with neutral chips rather than a broken table."
      verification: backstop
    - statement: "No-JS users still get usable conventional controls because fallback editors stay in the DOM until JS successfully enhances."
      verification: backstop
    - statement: "Wide value tables on narrow screens scroll horizontally inside the modal without growing the page sideways."
      verification: backstop
  artifacts:
    - path: cfb_system_maker/web.py
      provides: "GET /filter-detail + one-pass aggregate_filter_value_rows + remove_candidate_filters"
      exports: ["aggregate_filter_value_rows"]
    - path: cfb_system_maker/templates/index.html
      provides: "Grouped launchers for all D-01 filters + fallback controls"
    - path: cfb_system_maker/static/filter_modal.js
      provides: "Categorical/boolean table search/sort/select rendering"
  key_links:
    - from: "filter_modal.js"
      to: "/filter-detail"
      via: "fetch candidate_id + current form query; render rows with textContent"
      pattern: "/filter-detail"
    - from: "aggregate_filter_value_rows"
      to: "matches_system + grade_bet"
      via: "one pass; candidate removed; bucket by resolved value"
      pattern: "matches_system"
---

## Phase Goal

**As a** system builder, **I want to** open any filter in a live Bet Labs-style popup with performance evidence and About text before I commit, **so that** I can explore values without changing my system until I explicitly Save.

## Artifacts this phase produces

| Symbol | Module | Introduced in |
|--------|--------|---------------|
| `GET /filter-detail` | `web.py` | 04-02 |
| `aggregate_filter_value_rows(...)` | `web.py` | 04-02 |
| `remove_candidate_filters(system, candidate_id)` | `web.py` | 04-02 |
| `resolve_candidate_value(game, system, descriptor, perspective)` | `web.py` | 04-02 |
| Grouped sidebar launchers for all D-01 candidates | `index.html` | 04-02 |
| Categorical/boolean value table UI | `filter_modal.js` | 04-02 |
| (carries forward) `run_backtest_summary`, `/api/backtest`, `FeatureDef.description`, dialog shell | from 04-01 | — |

<objective>
Expand the Season vertical slice into the full launcher catalog plus categorical/boolean value-performance tables backed by one-pass `/filter-detail` (D-01, D-02, D-05, D-08, D-11–D-13, D-18–D-20).

Purpose: Users can explore list/boolean filters with per-value Record/ROI/Money before committing.
Output: `/filter-detail`, candidate removal helpers, grouped launchers, searchable sortable table UI, lookahead About warnings.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-filter-popup-modal/04-CONTEXT.md
@.planning/phases/04-filter-popup-modal/04-RESEARCH.md
@.planning/phases/04-filter-popup-modal/04-UI-SPEC.md
@.planning/phases/04-filter-popup-modal/04-PATTERNS.md
@.planning/phases/04-filter-popup-modal/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One-pass /filter-detail for categorical and boolean candidates</name>
  <files>cfb_system_maker/web.py, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-RESEARCH.md (Pattern 6 One-Pass Per-Value Aggregation; /filter-detail contract; Core Value Resolution table)
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-08, D-10, D-11, D-13, D-18)
    cfb_system_maker/web.py (_scan_values, _feature_options, /api/backtest from 04-01, CORE_FILTER_META)
    cfb_system_maker/backtest.py (matches_system, grade_bet, _side_spread)
    cfb_system_maker/features.py (resolve_feature_value, feature_ok, team_scoped)
    tests/test_filter_modal.py (fixtures from 04-01)
  </read_first>
  <behavior>
    - GET /filter-detail?candidate_id=feature:neutralSite (or core:provider) returns 200 JSON with descriptor metadata, description, rows sorted Description ascending by default for categorical/boolean.
    - Each row includes value, description, wins, losses, pushes, record (W-L-P string), roi, money (profit*100).
    - Candidate’s committed representation is removed before matching; other filters and Fade remain.
    - Boolean domain is exactly Yes/No (true/false mapped to those labels).
    - Unknown candidate_id → 400; empty domain → 200 with empty rows + neutral metadata (not 500).
    - Core team/conference rows use bet-side identity; never accept arbitrary unsanctioned candidate ids.
  </behavior>
  <action>
    Implement remove_candidate_filters(system, candidate_id) that clears the correct core fields (both bounds for ranges; list fields for seasons/weeks/teams/conferences/providers) or all FeatureFilter entries for a feature:* key (same-key pairs included for later numeric work).

    Implement aggregate_filter_value_rows(games, base_system, descriptor, *, feature_map, perspective) as one pass: matches_system against candidate-removed system → resolve value → grade_bet once → bucket → summarize. For team_scoped either, a game may contribute to distinct home/away buckets but equal duplicate values for that game are deduped (D-08 / research).

    Register GET /filter-detail: require candidate_id; optional perspective allowlisted for team_scoped features; parse rest of form as the committed system; return JSON shape (planner discretion, lock these names): candidate_id, label, control, description, lookahead_warning (bool or string), team_scoped, perspective, allowed_perspectives, domain, rows, chart_points (empty list for non-numeric this plan). Cap nothing for categorical option sets beyond existing observed domains; reject oversized crafted in-lists on the live parse path (D-18).

    Complete CORE_FILTER_META descriptions for week/team/conference/provider (and range stubs used as launchers even if numeric UI lands in 04-03). Tests cover categorical multi-value, boolean Yes/No, candidate removal, unknown id 400, empty domain.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x -k "filter_detail or categorical or boolean or remove_candidate"</automated>
  </verify>
  <acceptance_criteria>
    - /filter-detail is registered and returns application/json.
    - Boolean rows are exactly two labels Yes and No in tests.
    - A test proves an existing candidate filter is absent from the distribution base system (domain includes values outside the prior selection when other filters allow it).
    - python -m pytest tests/test_filter_modal.py -x -k "filter_detail or categorical or boolean" passes.
  </acceptance_criteria>
  <done>/filter-detail returns deterministic per-value performance rows with candidate removed.</done>
</task>

<task type="auto">
  <name>Task 2: Grouped launchers + categorical/boolean table UI</name>
  <files>cfb_system_maker/templates/index.html, cfb_system_maker/static/filter_modal.js, cfb_system_maker/static/styles.css, tests/test_filter_modal.py</files>
  <read_first>
    .planning/phases/04-filter-popup-modal/04-UI-SPEC.md (Categorical/Boolean contract; Sidebar Launchers; Copywriting; empty/error UI considerations)
    .planning/phases/04-filter-popup-modal/04-CONTEXT.md (D-01, D-02, D-05, D-11, D-12, D-13, D-20)
    cfb_system_maker/templates/index.html (current feature group details; lookahead quarantine; Season launcher from 04-01)
    cfb_system_maker/static/filter_modal.js (Season state machine from 04-01)
    docs/bet-labs-parity-plan.md (section 1.3 Filter detail modal — anatomy only)
  </read_first>
  <action>
    Convert all D-01 sidebar filter constraints into grouped launcher buttons (registry groups preserved; result_lookahead quarantine class/styling preserved). Global controls stay non-modal per D-01: bet type, spread/total side, position toggles, Fade, theory, load, Run System, Save System.

    Keep real canonical controls in filters-form as fallback; only hide them after successful JS init adds documentElement/root .js (never hide in base CSS alone — D-05).

    Extend filter_modal.js so opening a categorical/boolean candidate: fetches /filter-detail, renders About via textContent (include lookahead warning when flagged — D-20), builds a checkbox table (radio semantics for boolean) with columns Description, Record, ROI, Money; client-side case-insensitive Search values filter; default sort Description ascending; Record/ROI/Money headers are buttons toggling asc/desc with aria-sort on the active column (D-12, D-21). Multi-select categorical drafts save as op=in; boolean requires exactly one Yes/No before Save enables (D-13). No free-text comma entry in the primary modal UI (D-11).

    Wire live chips through existing /api/backtest using the draft selection. Save writes ff_* arrays or core list fields atomically (decode→replace→rebuild parallel arrays together — research Pitfall 3). Cancel still discards.

    Add rendered-contract tests for launchers across groups, dialog present once, fallback controls still in HTML, lookahead warning string available to the modal path, and table column headers.
  </action>
  <verify>
    <automated>python -m pytest tests/test_filter_modal.py -x -k "launcher or fallback or lookahead or table or categorical or boolean"</automated>
  </verify>
  <acceptance_criteria>
    - Rendered HTML includes data-candidate-id launchers for core:season, core:week, core:team, core:conference, core:provider, core:spread_range, core:total_range, and at least one feature:* from each major group including result_lookahead.
    - filter_modal.js contains client-side search and aria-sort handling (source contract).
    - Fallback conventional filter controls remain present in the raw HTML response without requiring the .js class.
    - python -m pytest tests/test_filter_modal.py -x passes together with tests/test_web.py -q.
  </acceptance_criteria>
  <done>All D-01 filters launch the modal; categorical/boolean exploration uses the value table with search/sort/select.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /filter-detail | Untrusted candidate_id, perspective, and form filters |
| Server row labels → DOM | Categorical values and descriptions rendered in JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | candidate_id / perspective | mitigate | Resolve only through CORE_FILTER_META / FEATURE_BY_KEY; 400 otherwise (D-18) |
| T-04-07 | XSS | table Description cells | mitigate | Build rows with DOM APIs + textContent; never assign untrusted HTML strings to innerHTML (D-19) |
| T-04-08 | Denial of Service | /filter-detail | mitigate | One-pass aggregation only; bound crafted list sizes on parse; no per-value run_backtest loops |
| T-04-09 | Tampering | distribution conditioning | mitigate | remove_candidate_filters clears full candidate representation before matching (D-08) |
| T-04-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
python -m pytest tests/test_filter_modal.py tests/test_web.py tests/test_web_features.py -x
</verification>

<success_criteria>
MODAL-01 launchers cover D-01 filters; MODAL-04 categorical/boolean tables work with server per-value stats; MODAL-05 About + lookahead warnings render as escaped text.
</success_criteria>

<output>
Create `.planning/phases/04-filter-popup-modal/04-02-SUMMARY.md` when done
</output>
