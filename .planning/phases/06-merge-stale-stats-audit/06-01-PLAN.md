---
phase: 06-merge-stale-stats-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [MERGE-01, MERGE-02]

must_haves:
  truths:
    - "master's HEAD includes merge commit 2e86ba5 (or a later commit that contains it)"
    - "The full test suite passes on master (441+ tests)"
    - "data/search_runs/ contains no stale SearchRun/SearchRunFinalist JSON computed under pre-merge matching semantics"
  artifacts: []
  key_links:
    - "git history: master branch contains fix/web-app-review-2026-08-26's commits via merge commit 2e86ba5"
---

<objective>
Verify that Phase 6's work — merging `fix/web-app-review-2026-08-26` into `master` and auditing `data/search_runs/` for stale beam-search results — is actually complete and correct, per MERGE-01 and MERGE-02.

This work was already executed directly (outside the plan/execute pipeline) on 2026-08-26: merge commit `2e86ba5` is on `master` and pushed to `origin/master`, 441 tests passed post-merge, and `data/search_runs/` was confirmed empty. See `.planning/phases/06-merge-stale-stats-audit/06-00-DIRECT-EXECUTION-SUMMARY.md` for the full record of that work.

This plan does NOT redo the merge, re-resolve conflicts, or re-run the branch switch. It re-verifies the two requirements through the normal pipeline so GSD's completion detection has a real PLAN.md/execution trail to point at, and to catch any regression or newly-appeared stale data since 2026-08-26.

Purpose: Close the tooling gap (no PLAN.md existed for this phase) without redoing finished, verified work.
Output: Confirmation that MERGE-01 and MERGE-02 hold on current `master`, recorded in this plan's execution summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-merge-stale-stats-audit/06-CONTEXT.md
@.planning/phases/06-merge-stale-stats-audit/06-00-DIRECT-EXECUTION-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify master contains the merge and the full test suite passes (MERGE-01)</name>
  <files>none — read-only verification</files>
  <action>
    Confirm `master`'s current HEAD contains merge commit `2e86ba5` per D-01: run `git merge-base --is-ancestor 2e86ba5 HEAD` (exit 0 means ancestor) or `git log --oneline | grep 2e86ba5` while on `master`, and separately confirm the branch history includes `fix/web-app-review-2026-08-26`'s commits (e.g. `git log --oneline --merges | grep 2e86ba5` shows the merge, and `git log 2e86ba5^2 --oneline -5` shows review-branch commits reachable from HEAD). Do not attempt to re-merge, rebase, or alter history — this is a read-only check. Then run the full test suite from repo root and confirm it passes with at least 441 tests collected.
  </action>
  <verify>
    <automated>cd "C:\Users\mckel\dev\cfb-site" &amp;&amp; git merge-base --is-ancestor 2e86ba5 HEAD &amp;&amp; python -m pytest -q</automated>
  </verify>
  <done>`git merge-base --is-ancestor 2e86ba5 HEAD` exits 0 (2e86ba5 is an ancestor of current master HEAD), and `python -m pytest -q` reports 441 or more passing tests with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Verify data/search_runs/ has no stale SearchRun data (MERGE-02)</name>
  <files>none — read-only verification; conditional on findings</files>
  <action>
    List `data/search_runs/` directly (not from memory or the prior SUMMARY) to confirm current state. If empty (as it was on 2026-08-26 per the prior audit), MERGE-02 remains a verified no-op — no further action needed. If any `SearchRun`/`SearchRunFinalist` JSON files are now present (created since the last audit), read `storage.py:360-414` to understand the current matching-semantics fields each record depends on, then check whether each file's fields reflect the current (post-merge) total-filter matching semantics or the old pre-merge semantics. Flag any file computed under pre-merge semantics by name in the task output rather than silently deleting or regenerating it — do not invent a regeneration mechanism beyond what MERGE-02 already scoped (audit and flag/regenerate only if stale data is actually found).
  </action>
  <verify>
    <automated>cd "C:\Users\mckel\dev\cfb-site" &amp;&amp; (ls data/search_runs/*.json 2>/dev/null | grep -c '\.json$') || echo 0</automated>
  </verify>
  <done>`data/search_runs/` is confirmed empty (0 `.json` files) and MERGE-02 remains a verified no-op, OR any newly-present `SearchRun`/`SearchRunFinalist` files are individually checked against current matching semantics and each is explicitly reported as either consistent or flagged as stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase performs read-only git and filesystem verification with no new code, no external input, and no network calls beyond `git` operating on the existing local repository. No new trust boundary is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | git history (`master` HEAD) | low | accept | Verification is read-only (`git merge-base --is-ancestor`); no task in this plan mutates history, so there is no tampering surface introduced by this phase. |
| T-06-02 | Repudiation | `data/search_runs/` audit outcome | low | mitigate | Task 2 requires the audit result (empty vs. flagged files) to be explicitly recorded in the execution summary, not just asserted, matching the prior SUMMARY.md's "confirmed by inspection, not assumption" standard. |

</threat_model>

<verification>
Run both tasks' `<automated>` verify commands from repo root on `master`:
1. `git merge-base --is-ancestor 2e86ba5 HEAD && python -m pytest -q` — confirms MERGE-01 (merge present, tests pass).
2. `ls data/search_runs/*.json 2>/dev/null | grep -c '\.json$' || echo 0` — confirms MERGE-02 (directory state).

Both success criteria from ROADMAP.md Phase 6 section must read as still true after these checks:
1. `master` contains all commits from `fix/web-app-review-2026-08-26` (merge commit `2e86ba5`), full test suite (441+ tests) passes.
2. `data/search_runs/` is empty (or newly-found files are individually audited and any stale ones flagged).
</verification>

<success_criteria>
- `git merge-base --is-ancestor 2e86ba5 HEAD` exits 0 on current `master`.
- `python -m pytest -q` passes with 441 or more tests, zero failures.
- `data/search_runs/` is confirmed empty, or any files present are individually audited with results reported (not assumed).
- MERGE-01 and MERGE-02 are confirmed to still hold — no regression since the 2026-08-26 direct execution.
</success_criteria>

<output>
Create `.planning/phases/06-merge-stale-stats-audit/06-01-SUMMARY.md` when done
</output>