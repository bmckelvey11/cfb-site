---
phase: 09-live-in-season-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-live-in-season-verification/09-01-SUMMARY.md
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "Criterion 1 (pre-season calendar dry run) is recorded as the phase's first verified truth without being re-executed."
    - "The interpreter gap (system Python cannot import the vendored CFBD client) is confirmed still true or resolved, so plans 09-02/09-03 know which python.exe to invoke."
    - "The live calendar gate for criteria 2 and 3 is probed once, mechanically, and its exact state (fallback vs live, season, week) is recorded rather than guessed from a calendar date."
  artifacts:
    - ".planning/phases/09-live-in-season-verification/09-01-SUMMARY.md"
  key_links:
    - "09-01-SUMMARY.md references 09-DRY-RUN.md's PASSED result for criterion 1 rather than duplicating the dry run."
---

<objective>
Close out criterion 1 of Phase 9 as a recorded, phase-scoped result (it was already run and PASSED on 2026-08-27 — see `09-DRY-RUN.md` — this plan incorporates that result into the phase's verification trail, it does not re-run it), confirm the `.venv` interpreter requirement still holds, and run a single mechanical gate probe that tells plans 09-02 and 09-03 whether the calendar currently allows them to execute.

Purpose: Give the two calendar-gated plans (09-02, 09-03) a checkable precondition instead of a guessed date, and give the phase a single place where criterion 1's already-passed result is formally logged.
Output: `09-01-SUMMARY.md` recording (a) criterion 1 = PASSED by reference to `09-DRY-RUN.md`, (b) the interpreter check result, (c) the current gate-probe output (season/week/is_fallback) as of the day this plan runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-live-in-season-verification/09-DRY-RUN.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm interpreter and record the calendar gate probe</name>
  <files>.planning/phases/09-live-in-season-verification/09-01-SUMMARY.md</files>
  <read_first>.planning/phases/09-live-in-season-verification/09-DRY-RUN.md, cfb_system_maker/upcoming.py</read_first>
  <action>
Confirm the environment note from `09-DRY-RUN.md` still holds: run `.venv/Scripts/python.exe -c "import cfbd"` from the repo root (via `cfb_system_maker/cfbd_client.py`'s `_load_cfbd_module` path, i.e. actually import through the app: `.venv/Scripts/python.exe -c "from cfb_system_maker.cfbd_client import _load_cfbd_module; _load_cfbd_module(); print('ok')"`) and separately confirm system `python -c "import sys; print(sys.version)"` still reports a 3.14+ interpreter incompatible with the vendored client (do not attempt to fix or upgrade anything — this is a read-only confirmation, the workaround is "always use `.venv/Scripts/python.exe` for CFBD calls," already established).

Run the gate probe: `.venv/Scripts/python.exe -c "from cfb_system_maker.upcoming import resolve_target_week; from datetime import datetime, timezone; r = resolve_target_week(datetime.now(timezone.utc)); print(r)"`. This makes one real network call to `GamesApi.get_calendar()` for the current year using the token resolved by `find_cfbd_token()` (env.env / CFBD_API_KEY / CFBD-API / BEARER_TOKEN per `cfbd_client.py`) — do not print or log the token value itself, only the `WeekResolution` result.

Record in the SUMMARY: the interpreter confirmation, the raw `WeekResolution(season=..., week=..., season_type=..., is_fallback=...)` output, and a plain-language statement of what it means for 09-02 and 09-03: if `is_fallback=False`, criterion 2's gate is open (season is live) — note the resolved week so criterion 3's `week >= 3` sub-gate can be evaluated too; if `is_fallback=True`, both criteria are still blocked and 09-02/09-03 must halt on their own preconditions when executed later. State criterion 1 as PASSED by reference to `09-DRY-RUN.md` (quote its "Result: PASSED" line and the raw datetime field-type finding) — do not re-run the criterion-1 dry run itself, it is already complete.
  </action>
  <acceptance_criteria>
    09-01-SUMMARY.md exists and contains: (1) a verbatim reference to 09-DRY-RUN.md's PASSED result for criterion 1, (2) the interpreter confirmation output (`.venv/Scripts/python.exe` import succeeds; system `python` version noted as incompatible or the incompatibility no longer reproducing — either finding is acceptable, but it must be stated, not assumed), (3) the literal `WeekResolution(...)` repr printed by the gate probe, (4) an explicit is_fallback/week readout translated into "criterion 2 gate: open/blocked" and "criterion 3 gate: open/blocked" language. No CFBD token value appears anywhere in the file.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

Zero new code symbols. This is a verification-only plan against existing code (`cfb_system_maker/upcoming.py`, `cfb_system_maker/cfbd_client.py`); no functions, classes, or modules are created or modified. The only artifact is the documentation record `09-01-SUMMARY.md`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local process -> CFBD API | Outbound authenticated HTTPS call using a bearer token resolved from env.env / env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | CFBD token resolution (`cfbd_client.find_cfbd_token`) | medium | mitigate | Task 1 explicitly instructs: print only the `WeekResolution` result, never the token or `Configuration` object; SUMMARY.md must not contain the token value. |
| T-09-SC | Tampering | No new package installs in this plan | low | accept | Plan makes zero new dependency installs; existing vendored client only. |
</threat_model>

<verification>
`09-01-SUMMARY.md` exists, contains the gate-probe output, and correctly classifies criterion 2 and criterion 3 gate state from that output. No secrets present in the file (manual grep for the token substring before finishing, per the `env.env` handling convention documented in the project CLAUDE.md).
</verification>

<success_criteria>
Criterion 1 is formally recorded as part of Phase 9's verification trail (not re-run). Plans 09-02 and 09-03 have a documented, mechanically-obtained gate state to check their own preconditions against when they run.
</success_criteria>

<output>
Create `.planning/phases/09-live-in-season-verification/09-01-SUMMARY.md` when done
</output>
