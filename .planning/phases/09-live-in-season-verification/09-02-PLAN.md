---
phase: 09-live-in-season-verification
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/phases/09-live-in-season-verification/09-02-SUMMARY.md
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "The live 2026 season's currently-resolved week is fetched from CFBD (not a fixture) and persisted to data/processed/upcoming.csv."
    - "At least one persisted upcoming game has a posted line (spread and/or total non-null) and no result (home_points/away_points null), proving it is a real unplayed, lined game."
    - "The Current Matches panel (dashboard route '/') renders that game matched against a saved system, with a play recommendation and no grade/result field -- proving matching, not grading, occurred."
  artifacts:
    - "data/processed/upcoming.csv (gitignored data artifact, not committed)"
    - "data/processed/upcoming_meta.json (gitignored data artifact, not committed)"
    - "data/systems/phase9-permissive-check.json (gitignored data artifact, not committed) -- a deliberately unfiltered saved system created so criterion 2 cannot false-negative on an over-narrow existing system"
    - ".planning/phases/09-live-in-season-verification/09-02-SUMMARY.md"
  key_links:
    - "cfb_system_maker.upcoming.build_upcoming -> cfb_system_maker.storage.save_upcoming_games/save_upcoming_meta -> cfb_system_maker.web._current_matches_panel -> matches_system(..., require_played=False)"
---

<objective>
Prove, against the real live 2026 CFBD season (not fixtures), that the Current Matches panel shows real unplayed games with posted lines, matched correctly against a saved system.

Purpose: This is the tracer for Phase 9 -- the one end-to-end path (CFBD API -> normalize -> enrich -> persist -> dashboard panel render) the phase exists to verify. No new code is written; this task exercises the existing pipeline against production data for the first time.
Output: `09-02-SUMMARY.md` recording either a PASS with concrete evidence (game ids, line values, rendered panel row) or a documented HALT if the precondition is not yet met.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-live-in-season-verification/09-01-SUMMARY.md
@cfb_system_maker/upcoming.py
@cfb_system_maker/web.py
</context>

<precondition>
Run `.venv/Scripts/python.exe -c "from cfb_system_maker.upcoming import resolve_target_week; from datetime import datetime, timezone; r = resolve_target_week(datetime.now(timezone.utc)); print(r)"` and only proceed with Task 1 if the printed `WeekResolution` has `is_fallback=False` (a live calendar week currently contains "now" -- the 2026 season is underway). If `is_fallback=True`, HALT: do not fabricate or force a result. Write `09-02-SUMMARY.md` stating "BLOCKED -- season not yet live as of {today's date}; is_fallback=True, resolved to {season}/{week} instead" and stop -- do not mark any must_have as satisfied. Do not update ROADMAP.md's Phase 9 status, REQUIREMENTS.md's DATA-02 status, or STATE.md's phase-progress tracking on this path -- the phase remains open until this plan re-runs and passes.
</precondition>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end live Current Matches -- one path, real data</name>
  <files>data/processed/upcoming.csv, data/processed/upcoming_meta.json, data/systems/phase9-permissive-check.json, .planning/phases/09-live-in-season-verification/09-02-SUMMARY.md</files>
  <read_first>cfb_system_maker/upcoming.py, cfb_system_maker/web.py (lines 473-512 dashboard route, lines 1869-1928 _current_matches_panel), cfb_system_maker/storage.py (save_system, load_upcoming_games), cfb_system_maker/models.py (SystemFilter, SavedSystem)</read_first>
  <action>
First confirm `data/processed/games.csv` exists (`dir data\processed\games.csv` or equivalent) -- the dashboard route at web.py line 481 returns `error="missing_data"` and never calls `_current_matches_panel` if the historical games file is absent, which would produce a false failure unrelated to this criterion. If missing, run `.venv/Scripts/python.exe -m cfb_system_maker sample --data-dir data` first to populate it (network-free, uses bundled sample data, does not affect the live upcoming fetch).

Run the real live fetch: `.venv/Scripts/python.exe -m cfb_system_maker upcoming --data-dir data`. This calls `build_upcoming` -> live `GamesApi.get_games`/`BettingApi.get_lines` for the resolved season, writes `data/raw/games_{season}.json` and `data/raw/lines_{season}.json`, normalizes the target week via `normalize_games`, and persists `data/processed/upcoming.csv` + `data/processed/upcoming_meta.json` via `storage.save_upcoming_games`/`save_upcoming_meta`. Note the printed "Resolved {season} {season_type} week {week} ({row_count} game(s))" line in the SUMMARY.

Inspect `data/processed/upcoming.csv` directly (open it or `.venv/Scripts/python.exe -c` a short read via `storage.load_upcoming_games`) and find at least one row where `spread` or `total` is non-empty AND `home_points`/`away_points` are both empty -- this is the concrete proof of "real unplayed game with a posted line." Record that row's `game_id`, `home_team`, `away_team`, `spread`, `total` in the SUMMARY. If zero such rows exist in the current week (e.g. lines not yet posted this early in the week), state that explicitly as a partial/inconclusive result rather than marking the criterion PASSED -- do not treat an empty week as a code failure.

Create a deliberately permissive saved system so the panel cannot false-negative on an over-narrow existing system (the repo's only existing saved system, `data/systems/total-unders-high-lines.json`, requires `min_total=55.0`, which may match zero games in a given week): `.venv/Scripts/python.exe -c` a short script that imports `cfb_system_maker.models.SystemFilter` and `cfb_system_maker.storage.save_system`, constructs `SystemFilter(bet_type="spread", side="home")` (all other fields default -- no team/conference/spread/total/feature constraints), and calls `save_system("phase9-permissive-check", that_filter, "data")`.

Start the web app: `.venv/Scripts/python.exe -m cfb_system_maker web --data-dir data --port 5000` (background it or run with a short timeout), then fetch `http://127.0.0.1:5000/` (e.g. via `curl` or a short `urllib.request` call) and inspect the rendered HTML for the Current Matches panel section. Confirm the row for the game identified above (or another real upcoming game) appears with a `play` recommendation and matches on `phase9-permissive-check`, and confirm there is no grade/result/profit field rendered for that row (per `_current_matches_panel`'s docstring: "grading is never called here because an upcoming game has no result to grade" -- there is no `result`/`profit` key in the row dict at web.py lines 1896-1906, only `kickoff`/`matchup`/`play`/`system_name`/`type_label`/`details`). Record the matched row's rendered text in the SUMMARY. Stop the web server after confirming.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from cfb_system_maker.storage import load_upcoming_games; games, kickoffs = load_upcoming_games('data'); assert games, 'upcoming.csv is empty'; lined_unplayed = [g for g in games if (g.spread is not None or g.total is not None) and g.home_points is None and g.away_points is None]; print(len(games), 'upcoming game(s) loaded;', len(lined_unplayed), 'lined-and-unplayed')"</automated>
  </verify>
  <acceptance_criteria>
    `data/processed/upcoming.csv` exists and is non-empty after running the live `upcoming` CLI command (not a fixture). At least one row has a non-null `spread` or `total` and null `home_points`/`away_points` (or the SUMMARY explicitly documents that zero such rows existed this week, with the raw meta week/season noted). `data/systems/phase9-permissive-check.json` exists with no active filters. The rendered dashboard HTML at `/` contains a Current Matches row for a real game from `upcoming.csv`, with a `play` string present and no `result`/`profit`/`grade` text in that row's rendered output. 09-02-SUMMARY.md documents concrete game_id/team/spread/total values and the matched row's rendered play text, not a general "looks correct" statement.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

Zero new code symbols. This is a verification-only plan against existing code (`cfb_system_maker/upcoming.py`, `cfb_system_maker/web.py`, `cfb_system_maker/storage.py`); no functions, classes, or modules are created or modified. Data artifacts written (`data/processed/upcoming.csv`, `data/processed/upcoming_meta.json`, `data/systems/phase9-permissive-check.json`) are gitignored (`data/` per `.gitignore` line 1) and are not committed to the repo -- they exist only to make the live verification observable and reproducible.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local process -> CFBD API | Outbound authenticated HTTPS call fetching live games/lines for the current season |
| browser/curl -> local Flask dev server | Loopback-only request to `127.0.0.1:5000`, not exposed externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information Disclosure | CFBD token used by `build_upcoming` | medium | mitigate | Task 1 never prints `Configuration`/token; SUMMARY.md captures only game/meta data and rendered HTML fragments. |
| T-09-03 | Tampering | New saved system file (`phase9-permissive-check.json`) written to gitignored `data/systems/` | low | accept | `data/` is gitignored (confirmed via `.gitignore` line 1); no risk of committing live data or leaking it into the repo. |
| T-09-SC | Tampering | No new package installs in this plan | low | accept | Plan makes zero new dependency installs. |
</threat_model>

<verification>
`data/processed/upcoming.csv` is non-empty and was written by a live CFBD fetch (not `sample`/fixtures). The rendered dashboard HTML shows a Current Matches row with a `play` field and no result/grade field for an unplayed, lined game.
</verification>

<success_criteria>
Criterion 2 of Phase 9 is either PASSED with concrete evidence (game id, line, rendered panel text) or explicitly documented as still BLOCKED by the precondition -- never silently skipped or assumed.
</success_criteria>

<output>
Create `.planning/phases/09-live-in-season-verification/09-02-SUMMARY.md` when done
</output>
