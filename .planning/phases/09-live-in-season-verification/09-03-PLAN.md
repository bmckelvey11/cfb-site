---
phase: 09-live-in-season-verification
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - .planning/phases/09-live-in-season-verification/09-03-SUMMARY.md
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "A saved system filtered on a computed season-to-date feature (running_games_played >= 2, the first value only reachable entering week 3+, since week 1 enters at 0 and week 2 enters at 1) is evaluated against the live week-3+ upcoming games."
    - "A second filter on a genuine computed statistic (running_win_pct >= 0.0, home perspective) is evaluated alongside the counter gate, proving a real computed stat -- not just the games_played accumulator -- flows through matching correctly."
    - "The feature values used for matching are real non-null computed statistics (not the week-1/2 games_played=0 fail-closed state) for at least one matched game."
    - "The team-scoped feature is read via a resolvable perspective (home/away/bet_side/either), not the default single perspective, which would resolve to None for a team_scoped feature and match nothing regardless of week."
  artifacts:
    - "data/systems/phase9-season-to-date-check.json (gitignored data artifact, not committed) -- a saved system with running_games_played >= 2 and running_win_pct >= 0.0 feature filters"
    - ".planning/phases/09-live-in-season-verification/09-03-SUMMARY.md"
  key_links:
    - "cfb_system_maker.running_stats.compute_running_stats -> cfb_system_maker.enrich.enrich_games (via upcoming.enrich_upcoming's full-season accumulation base) -> cfb_system_maker.features.feature_ok/resolve_feature_value -> cfb_system_maker.backtest.matches_system"
---

<objective>
Prove, against the real live 2026 season from week 3 onward, that a feature-filtered saved system using `running_games_played` (a `computed_running` season-to-date stat from `running_stats.py`) correctly matches upcoming games using actual computed values -- not week 1-2's `games_played=0` fail-closed state, which is expected behavior and not a bug under test here.

Purpose: Closes the second half of DATA-02 -- confirming the season-to-date accumulator path (`running_stats.compute_running_stats` -> `enrich.enrich_games` -> `features.feature_ok`) produces real non-null values once there is enough season history, using the live pipeline `09-02` already exercised.
Output: `09-03-SUMMARY.md` recording either a PASS with concrete feature values and matched game ids, or a documented HALT if week 3 hasn't arrived yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-live-in-season-verification/09-02-SUMMARY.md
@cfb_system_maker/running_stats.py
@cfb_system_maker/features.py
</context>

<precondition>
Run `.venv/Scripts/python.exe -c "from cfb_system_maker.upcoming import resolve_target_week; from datetime import datetime, timezone; r = resolve_target_week(datetime.now(timezone.utc)); print(r)"` and only proceed with Task 1 if `is_fallback=False` AND `week >= 3` (or `season_type != 'regular'`, since any postseason week necessarily follows week 3+ of the regular season). If the resolved week is 1 or 2 of the regular season, or `is_fallback=True`, HALT: write `09-03-SUMMARY.md` stating "BLOCKED -- resolved to {season}/{season_type}/week {week}, need regular-season week 3+ for season-to-date stats to be non-trivially computed" and stop. Do not lower the `running_games_played` threshold to make an earlier week pass -- per `running_stats.py`, entering week 1 every team's `games_played=0` (fails closed by design), entering week 2 it is `1` for any team that played week 1, and only entering week 3 does `running_games_played >= 2` become reachable -- that is the exact distinction this criterion tests, so the threshold must stay at 2. Do not update ROADMAP.md's Phase 9 status, REQUIREMENTS.md's DATA-02 status, or STATE.md's phase-progress tracking on this path -- the phase remains open until this plan re-runs and passes.
</precondition>

<tasks>

<task type="auto">
  <name>Task 1: Live season-to-date feature filter match against week 3+</name>
  <files>data/systems/phase9-season-to-date-check.json, .planning/phases/09-live-in-season-verification/09-03-SUMMARY.md</files>
  <read_first>cfb_system_maker/running_stats.py, cfb_system_maker/features.py (lines 300-410 computed_running FeatureDefs, lines 505-533 resolve_feature_value/_perspective_to_side, lines 548-580 feature_ok), cfb_system_maker/models.py (FeatureFilter), cfb_system_maker/upcoming.py (enrich_upcoming, _accumulation_base)</read_first>
  <action>
Rerun `.venv/Scripts/python.exe -m cfb_system_maker upcoming --data-dir data` to refresh `data/processed/upcoming.csv` and `data/processed/upcoming_features.json` (via `enrich_upcoming`, which rebuilds the season's completed-game accumulation base from `data/raw/games_{season}.json`/`lines_{season}.json` and computes `running_stats.compute_running_stats` over it) for the current live week -- do this even if 09-02 already ran `upcoming` this session, since more games may have completed since.

Inspect `data/processed/upcoming_features.json` directly (`.venv/Scripts/python.exe -c` a short script loading it with `cfb_system_maker.enrich.load_features_from` and `cfb_system_maker.enrich.upcoming_features_path`) and confirm at least one game has a non-null `home_running_games_played` or `away_running_games_played` key with value >= 2 (the team-scoped storage key is `{side}_running_games_played` per `enrich.py` lines 208-216, since `running_games_played`'s `FeatureDef` has `team_scoped=True`). Record that game's id, team, and the raw `running_games_played`/`running_win_pct`/`running_ats_pct` values found in the SUMMARY as corroboration that real (non-`games_played=0`) stats exist this week.

Create the saved system: construct `SystemFilter(bet_type="spread", side="home", feature_filters=(FeatureFilter(key="running_games_played", op="gte", value=2, perspective="home"), FeatureFilter(key="running_win_pct", op="gte", value=0.0, perspective="home")))` via a short `.venv/Scripts/python.exe -c` script importing `cfb_system_maker.models.SystemFilter`/`FeatureFilter` and `cfb_system_maker.storage.save_system`, and save it as `phase9-season-to-date-check` under `data`. Use `perspective="home"` explicitly on both filters -- the default `perspective="single"` looks up the bare key (e.g. `features.get("running_games_played")`), which does not exist for a `team_scoped=True` feature (only `home_*`/`away_*` prefixed keys are written by enrichment) and would silently match zero games regardless of week, masking the exact distinction this criterion is meant to catch. The second filter (`running_win_pct >= 0.0`) is a genuine computed statistic, not an accumulator counter -- it proves a real computed win-percentage value flows through `resolve_feature_value` -> `feature_ok` -> `matches_system` correctly, not just the `running_games_played` gate.

Evaluate the match directly against the pipeline (no need to restart the web server if 09-02 already confirmed the panel renders correctly -- this task verifies the matching layer, which the panel already exercises): `.venv/Scripts/python.exe -c` a short script that loads `data/processed/upcoming.csv` via `storage.load_upcoming_games`, loads `data/processed/upcoming_features.json` via `enrich.load_features_from`/`enrich.upcoming_features_path`, loads the `phase9-season-to-date-check` system via `storage.load_system`, and calls `backtest.matches_system(record, system, feature_map, require_played=False)` for each upcoming game, printing which game ids match. Confirm at least one match exists and that its `home_running_games_played` feature value (looked up directly from the loaded feature map) is a real integer >= 2, not None and not 0, and that its `home_running_win_pct` value is a real float (not None), confirming the second filter also resolved against a genuine computed statistic. If zero games match this week (e.g. no home team happens to have 2+ games played, which is unlikely but possible in a short week), state that explicitly with the raw feature values observed rather than forcing a false PASS.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from cfb_system_maker.storage import load_system; s = load_system('phase9-season-to-date-check', 'data'); keys = {f.key: f for f in s.feature_filters}; assert len(s.feature_filters) == 2 and keys['running_games_played'].perspective == 'home' and keys['running_win_pct'].perspective == 'home'; print('saved system feature filters confirmed:', s.feature_filters)"</automated>
  </verify>
  <acceptance_criteria>
    `data/systems/phase9-season-to-date-check.json` exists with exactly two feature filters: (1) key `running_games_played`, op `gte`, value `2`, perspective `home`; (2) key `running_win_pct`, op `gte`, value `0.0`, perspective `home`. `data/processed/upcoming_features.json` contains at least one game with `home_running_games_played` or `away_running_games_played` >= 2 (or the SUMMARY explicitly documents zero such games with the raw values observed, and does not mark the criterion PASSED in that case). `09-03-SUMMARY.md` states, with concrete game ids and feature values, whether `matches_system` returned at least one match for the new system and whether that match's underlying `running_games_played` and `running_win_pct` values were real computed numbers rather than the week-1/2 zero/None state.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

Zero new code symbols. This is a verification-only plan against existing code (`cfb_system_maker/running_stats.py`, `cfb_system_maker/features.py`, `cfb_system_maker/backtest.py`); no functions, classes, or modules are created or modified. The data artifact written (`data/systems/phase9-season-to-date-check.json`) is gitignored (`data/` per `.gitignore` line 1) and is not committed to the repo -- it exists only to make the live verification observable and reproducible.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local process -> CFBD API | Outbound authenticated HTTPS call refreshing live games/lines for the current season |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | CFBD token used by the `upcoming` CLI rerun | medium | mitigate | Task 1 never prints `Configuration`/token; SUMMARY.md captures only game ids, feature values, and match results. |
| T-09-05 | Tampering | New saved system file (`phase9-season-to-date-check.json`) written to gitignored `data/systems/` | low | accept | `data/` is gitignored; no risk of committing live data or leaking it into the repo. |
| T-09-SC | Tampering | No new package installs in this plan | low | accept | Plan makes zero new dependency installs. |
</threat_model>

<verification>
`data/systems/phase9-season-to-date-check.json` has the correct two feature filters (`running_games_played >= 2`, `running_win_pct >= 0.0`), both with `perspective="home"`. At least one live upcoming game's `home_running_games_played`/`away_running_games_played` is a real integer >= 2 this week, and `matches_system` correctly matches on both filters (or the SUMMARY documents an inconclusive zero-match week with raw evidence, never a forced pass).
</verification>

<success_criteria>
Criterion 3 of Phase 9 is either PASSED with concrete evidence (game id, computed feature value, match result) or explicitly documented as still BLOCKED by the precondition (week 1-2) -- never silently skipped, never satisfied by a games_played=0/1 false positive.
</success_criteria>

<output>
Create `.planning/phases/09-live-in-season-verification/09-03-SUMMARY.md` when done
</output>
