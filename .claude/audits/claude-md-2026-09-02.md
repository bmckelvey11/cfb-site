# CLAUDE.md / AGENTS.md audit — cfb — 2026-09-02

Audit-only. Nothing modified. Executor: haiku for the diffs below; F5 needs judgment (sonnet or human).

## Inventory (always-loaded at launch from repo root)

| File | Lines | Bytes | Loads |
|---|---|---|---|
| `C:\Users\mckel\CLAUDE.md` (parent dir) | 88 | — | every session under home |
| `~/.claude/CLAUDE.md` (user) | 50 | — | every session |
| `CLAUDE.md` (root, **uncommitted edits**, `git status` = ` M`) | 42 | 2370 | launch |
| `.claude/CLAUDE.md` | 25 | 882 | launch |
| `cfb_system_maker/CLAUDE.md` | 138 | 17341 | on demand |
| `models/totals/CLAUDE.md` | 13 | 704 | on demand |
| `models/over_zero/CLAUDE.md` | 15 | 840 | on demand |
| `research/spread/CLAUDE.md` | 16 | 1058 | on demand |
| `AGENTS.md` | 2 | 151 | never (pointer only) |
| `.claude/worktrees/*/CLAUDE.md` ×2 | 36 | — | only inside worktree |

Pass: no file over 200 lines. No `@path` imports anywhere (check 4: n/a). `AGENTS.md` holds no rules, just points at `CLAUDE.md` (check 3: pass). No linter or formatter configured (`pyproject.toml`, `ruff.toml`, `.pre-commit-config.yaml` all absent), so check 5 reduces to hooks.

## Findings (severity order)

### F1 · HIGH · Self-contradicting rule in root `CLAUDE.md:12`
Line 12 says both "offer to persist it as a script" and "always write a reusable script." One is a prompt, the other a mandate. Claude "may pick one arbitrarily." This line is in the uncommitted diff (`git diff --stat CLAUDE.md` → +7/-1).

```diff
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -10,4 +10,4 @@
-- After finishing tasks/findings tracked in a doc (review docs, plans, TODO lists), update that doc marking each  item done + the completion date — don't leave it stale once the work lands.
+- After finishing tasks/findings tracked in a doc (review docs, plans, TODO lists), update that doc marking each item done + the completion date — don't leave it stale once the work lands.
 
-- After any analysis, offer to persist it as a script — written to be universal, not a one-off. always write a reusable script for any modelling or backtesting. if it needs to be reproduced, write a script.
+- Any modelling, backtesting, or analysis that might be reproduced gets a reusable script, not a one-off. Write the script as part of the task.
```

### F2 · HIGH · `.claude/CLAUDE.md:8-16` restates the `gsd-workflow-guard.js` hook, and disagrees with it
`~/.claude/settings.json` registers `gsd-workflow-guard.js` on PreToolUse `Bash|Edit|Write|MultiEdit`. The hook's own header (lines 4-8) says it "nudges Claude to use /gsd-quick or /gsd-fast" on edits outside a GSD context. `.claude/CLAUDE.md:12` routes small fixes to `/gsd-quick`; the hook routes trivial fixes to `/gsd-fast` (hook line 156). Two sources, different answer. The hook fires regardless; the CLAUDE.md copy adds context cost and a conflict.

```diff
--- a/.claude/CLAUDE.md
+++ b/.claude/CLAUDE.md
@@ -6,12 +6,7 @@
 <!-- GSD:workflow-start source:GSD defaults -->
 
 ## GSD Workflow Enforcement
 
-Prefer GSD so planning artifacts stay in sync:
-
-- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
-- `/gsd-debug` for investigation and bug fixing
-- `/gsd-execute-phase` for planned phase work
-
-Small fixes and doc edits may go straight to the files. Root `CLAUDE.md` is the agent source of truth; this file is the GSD overlay (`claude_md_path`).
+GSD routing is enforced by the `gsd-workflow-guard` PreToolUse hook; do not duplicate it here. `/gsd-debug` for investigation, `/gsd-execute-phase` for planned phase work. Root `CLAUDE.md` is the agent source of truth; this file is the GSD overlay (`claude_md_path`).
 <!-- GSD:workflow-end -->
```

Risk note: this block sits inside `GSD:workflow-start/end` markers and may be regenerated by GSD tooling. If it is, the fix belongs in GSD config, not here. Executor should check `.planning/config.json` for a workflow-template setting before applying.

### F3 · MEDIUM · Nested unit files restate root rules they say they defer to
Each nested file opens with "Shared data, archive, and no-lookahead rules live in root `CLAUDE.md`" and then restates them.

| Root rule | Root line | Restated at |
|---|---|---|
| Don't edit `cfbd-python/` | 30 | `cfb_system_maker/CLAUDE.md:11` |
| `CFB_DATA_ROOT` via `cfb_paths.py`, no fallback | 25-27 | `cfb_system_maker:57-58`, `:88-89`; `models/over_zero:7`; `models/totals:11`; `research/spread:7` |
| MotherDuck is a manual mirror, local is truth | 26-27 | `cfb_system_maker:70` |
| Run from repo root | 33 | `models/over_zero:6`; `research/spread:6` |
| Default `python -m pytest`, slow excluded | 34 | `cfb_system_maker:18-19`, `:134` |
| No lookahead / `result_lookahead` tag | 31-32 | `cfb_system_maker:123` |

Restatement is not contradiction, but each copy drifts independently (root says `pytest.ini` excludes slow; `cfb_system_maker:18` spells out the addopts; both currently match `pytest.ini`). Cut the pure restatements; keep unit-specific detail.

```diff
--- a/models/over_zero/CLAUDE.md
+++ b/models/over_zero/CLAUDE.md
@@ -3,8 +3,5 @@
 Scope: Arscott, saturation, floor-bias, and monitoring work under this tree. Shared data,
 archive, and no-lookahead rules live in root `CLAUDE.md`.
 
-- Run commands from repository root unless a script documents a narrower working directory.
-- Resolve raw and processed data through root `cfb_paths.py` and required `CFB_DATA_ROOT`.
-  Never recreate an in-repository `data/` directory.
 - Historical model copies may use local sibling imports; preserve numerical behavior when
   changing paths.
```

```diff
--- a/models/totals/CLAUDE.md
+++ b/models/totals/CLAUDE.md
@@ -9,5 +9,4 @@
   no-bets.
 - Never cite leaked-era 57% / +8.82% ROI.
-- `cfb_paths.DATA_ROOT` supplies warehouse/data location; no repository-relative fallback.
 - Tests: `tests/test_totals_clv.py`, `tests/test_totals_inference.py`, and
   `tests/test_totals_model.py`.
```

```diff
--- a/research/spread/CLAUDE.md
+++ b/research/spread/CLAUDE.md
@@ -4,7 +4,5 @@
 supporting scripts. Shared data, archive, and no-lookahead rules live in root `CLAUDE.md`.
 
-- Run scripts from repository root.
-- Source data resolves through root `cfb_paths.py`; raw Prediction Tracker source belongs
-  under `$CFB_DATA_ROOT/raw/prediction_tracker/`.
+- Raw Prediction Tracker source belongs under `$CFB_DATA_ROOT/raw/prediction_tracker/`.
 - Preserve preregistration order. Parent model-evaluation plan remains binding; addendum
```

```diff
--- a/cfb_system_maker/CLAUDE.md
+++ b/cfb_system_maker/CLAUDE.md
@@ -55,6 +55,3 @@
 
-Canonical data dir is required `CFB_DATA_ROOT`, resolved by root `cfb_paths.py`. Explicit
-`--data-dir` still wins where a command supports it; no repository-relative fallback exists.
-
+Explicit `--data-dir` wins over `CFB_DATA_ROOT` where a command supports it.
@@ -121,3 +118,2 @@
-- **No lookahead:** new registry features must be computable pre-game (entering-game state) or tagged `result_lookahead` and quarantined in the UI. Do not fold a game's own result into its features.
 - **CFBD field names are inconsistent**
```

(`cfb_system_maker:11`, `:70`, `:88-89`, `:134` carry unit-specific detail alongside the restatement; leave those.)

### F4 · MEDIUM · `~/.claude/CLAUDE.md:29-50` loads dl-master preferences into every project
22 of 50 user-scope lines are "Learned User Preferences (dl-master)" and "Learned Workspace Facts (dl-master)": OnlyFans paths, bunkr queues, worker counts. Loaded into cfb, video-wall, finances, every session. Line 50 already names `C:\Users\mckel\dev\dl-master\CLAUDE.md` as that repo's memory. Move the two sections there (or into dl-master auto-memory) and delete here.

```diff
--- a/C:/Users/mckel/.claude/CLAUDE.md
+++ b/C:/Users/mckel/.claude/CLAUDE.md
@@ -27,24 +27 @@
 When the user types any of those, invoke the Skill tool with `skill: "perplexity"` before doing anything else.
-
-## Learned User Preferences (dl-master)
-
-[lines 31-38, unchanged content → append to C:\Users\mckel\dev\dl-master\CLAUDE.md]
-
-## Learned Workspace Facts (dl-master)
-
-[lines 42-50, unchanged content → append to C:\Users\mckel\dev\dl-master\CLAUDE.md]
```

Executor: cut lines 29-50 verbatim, append to dl-master `CLAUDE.md`, confirm dl-master's `AGENTS.md` sync note (line 50) still holds.

### F5 · MEDIUM · `cfb_system_maker/CLAUDE.md` is 17KB in 138 lines; ~40% is derivable from code — NEEDS JUDGMENT
Lines 60, 62, 128 each exceed 800 characters. Sections "What this is" (7-11) and "Pipeline & architecture" (46-72) describe module wiring that `/doctor`'s trim rule classifies as derivable (directory layouts, architecture overviews). Check 6 candidates: the MotherDuck promote runbook at line 70 already points to `docs/duckdb-warehouse-plan.md`; the `--only` phase-matching rules at 96-100 and explode-lists at 105-112 are multi-step procedures better held in a skill or `.claude/rules/` file scoped to `cfb_system_maker/**`. No diff proposed: deciding what is "pitfall vs derivable" needs the owner. Suggested split: keep Commands, Scraper conventions, Key conventions; move Pipeline & architecture to `docs/` and reference it.

### F6 · LOW · `.claude/CLAUDE.md:19-25` unconfigured profile placeholder contradicts "owns GSD workflow only"
Root `CLAUDE.md:4-5` says `.claude/CLAUDE.md` "owns only GSD workflow." Lines 19-25 are a "Developer Profile … not yet configured" stub marked "do not edit manually." Either run `/gsd-profile-user` or accept 7 dead lines per session. No diff (managed block).

### F7 · LOW · `~/.claude/CLAUDE.md:22-27` restates platform slash-command dispatch
"When the user types `/graphify`, invoke the Skill tool" — typed slash commands already invoke the skill; the description in each SKILL.md handles inference. Six lines, redundant with platform feature.

```diff
--- a/C:/Users/mckel/.claude/CLAUDE.md
+++ b/C:/Users/mckel/.claude/CLAUDE.md
@@ -21,7 +21 @@
 **Rule of thumb**: If the user can do the last step themselves in 2 seconds (open a file, run a command, click a link), hand it to them instead of automating it with expensive tooling.
-# graphify
-- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
-When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.
-# perplexity
-- **perplexity** (`~/.claude/skills/perplexity/SKILL.md`) - web-grounded search/research/reasoning. Triggers: `/perplexity-ask`, `/perplexity-search`, `/perplexity-research`, `/perplexity-reason`
-When the user types any of those, invoke the Skill tool with `skill: "perplexity"` before doing anything else.
```

Risk note: these lines look tool-generated (installer stubs). If a skill installer rewrites them on update, deleting is churn; harmless either way.

### F8 · LOW · `research/spread/CLAUDE.md:14` paths resolve only from the unit dir
Line 6 says run from repository root; line 14 cites `docs/prereg-line-movement.md` and `docs/line-movement-results.md`. From root those are MISS; `research/spread/docs/prereg-line-movement.md` and `research/spread/docs/line-movement-results.md` exist.

```diff
--- a/research/spread/CLAUDE.md
+++ b/research/spread/CLAUDE.md
@@ -14,2 +14,2 @@
-  the actual goal: closing line value. See `docs/prereg-line-movement.md` and
-  `docs/line-movement-results.md`. Treat open questions as research, not validated edge.
+  the actual goal: closing line value. See `research/spread/docs/prereg-line-movement.md` and
+  `research/spread/docs/line-movement-results.md`. Treat open questions as research, not validated edge.
```

### F9 · INFO · Root `CLAUDE.md` edits are uncommitted; worktree copies lag
`git status --short CLAUDE.md` → ` M`. Both `.claude/worktrees/*/CLAUDE.md` lack the Standing Rules block (diff confirmed: lines 8-13 absent). Both worktrees and root share last commit `4074488` for this file, so this is one pending commit, not drift. Resolves on commit + merge. No diff.

### F10 · INFO · Root `CLAUDE.md:4-5` names `CONTEXT.md` and `PRODUCT.md` as owners of terminology and intent, but neither loads
Both exist at root. Nothing imports them. If they should be in context every session, add `@CONTEXT.md`; if not, fine as-is. Owner call.

## Verified negatives
- `models/over_zero/CLAUDE.md:14` "no dedicated pytest suite": true for collection (`pytest.ini` testpaths=tests). Two `test_*.py` files exist under `models/over_zero/research/b6_*`, `b7_*` but are not collected.
- `cfb_system_maker/CLAUDE.md:44` "no linter configured": true.
- All 22 file paths cited across the CLAUDE.md files exist, except the two in F8.
- `gsd-validate-commit.js` enforces Conventional Commits + 72-char subject; no CLAUDE.md restates it. Pass.

## Status — applied 2026-09-02

Applied by haiku executor, verified by Fable: F1, F2, F3, F7, F8. F4: deleted from `~/.claude/CLAUDE.md`; the append to dl-master was reverted because `dl-master/CLAUDE.md` already held newer versions of both sections (queue paths under `queues/`, in-repo bunkr), so the ~/.claude copy was stale, not missing. F2 regen check: no controlling key in `.planning/config.json`. Open: F5, F6, F10 (owner calls). F9 resolved by this commit.

## Apply order for executor
F1 → F8 → F3 → F7 → F4 → F2 (check regen risk first). Skip F5, F6, F9, F10. After each file, `python -m pytest -q` is not affected (docs only); verify with `git diff --stat` and a `/context` check that all memory files still load.
